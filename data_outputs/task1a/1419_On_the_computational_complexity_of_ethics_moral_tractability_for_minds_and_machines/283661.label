{
  "style": "",
  "texts": [
    "This yields the following problem: c2: optimal combination of values Input: Same as c1 with the addition of a set of outcome value functions",
    "Output: An action a such that v(a) is maximized over all v ∈ V and a ∈ E.",
    "If we posit that the values interact trivially, in the sense that values can be summarized v 1 (a) + v 2 (a) + … + v i (a) to yield a single total value V(a) (i.e., obeying the law of additivity), the optimal action a * can be formally expressed as:",
    "If the agent needs to make distinct calls to the oracle for each value, the time complexity is the product of n (number of actions) and i (number of values), yielding O(ni).",
    "If i is equal to the number of actions, the runtime grows quadratically in relation to n, which still yields the polynomial O(n 2)."
  ],
  "correct_citations": []
}