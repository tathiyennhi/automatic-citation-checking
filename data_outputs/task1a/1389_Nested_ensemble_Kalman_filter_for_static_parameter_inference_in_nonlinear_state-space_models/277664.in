{
  "texts": [
    "enkf,t (x t |y 0:t, x1:N t), j = 1.",
    ", N, via a single run of Algorithm 3 with inputs θ, resampled particles x",
    "t-1 and a suitable choice of H and R. It should be clear that the unnormalised weight of the jth particle x j t is given by wt",
    "enkf,t (y t |y 0:t-1, x1:N t) is obtained from the single run of Algorithm 3 in the propagate step.",
    "We note that p (θ) t (x t |x t-1) is sampled in step 1 of Algorithm 3 when generating the forecast ensemble, but need never be evaluated, since the corresponding forecast density, p enkf,t (x t |y 0:t-1, x1:N t), appears in both p (θ) enkf,t (x t |y 0:t, x1:N t) and g (θ) enkf,t (x t |y 0:t, x1:N t), and therefore cancels in the weight This necessarily sacrifices inferential accuracy (compared to targeting p (θ)"
  ]
}