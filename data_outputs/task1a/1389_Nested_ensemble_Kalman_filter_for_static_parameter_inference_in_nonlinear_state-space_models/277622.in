{
  "texts": [
    "Update weights and likelihood.",
    "Run Algorithm 1 with inputs θ i and {x i,1:N t-1, w i,1:N t-1 } to obtain p (θ i) t (y t |y 0:t-1, x i,1:N t, a i,1:N t-1) and {x i,1:N t, w i,1:N t }.",
    "Construct the weight",
    "and update the likelihood",
    "). if ESS < γM then 2."
  ]
}