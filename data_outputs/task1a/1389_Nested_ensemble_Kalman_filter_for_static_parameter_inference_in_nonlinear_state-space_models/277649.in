{
  "texts": [
    "After completion of the mutation step, if updating N is triggered then for each particle θ i, ensemble members x i,1:N 0:t are replaced by xi,1: Ň 0:t using the generalised importance sampling strategy of Del Moral et al. (2006), with an artificial backward kernel chosen to give an incremental weight of 1 (Botha et al., 2023b).",
    "Hence, for each particle θ i, a new ensemble xi,1: Ň 0:t is generated through repeated application of Algorithm 3.",
    "Once the updating of N is triggered, we adapt the method of Duan and Fulop (2015) to our context, by setting Ň = σ2",
    "N N, where σ2",
    "N is an estimate of the variance of p Input: weighted parameter particles {θ i, u i t-1 }, associated ensemble members x i,1:N t-1 and likelihood evaluations p (θ i) enkf,t-1 (y 0:t-1 |x i,1:N 0:t-1), i = 1,."
  ]
}