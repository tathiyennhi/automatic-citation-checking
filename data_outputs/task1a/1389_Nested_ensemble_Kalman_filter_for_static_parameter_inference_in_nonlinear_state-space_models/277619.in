{
  "texts": [
    ") is the estimate of observed-data likelihood obtained by running Algorithm 1 with parameter θ i and the associated state particle system {x i,1:N t-1, w i,1:N t-1 }.",
    "Hence, over the entire observation period, each particle θ i has associated state particles and genealogies {x i,1:N 0:T, a i,1:N 0:T -1 }.",
    "Parameter weight degeneracy is monitored via effective sample size (ESS), which is calculated at time t using the normalised parameter particle weights as",
    "If ESS falls below some user specified threshold, γM, a resample-move step is triggered by drawing M times from the mixture M i=1 u i t K t θ i, •, where K t is a pseudo-marginal Metropolis-Hastings kernel with target π t (θ|y 0:t).",
    "Hence, after resampling, a new particle θ * is proposed from a kernel q(θ i, •) and accepted with probability"
  ]
}