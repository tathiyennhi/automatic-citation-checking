{
  "style": "APA",
  "texts": [
    "Suppose now that f (θ) t can be evaluated point-wise and may be neither linear nor Gaussian.",
    "Key to our approach in this scenario is that f (θ) t can be approximated by a linear Gaussian conditional density which we denote by g (θ)",
    "t (•|x) and whose form is given by (8).",
    "For example, if f (θ) t (y|x) = N (y t; h(x t), R) for some nonlinear function h(•) (which may depend on θ), an approximate density g (θ) t can be found be taking the first two terms of a Taylor series expansion of h(•) about the forecast mean µ t|t (see e.g.",
    "Evensen et al., 2022), or by augmenting x t to include h(•) in the forecast ensemble (see e.g."
  ],
  "correct_citations": [
    "Evensen et al., 2022), or by augmenting x t to include h(•) in the forecast ensemble (see e.g."
  ]
}