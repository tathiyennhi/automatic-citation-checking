{
  "texts": [
    ", log p (θ [k]) enkf (y 0:t |x [k],1:N 0:t",
    "We then approximate the log-likelihood via the weighted average log p",
    "where d [i] denotes the distance (using some metric) between θ and θ [i].",
    "Note that we suppress explicit dependence of the surrogate on the k-nearest neighbours for notational simplicity.",
    "For a given particle θ i, Stage One of the DA scheme proposes θ * ∼ q(θ i, •), computes p (θ *) knn,t (y 0:t) and the screening acceptance probability"
  ]
}