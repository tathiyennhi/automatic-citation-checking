{
  "style": "APA",
  "texts": [
    "We use the same hyperparameter values from Dettmers et al. (2023) to avoid additional hyperparameter tuning.",
    "Depending on the dataset, training on four GPUs takes 25 minutes to 17 hours.",
    "For more additional details, see Appendix F.5.",
    "Evaluation We evaluate the performance of the models on the test splits of the target datasets (see Table 6 in Appendix A).",
    "To prevent \"prompt hacking\", following Sanh et al. (2022), we first write five prompt templates for the target datasets and then benchmark the model performance."
  ],
  "correct_citations": [
    "We use the same hyperparameter values from Dettmers et al. (2023) to avoid additional hyperparameter tuning.",
    "To prevent \"prompt hacking\", following Sanh et al. (2022), we first write five prompt templates for the target datasets and then benchmark the model performance."
  ]
}