{
  "texts": [
    "Then, using the data set, we can infer the model's parameters (i).",
    "The model will be interpretable because we will know the impact of each variable on the output.",
    "For example, if 3 is negative, then that implies that as r increases, the force F will decrease.",
    "From physics, we know that this form of the model is unlikely to be accurate.",
    "On the other hand, we could model the input-output relationship using a neural network (NN), i.e., F = NN(q 1, q 2, r,)."
  ]
}