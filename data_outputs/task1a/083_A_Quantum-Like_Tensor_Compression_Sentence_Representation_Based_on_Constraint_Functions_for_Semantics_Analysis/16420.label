{
  "style": "",
  "texts": [
    "Combination of AQE and WQEThe similarity of the ith sentence pair is calculated using WQE when γ 2 < y 2i < σ 2, f i (x) > 0 and g i (x) > 0, with i = 1, 2, • • •, N. AQE is used to compute the similarities of other sentences which do not adhere to the constraint functions.",
    "Combination of the Three ModelsBy considering the distinct values of constraint functions 16 and 17, a fusion of AQE, SQE, and WQE is employed to extract a more comprehensive range of semantic information.",
    "The sentence similarity is computed using SQE once more when y 1i > σ 1, f i (x) > 0 and g i (x) > 0.",
    "The similarity of sentence pairs is recalculated via WQE or SQE when γ 2 < y 2i < σ 2, f i (x) > 0 and g i (x) > 0.",
    "To minimize the MSE and maximize the Pcc, the parameter adjustment guidelines are as follows."
  ],
  "correct_citations": []
}