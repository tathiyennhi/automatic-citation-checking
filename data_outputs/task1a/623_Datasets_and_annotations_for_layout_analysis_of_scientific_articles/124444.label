{
  "style": "IEEE",
  "texts": [
    "In addition, tables in HTML format are represented as a tree structure.The PDF and XML files are matched through the algorithm of [49], following the general schema described in Sect. 3.2.2 to detect table regions and subsequently to convert them to images.",
    "Table regions are identified within the two sources and kept if the cosine similarity of the TF-IDF features of the two texts is larger than 90% and their length differs by less than 10%; rare tables with formulas or no frequent characters are removed as well.",
    "Finally, each table is rendered as image along with its HTML ground-truth annotation, without hyperlinks and metadata unused for the downstream task.",
    "FinTabNet and PubXNetTwo datasets are proposed in [48]: (i) PubXNet, is an enhanced version of PubTabNet [64] adding table cells labels for a joint subset of PubTabNet and PubLayNet [49], (ii) the FinTabNet dataset is proposed to perform table detection and structure recognition on financial documents.",
    "Since HTML was already available, authors directly matched that information with the source PDF for automatic labeling."
  ],
  "correct_citations": [
    "In addition, tables in HTML format are represented as a tree structure.The PDF and XML files are matched through the algorithm of [49], following the general schema described in Sect. 3.2.2 to detect table regions and subsequently to convert them to images.",
    "FinTabNet and PubXNetTwo datasets are proposed in [48]: (i) PubXNet, is an enhanced version of PubTabNet [64] adding table cells labels for a joint subset of PubTabNet and PubLayNet [49], (ii) the FinTabNet dataset is proposed to perform table detection and structure recognition on financial documents."
  ]
}