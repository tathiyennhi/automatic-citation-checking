{
  "texts": [
    "about the local fusion feature block, extracts visual features at multiple scales using three-pass networks with distinct convolution kernels.",
    "The channel attention module, self-attention module, and fusion operation are all parts of the global feature fusion module-enhances the MR image's key features.",
    "For training the network, 40 sets of 3D MRI images with 256 slices are used, and 10 sets are employed to measure the proposed methodology.Ood-GAN [43] discovered by Marek et al.",
    "A robust dialogue system must detect OOD utterances.",
    "Most dialogue systems achieve this by training on annotated OOD data."
  ]
}