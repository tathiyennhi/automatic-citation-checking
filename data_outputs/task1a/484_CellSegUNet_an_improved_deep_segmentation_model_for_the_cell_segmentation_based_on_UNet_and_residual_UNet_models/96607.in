{
  "texts": [
    "model, details such as spatial geometric properties of objects are not very clear.",
    "In addition, when deep network structures are created with UNet and UNet??",
    "based architectures, the system may slow down toward the middle layers.",
    "This is because gradients are diluted farther from the output of a network where the error is calculated, resulting in slower learning for very far weights.",
    "There is also the risk of ignoring layers where abstract features are represented [3]."
  ]
}