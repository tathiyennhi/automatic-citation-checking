{
  "style": "",
  "texts": [
    "The transformation back to the spatial domain integrates this fine-tuned information, preparing the model for inference or further training.",
    "The activation memory footprint of MaCP is significantly reduced because only a small subset of spectral coefficients n is updated while keeping the remaining parameters frozen.",
    "The activation memory for MaCP consists of two components: (1) memory for the forward pass, requiring B • S • H to store activations for all input-output pairs, where S is the sequence length, H is the embedding dimension, and B is the batch size; and (2)memory for backpropagation, limited to the selected spectral coefficients, B • n. Thus, the total activation memory footprint for MaCP is:",
    "compared to LoRA, which requires:",
    "where n < S • H, ensuring MaCP consumes less memory."
  ],
  "correct_citations": []
}