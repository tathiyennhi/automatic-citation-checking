{
  "texts": [
    "Convolution, C 1 channel number of the first convolution layer, K 1 kernel size of the first convolution layer, act. activation, no.",
    "number, cat.",
    "Category, FC fully connected; + Layer normalization 51 is applied instead of batch normalization 52.",
    "Transfomer blocks were added prior to global pooling; since the output of the convolutional layer was batch size × height × width × channel, we reshaped the output to batch size × height * width × channel before feeding into the transformer block; the output from the transformer 48 was reshaped back to the dimension of the convolutional layer before global pooling was applied.",
    "For all the models, a patch dimension of 512 × 512 × 3 is applied."
  ]
}