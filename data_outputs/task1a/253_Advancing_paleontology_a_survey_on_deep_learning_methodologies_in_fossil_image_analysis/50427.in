{
  "texts": [
    "Consequently, the mathematical representation can expressed as:",
    "where f is the 3D image data and g is the 3D kernel.",
    "CNNs designed for 3D data are particularly advantageous when the internal structure of a given specimen is of significance.",
    "A typical CNN architecture for classification includes convolutional layers, activation functions (e.g., ReLU), pooling layers, flattening layers, and fully connected layers.",
    "Convolutional layers extract features like edges and shapes, while deeper layers identify more complex patterns."
  ]
}