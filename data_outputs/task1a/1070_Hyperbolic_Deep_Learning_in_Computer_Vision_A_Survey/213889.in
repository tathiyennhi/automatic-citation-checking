{
  "texts": [
    "The pretext task is to predict the representation z t+δ of a clip δ frames into the future.",
    "The model produces an estimate ẑt+δ = φ(c t, δ), where c t = g(z 1,.",
    ", z t) is an encoding of all past video frames.",
    "All function f, g, φ are parameterized by a neural network.",
    "The training is supervised by a contrastive loss:"
  ]
}