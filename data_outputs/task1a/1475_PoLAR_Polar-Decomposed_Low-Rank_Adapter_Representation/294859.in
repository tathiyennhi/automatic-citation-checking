{
  "texts": [
    "Since X ∈ St(m, r) is uniformly sampled from St(m, r) using methods described in Lemma 6, we can write X = Z(Z ⊤ Z) -1/2, where Z ∈ R m×r has entries iid sampled from N (0, 1).",
    "We thus have",
    "It is clear that entries of U ⊤ Z are also iid Gaussian random variables N (0, 1).",
    "As a consequence of Lemma 8, we have that w.p. at least 1 -",
    "We also have from Lemma 7 that with probability at least 1exp(-m/2)"
  ]
}