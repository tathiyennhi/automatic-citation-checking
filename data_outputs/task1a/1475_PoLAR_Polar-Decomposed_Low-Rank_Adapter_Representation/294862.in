{
  "texts": [
    "where A ∈ R m×N holds m labels for each example, W 0 ∈ R m×n is the pre-trained weight, and",
    "where (a) uses the fact that the data is whitened and (b) defines Λ = AD ⊤ ∈ R m×n, i.e., the matrix to be factorized.",
    "Thus, we can write",
    "F + c for Λ ′:= W 0 -Λ and constant c:= -Tr(ΛΛ ⊤) + Tr(AA ⊤).",
    "Using the same arguments, one can frame low-rank adaptation of a linear model with the PoLAR parameterization as the matrix factorization problem given in (4)."
  ]
}