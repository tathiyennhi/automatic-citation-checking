{
  "texts": [
    "Then for every τ ≥ 0, with probability at least 1exp(-τ 2 /2), we have",
    "Lemma 8 (Rudelson and Vershynin (2009)).",
    "If Z ∈ R m×r is a matrix whose entries are independently drawn from N (0, 1).",
    "Suppose that m ≥ r. Then for every τ ≥ 0, we have for some universal constants C 1 > 0 and C 2 > 0 that",
    "If U ∈ St(m, r A) is a fixed matrix, X ∈ St(m, r) is uniformly sampled from St(m, r) using methods described in Lemma 6, and r > r A, then we have that with probability at least"
  ]
}