{
  "texts": [
    "Phase I. Tr(I r A -Φ t Φ ⊤ t) ≥ r A -0.5.",
    "This is the initial phase, and the condition is equivalent to Tr(Φ t Φ ⊤ t) ≤ 0.5.",
    "For notational convenience, let the SVD of",
    "≤ 1, we can simplify (20) as",
    "where (a) uses σ r A (I r A -Λ t) ≥ 0.5; (b) uses Lemmas 15 and 11, which jointly imply that"
  ]
}