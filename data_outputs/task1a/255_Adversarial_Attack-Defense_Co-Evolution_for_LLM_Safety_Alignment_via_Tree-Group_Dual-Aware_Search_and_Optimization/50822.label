{
  "style": "",
  "texts": [
    "The attack's goal is to generate modified queries q that maximize jailbreak performance-i.e., high harmfulness ĵh and strong co-relevance ĵc to the original malicious query.",
    "The placeholder mapping configuration for M A is as follows: Input q ← P A (the rewriting prompt); Output o ← q (modified queries from M A); Reward r ← rA = ĵh + ĵc 2.",
    "Configuration.",
    "The defense's goal is to generate responses (ô) each with low harmfulness (j h) and high responsibility (j r), even to aggressive queries.",
    "We select the modified query (q m = arg max q j h) with the highest harmfulness score within q as the typical query for this node, enabling the M D to effectively defend against the node's most aggressive query after training."
  ],
  "correct_citations": []
}