{
  "texts": [
    "After obtaining the solution, fictitious samples were generated by copying existing ones and adding them to the dataset.",
    "NN training.",
    "The data was split into training and test sets in a 10-fold cross-validation manner to train the network.",
    "The network was trained using sharpness-aware minimization 73 with a stochastic optimization method called AdamW 74 as the base optimizer (learning rate = 1 × 10 -3, weight decay = 0.1).",
    "The data sequence length was randomly chosen from 4 to 32 for every training iteration."
  ]
}