{
  "texts": [
    "number of sites(23 versus 19)",
    ", which will result in more modes in the high dimensional distribution, which is harder to learn.",
    "Furthermore, using a larger dataset like BraTS 2021 makes it harder for the generative models to memorize the training images 9, 10",
    "It would be very interesting to compare our results to SinGAN-Seg 27",
    ", where a single image is used to train the generative model, but we suspect that such a model is prone to memorization."
  ]
}