{
  "texts": [
    "It adapts the other components to 3D operations as well.",
    "A Swin Transformer block consists of a 3D shifted window-based MSA module followed by a feedforward network (FFN), a MLP.",
    "Layer normalization is applied before each MSA module and FFN, and a residual connection is applied after each module.",
    "Consider a stage with T × H × W input tokens.",
    "If the 3D window size is P × M × M, we partition the input into"
  ]
}