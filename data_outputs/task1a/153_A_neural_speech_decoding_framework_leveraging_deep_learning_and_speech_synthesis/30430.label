{
  "style": "",
  "texts": [
    "Finally, our decoding pipeline is generalizable to unseen words (Fig. 2b).",
    "This provides an advantage compared",
    "Transposed conv (3) Transposed conv (3) Transposed conv (3) Transposed conv (3) Spatial MaxPool Res block (3 x 3 x 3) Res block (3 x 3 x 3) Res block (3 x 1 x 1) Res block (5 x 1 x 1) Conv (9 x 1 x 1) C × T C × T SWIN transformer block Patch merging Patch merging SWIN transformer block SWIN transformer block a b d c Temporal conv (k = 3) Temporal conv (k = 3) Feature for speech parameters Channel MLP Channel MLP (Linear) T × F T × F (Mel) Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP Channel MLP L t L t α t α t Temporal conv (k = 3) Temporal conv (k = 3) Temporal conv (k = 5) Temporal conv (k = 5) Spectrogram ECoG (x)",
    "T × 64 T × 128 Channel MLP e T × 128 T × 128 f i = 1 to 6 t a i = 1 to 6 t f û t f 0 t f 0 t a û t b û t f i = 1 to 6 t a i = 1 to 6 t f û t a û t b û t Channel MLP Channel MLP Channel MLP Feature for speech parameters Feature for speech parameters Feature for speech parameters 2C We input a spectrogram into a network of temporal convolution layers and channel MLPs that produce speech parameters.",
    "b,c, The ECoG decoder (c) using the 3D ResNet architecture."
  ],
  "correct_citations": []
}