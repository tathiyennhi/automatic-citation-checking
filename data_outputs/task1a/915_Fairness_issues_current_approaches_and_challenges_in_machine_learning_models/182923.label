{
  "style": "IEEE",
  "texts": [
    "They need these datasets for testing the fairness of RAIs or other predicting models.",
    "Scholars have introduced approaches to test if a model prediction is biased toward any group [110,111].",
    "However, if we apply these approaches to predictive models with biased datasets, the results may not indicate that even though the model is fair.",
    "This situation will make the purpose of the unfairness testing algorithms ambiguous.",
    "Thus, if some scholars remove some biases from a few datasets and make them publicly available, other scholars can look into them and work on removing other biases from those datasets."
  ],
  "correct_citations": [
    "Scholars have introduced approaches to test if a model prediction is biased toward any group [110,111]."
  ]
}