{
  "texts": [
    "The benchmark problem con- sists in the identification of the combination of masses and Hooke's constants of spring x = [m 1, m 2, k 1, k 2] that mini- mizes h 1 (t = 6) considering the domain = [1, 4] 4 and the initial conditions of motion h 1 = h 2 = 0 and ̇h1 = ̇h2 = 0.",
    "(44) m 1 ḧ1 (t) = (-k 1 -k 2) h 1 (t) + k 2 h 2 (t) (45) m 2 ḧ2 (t) = k 2 h 1 (t) + (-k 1 -k 2) h 2 (t).",
    "Paciorek Function with NoiseThe Paciorek function reproduces an optimization settings where the objective function is affected by measurement noise and localized multimodal behaviour.",
    "This scenario is replicated through a uniformly distributed random noise over the two levels of fidelity defined as follows (Fig. 10):where A = 0.5, = 0.2, and the input variable is defined across the input domain = [0, 3, 1] 2.",
    "Results and DiscussionFirst, we define the following evaluation metrics to assess the performances of the Bayesian schemes [83]: where x * is the location of the analytical optimum, x * is the optimum identified by the algorithm, and f max and f * are the maximum and minimum of the objective function, respectively."
  ]
}