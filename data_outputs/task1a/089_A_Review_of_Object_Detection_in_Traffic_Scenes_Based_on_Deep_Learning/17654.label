{
  "style": "IEEE",
  "texts": [
    "In this case, if the IoU values of both bounding boxes are greater than a predefined threshold (usually 0.5), it means that the object has been correctly recognized [9,[93][94][95].IoU is calculated as follows: 4-3-1.)",
    "(, ground predict BB BB J IoU = boxes truth ground and predicted of union of Area boxes truth ground and predicted of ion in tersect of Area = IoUFor each detection task, precision and recall are evaluated using the IoU value with a given threshold (t).",
    "If IoU ≥ t, the prediction is considered correctly identified, whereas if IoU < t, the prediction is considered incorrectly identified.",
    "To compute the values of precision and recall, each bounding box is classified into the following categories [95]:True Positive (TP): The model is predicated and positive; in actuality, it is true.True Negative(TN): The model has predicted negative, and in actuality, it is true.False Positive(FP): The model has predicted positive, and in actuality, it is false.False Negative(FN): Model has predicted negative and in actual is false.Variables used for the calculation of metrics are: mn mn mn mn mn mn mn C C C C C C C FN TN FP TP TN TP Accuracy + + + + =Precision indicates how many positive identifications are actually correct.",
    "It is derived from the Intersection over Union (IoU), which is the ratio of the overlap area between the ground truth and the predicted bounding boxes to their union area."
  ],
  "correct_citations": [
    "In this case, if the IoU values of both bounding boxes are greater than a predefined threshold (usually 0.5), it means that the object has been correctly recognized [9,[93][94][95].IoU is calculated as follows: 4-3-1.)",
    "To compute the values of precision and recall, each bounding box is classified into the following categories [95]:True Positive (TP): The model is predicated and positive; in actuality, it is true.True Negative(TN): The model has predicted negative, and in actuality, it is true.False Positive(FP): The model has predicted positive, and in actuality, it is false.False Negative(FN): Model has predicted negative and in actual is false.Variables used for the calculation of metrics are: mn mn mn mn mn mn mn C C C C C C C FN TN FP TP TN TP Accuracy + + + + =Precision indicates how many positive identifications are actually correct."
  ]
}