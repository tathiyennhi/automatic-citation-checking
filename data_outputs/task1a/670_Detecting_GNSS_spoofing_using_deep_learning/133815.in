{
  "texts": [
    "When building a spoofer detector, two hypotheses are tested:1 The null hypothesis is (H 0) that the legitimate signal and noise are present, but there is no spoofing signal,(5)( τi, fd,i) = arg max τ,f d C i (τ, f d) 2 (6) αi = C i ( τi, fd,i) (7) φi =∠C i ( τi, fd,i), (8) C i (τ, f d) 2 H 1 ≷ H 0 β.",
    "(9) y[n] = M i=1 x i [n; θ i] + M s j=1 x j [n; θ s,j] + η[n] H 0: y[n] = M i=1 x i [n; θ i] + η[n].2 The alternative hypothesis is (H 1) that the legitimate signal, spoofed signal, and noise are present in the dataset; The effect of a spoofing signal on the CAF is well-known and shown in Fig. 1 for clarity.",
    "Figure 1a shows an arbitrary CAF under H 0 (Fig. 1b) shows the situation when a spoof- ing signal is present as well, causing the appearance of a secondary peak on the CAF.",
    "This work proposes to train a deep neural network (DNN), data-driven model to learn to classify between spoofed or clean signal receptions.",
    "Data-driven GNSS spoofing detectionThe purpose of this work is to design and use a neural network (NN) in order to recognize the spoofed signal from CAF images."
  ]
}