{
  "style": "",
  "texts": [
    "The expected cumulative reward for trajectory is denoted as: and the return for a trajectory is denoted as R = (G 0,..., G H).",
    "By performing gradient ascent on the expected return J and updating the policy network parameters, the net- work gradually adjusts its parameters to reach a global or local maximum of J, s.t.",
    "The gradient of J with respect to the policy parameters then is computed as:",
    "The policy network parameters at time step t + 1 are obtained by: While this method exhibits a good convergence property, it still suffers from slow learning due to the high gradient variance of R.",
    "This issue can be addressed by introducing a base- line b to the Eq. 10, s.t."
  ],
  "correct_citations": []
}