{
  "texts": [
    "Every agent ∀A n ∈ A is characterized by a tuple ⟨ n, n ⟩ where n and n are agent A n 's start location and goal location, respectively.",
    "The vertices ∀V i ∈ V can be occupied by any agents ∀A n ∈ A or any obstacles ∀O m ∈ O dyn and ∀O k ∈ O static.",
    "The agents and dynamic obstacles can move from",
    "Time is assumed to be discretized into time steps so each agent can decide on a single action where to move through an adjacent edge for every time step.",
    "When there exists a joint sequence of actions = (Π 1, …, Π N) where"
  ]
}