{
  "texts": [
    "Given these seed terms, our intent is to pass the entire document, query GPT about causes, parse the effects to ask about further causes, and so on.",
    "However, the GPT API has a token limit foot_0, so a large file cannot fit within one prompt.",
    "Consequently, we send several prompts, each one aggregating as many complete sentences as can fit (with a safety margin) within a prompt.",
    "Each prompt uses the following template, where 'factor' could be replaced by 'obesity': [Question prompt.]",
    "With the text below answer the questions."
  ]
}