{
  "texts": [
    "L u→o ct is calculated in a symmetric way as L o→u ct. Then we obtain the contrastive loss L ct byL ct = 1 2 (L o→u ct + L u→o ct).",
    "D.4 Action PredictionThe action ãt is predicted based on the enhanced obser- vation feature f ′ Ot and the instruction feature f I through the cross-modal Transformer encoder E c (•) and the action prediction module E a (•) (see Eq. 2 and Eq.",
    "The total training objective L of CONSOLE is:L = L nav + λ 1 L cs + λ 2 L ct,(18)where λ 1 and λ 2 are the balance meta-parameters.",
    "E EXPERIMENTS E.1 Experimental SetupDatasets.",
    "We evaluate CONSOLE on four mainstream VLN benchmarks: R2R [1], REVERIE [2], R4R [3], and RxR [4]."
  ]
}