{
  "style": "",
  "texts": [
    "With the current important landmark U la t and its cooccurrences {U co t,i } N co i=1, we conduct the landmark discovery through CLIP to highlight the single-view observation where U la t with {U co t,i } N co i=1 is most likely to appear for assisting action decision.",
    "To this end, for each single-view observation O t,n, we calculate the probability p U la t,n that the current important landmark U la t appears in it by:p U la t,n = exp(sim(f U la t, f Bt,n)/τ) No m=1 exp(sim(f U la t, f Bt,m)/τ),(10)wheref U la t = f U la z or f U la z+1, f Bt,nis the visual feature for O t,n (Eq. 7), exp(•) represents the exponent operation, and sim(•, •) denotes the similarity computation, which is the dot product between two features.",
    "We also calculate the probability p U co t,i,n that U co t,i appears in O t,n for each cooccurrence U co t,i like Eq. 10.",
    "D.2.3 Learnable Cooccurrence ScoringSince the cooccurrence priors are extracted without visual constraints, the cooccurrences may not appear in the observations or may appear in multiple single-view observations to cause misleading.",
    "Therefore, we introduce a learnable cooccurrence scoring module to correct the priors according to actual observations."
  ],
  "correct_citations": []
}