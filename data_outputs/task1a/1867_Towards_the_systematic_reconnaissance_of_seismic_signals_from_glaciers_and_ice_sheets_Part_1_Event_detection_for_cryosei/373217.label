{
  "style": "",
  "texts": [
    "A on and A off are thresholds for which an event is triggered and detriggered, respectively, and n is the number of sta-lta pairs, defined as the smallest integer satisfying the following equation:",
    "n > ln(max(sta, 1/ sta, lta, 1/ lta))/ ln.",
    "As an example to demonstrate how the algorithm generates a set of sta-lta pairs (Step 1), using parameter values of sta = 1 s, lta = 10 s, sta = 10, lta = 10, and = 2 as inputs to the multi-STA/LTA algorithm, the following pairs are generated:",
    "{(sta 0 /lta 0),.",
    "., (sta i /lta i),."
  ],
  "correct_citations": []
}