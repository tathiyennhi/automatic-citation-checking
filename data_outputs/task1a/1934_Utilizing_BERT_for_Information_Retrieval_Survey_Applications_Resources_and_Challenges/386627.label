{
  "style": "IEEE",
  "texts": [
    "MemRecall imitates the working memory in terms of competitive retrieval, rehearsal, and decay to facilitate multi-step reasoning.",
    "Another BERT model, called judge, is introduced to CogLTX to score the relevance of the blocks and is trained jointly with the original BERT reasoner.",
    "Although the use of CogLTX for IR applications has not been explored, it works as a basic building block for KeyBLD [74], which is used in IR applications.",
    "In contrast to CogLTX, KeyBLD [74] first selects the key blocks of a long document by local query-block preranking, and then aggregates a few blocks to form a short document that can be processed by BERT for ad-hoc IR.",
    "Figure 3 illustrates the basic architecture of KeyBLD."
  ],
  "correct_citations": [
    "Although the use of CogLTX for IR applications has not been explored, it works as a basic building block for KeyBLD [74], which is used in IR applications.",
    "In contrast to CogLTX, KeyBLD [74] first selects the key blocks of a long document by local query-block preranking, and then aggregates a few blocks to form a short document that can be processed by BERT for ad-hoc IR."
  ]
}