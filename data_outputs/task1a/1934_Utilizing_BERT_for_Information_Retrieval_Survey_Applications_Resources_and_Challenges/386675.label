{
  "style": "IEEE",
  "texts": [
    "In the same context, Yan et al. [85] proposed a document expansion-based retriever that first generates a query set for each passage, based on the pointer-generator model [11], and then performs indexing.",
    "After the retrieval stage, the candidate documents are fed into BERT for reranking.",
    "Inspired by 𝑆𝑡𝑟𝑢𝑐𝑡𝐵𝐸𝑅𝑇 [131], which incorporates the structural objectives of the word and the sentence to reconstruct their correct orders, the authors of this paper addressed the challenging task of predicting the sentences preceding and following a given sentence, instead of the original 𝑁 𝑆𝑃 task.",
    "The results of comprehensive experimental evaluations in [85] verified its superiority over the relevant state-of-the-art approaches in terms of both passage ranking and document ranking.",
    "RESOURCES FOR BERT-BASED RANKING MODELSIn this section, we first present different datasets used in ad-hoc IR applications and then detail the resources available for BERT for IR, including MatchZoo [45], Anserini [94], Pyserini [49], Birch [144], PyTerrier [22], Capreolus [14], OpenNIR [110], and OpenMatch [152], in Section 4.2."
  ],
  "correct_citations": [
    "In the same context, Yan et al. [85] proposed a document expansion-based retriever that first generates a query set for each passage, based on the pointer-generator model [11], and then performs indexing.",
    "Inspired by 𝑆𝑡𝑟𝑢𝑐𝑡𝐵𝐸𝑅𝑇 [131], which incorporates the structural objectives of the word and the sentence to reconstruct their correct orders, the authors of this paper addressed the challenging task of predicting the sentences preceding and following a given sentence, instead of the original 𝑁 𝑆𝑃 task.",
    "The results of comprehensive experimental evaluations in [85] verified its superiority over the relevant state-of-the-art approaches in terms of both passage ranking and document ranking.",
    "RESOURCES FOR BERT-BASED RANKING MODELSIn this section, we first present different datasets used in ad-hoc IR applications and then detail the resources available for BERT for IR, including MatchZoo [45], Anserini [94], Pyserini [49], Birch [144], PyTerrier [22], Capreolus [14], OpenNIR [110], and OpenMatch [152], in Section 4.2."
  ]
}