{
  "style": "IEEE",
  "texts": [
    "Distillation Models.To address the limitations related to memory and the long time needed by state-of-the-art BERT models to make inferences, researchers have developed distillation models of BERT.",
    "Jiao et al.",
    "proposed TinyBERT [133] that uses transformer distillation, general distillation, and task-specific distillation.",
    "Transformer distillation relies on learning attention weights to enable the teacher (BERT) to transfer its linguistic knowledge to the student (TinyBERT), while the other two steps of distillation subsequently utilize the learned knowledge to perform task-specific distillation on augmented datasets.",
    "In contrast to TinyBERT, which uses distillation for building task-specific models, Sanh et al.proposed DistilBERT [125], which uses knowledge distillation in pretraining to develop a lightweight, general-purpose, and pretrained model."
  ],
  "correct_citations": [
    "proposed TinyBERT [133] that uses transformer distillation, general distillation, and task-specific distillation.",
    "In contrast to TinyBERT, which uses distillation for building task-specific models, Sanh et al.proposed DistilBERT [125], which uses knowledge distillation in pretraining to develop a lightweight, general-purpose, and pretrained model."
  ]
}