{
  "style": "IEEE",
  "texts": [
    "In light of these aspects, we discuss the state-of-the-art dual-encoder-based models, distillation models, and effective training strategies in Sections 3.4.1, 3.4.2, and 3.4.3, respectively.",
    "Finally, we provide an extended analysis of the relevant comparisons and experiments in Section 3.4.4.",
    "Dual-encoder-based Models.Researchers have recently attempted to retrieve documents in a low-dimensional embedding space [58,69] through a dense retrieval model (DRM), the architecture of which is based on the representationfocused model.",
    "It maps queries and documents into low-dimensional embeddings via dual-encoders (i.e., the query encoder and the document encoder).To achieve a better trade-off between effectiveness and efficiency, researchers have attempted to encode independent queries and documents to reduce the computational complexity of the stage involving their interaction.",
    "Qiao et al.proposed BERT(Rep) [89], which independently encodes the query and the document via BERT, and uses the [CLS] embeddings of the last layer as the representations of the query and the document."
  ],
  "correct_citations": [
    "Dual-encoder-based Models.Researchers have recently attempted to retrieve documents in a low-dimensional embedding space [58,69] through a dense retrieval model (DRM), the architecture of which is based on the representationfocused model.",
    "Qiao et al.proposed BERT(Rep) [89], which independently encodes the query and the document via BERT, and uses the [CLS] embeddings of the last layer as the representations of the query and the document."
  ]
}