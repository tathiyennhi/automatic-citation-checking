{
  "style": "IEEE",
  "texts": [
    "First, the model produces relevance scores for each query term based on similarity matrices.",
    "Then, for models using a dense combination, CEDR concatenates the [CLS] vector with the signals of the model.",
    "For models that sum query term scores, such as the DRMM [46], CEDR shows that using the [CLS] vector in the dense calculation is effective.",
    "To better understand the contributions of contextualized word embeddings to the model, the experimental evaluation of CEDR involves visual comparison of GloVe word embeddings, ELMo representations (layer 2), and fine-tuned BERT representations (layer 5) as shown in Figure 5.",
    "For the query \"curbing population growth\" in this figure, the term \"curb\" that appears in both relevant and non-relevant documents has two distinct senses."
  ],
  "correct_citations": [
    "For models that sum query term scores, such as the DRMM [46], CEDR shows that using the [CLS] vector in the dense calculation is effective."
  ]
}