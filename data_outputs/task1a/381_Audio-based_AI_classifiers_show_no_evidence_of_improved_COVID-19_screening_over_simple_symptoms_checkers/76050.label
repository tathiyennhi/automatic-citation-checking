{
  "style": "APA",
  "texts": [
    "The base ResNet-50 model showed initial strong promise for ABCS 5, further motivating its inclusion in this comparison.",
    "We achieve estimates of uncertainty through Monte-Carlo Dropout to achieve approximate Bayesian inference over the posterior, Article https://doi.org/10.1038/s42256-023-00773-8 as in ref. 39.",
    "We opt to use the pre-trained model for a warm start to the weight approximations, and allow full retraining of layers.",
    "The features used to create an intermediate representation, as input to the convolutional layers, are Mel filterbank features with default configuration from the VGGish GitHub (ref.",
    "40): X i ∈ ℝ 96×64, 64 log-mel spectrogram coefficients using 96 feature frames of 10 ms duration, taken from a resampled signal at 16 kHz."
  ],
  "correct_citations": [
    "The base ResNet-50 model showed initial strong promise for ABCS 5, further motivating its inclusion in this comparison."
  ]
}