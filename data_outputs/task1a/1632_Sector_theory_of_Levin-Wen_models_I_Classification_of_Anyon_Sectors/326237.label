{
  "style": "",
  "texts": [
    "Whenever a string diagram on a surface Σ C 1 is interpreted as an element of H C 1, this will implicitly be done usingπ C 1.",
    "If x is a string diagram on Σ C 1, then we write ϕ x:= π -1 C 1 ([x]) for the corresponding vector in the string-net subspace H C 1.",
    "Let us also introduce the map σ C1: H C 1 → A(Σ C 1) defined on product vectors by σ C 1 (ϕ f):= d -1/4 ∂l [x f] Σ C 1,(22)and extended linearly to all of H C 1.",
    "Here,d ∂l = e∈ ⃗ ∂C d l(e)is the product of the quantum dimensions of the objects assigned by l to the boundary of C. Note that by construction we have σ C (H b C 1) ⊂ A(Σ C 1; b) for any boundary condition b.",
    "The reason for modifying the map π C 1 with weights as in Eq."
  ],
  "correct_citations": []
}