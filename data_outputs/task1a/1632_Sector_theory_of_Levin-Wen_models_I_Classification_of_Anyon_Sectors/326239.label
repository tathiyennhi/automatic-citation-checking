{
  "style": "",
  "texts": [
    "(25) Here we represented S × I as a subset of the plane with the bottom boundary component identified with the outer boundary in the figure.",
    "More generally, if N ⊂ S ∈ Bd(Σ C) then we define a *-representation t N of A(N) on H C N by letting t N (a) annihilate the orthogonal complement of the string-net subspace H C N, and letting t N (a) act on this subspace byt N (a)ϕ:= σ -1 C N a ▷ N σ C N (ϕ)(26)for any ϕ ∈ H C N. We also define the A( N) op -action t op N (a):= t N (a op).",
    "It follows immediately from the definitions thatLemma 4.3.",
    "If N ⊂ S ∈ Bd(Σ C) for some finite region C then t N (a)t S (id) = t S (id)t N (a) = t S (ι(a)),where ι: A(N) → A(S) is the inclusion (11).",
    "Definition of B f projectorsFor a boundary component S f that corresponds to a face f ∈ F we write t f:= t S f for the corresponding Tube S f -representation on H C f. We define the orthogonal projectorB f = t f (P 1), where P 1 = ∈ Tube S f.(27)That is, B f enforces string-net constraints and inserts a dotted loop around the face f. Using Convention 4.2, we can also describe the action of B f on H C f graphically asB f --→."
  ],
  "correct_citations": []
}