{
  "style": "APA",
  "texts": [
    "SHAP produces an account of the impact features have on an individual output by considering the impact of their inclusion (applying techniques found in game theory).",
    "Figure 9 below is an example of SHAP output from a policyholder in Group A, as defined in Modelled example 3.",
    "In addition, SHAP values can be used to produce a localised feature importance plot, such as in Figure 10.",
    "As an alternative to SHAP, LIME fits a simpler, glass-box model as a surrogate that is easier to interpret (see Ribeiro et al., 2016) We use a ridge regression LIME with an intercept to produce the following local explanations for a policyholder with a low-predicted-risk premium.",
    "The output is based on 10,000 data points sampled."
  ],
  "correct_citations": [
    "As an alternative to SHAP, LIME fits a simpler, glass-box model as a surrogate that is easier to interpret (see Ribeiro et al., 2016) We use a ridge regression LIME with an intercept to produce the following local explanations for a policyholder with a low-predicted-risk premium."
  ]
}