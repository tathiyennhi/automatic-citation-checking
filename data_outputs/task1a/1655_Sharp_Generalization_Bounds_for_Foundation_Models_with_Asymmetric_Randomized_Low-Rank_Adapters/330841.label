{
  "style": "",
  "texts": [
    "Then we can represent each f byf = f -f k + k j=1 (f j -f j-1),where f 0 = h. Then we haveRS (F) = E σ   sup f ∈F 1 m m i=1 σ i   f (x i) -f k (x i) + k j=1 (f j (x i) -f j-1 (x i))    .Applying the triangle inequality, we obtainRS (F) ≤ E σ sup f ∈F 1 m m i=1 σ i (f (x i) -f k (x i)) + k j=1 E σ sup f ∈F 1 m m i=1 σ i (f j (x i) -f j-1 (x i)).We consider the first and second terms in the last expression separately.",
    "For the first term, recall that the σ i 's are random variables taken from {-1, +1}.",
    "Applying the Cauchy-Schwarz inequality, we obtainE σ sup f ∈F 1 m m i=1 σ i (f (x i) -f k (x i)) ≤ E σ   sup f ∈F 1 m m i=1 σ 2 i m i=1 (f (x i) -f k (x i)) 2  .Since σ i are independent random variables with variance 1, we can simplify this to≤ E σ   sup f ∈F 1 m m i=1 (f (x i) -f k (x i)) 2   = E σ sup f ∈F L 2 S (f, f k) ≤ ϵ k.Now, we consider the second termk j=1 E σ sup f ∈F 1 m m i=1 σ i (f j (x i) -f j-1 (x i)).",
    "We fix j, and define g f:= f j -f j-1.",
    "That is, we define a new function g f for each f ∈ F. Let G def.= {g f: f ∈ F} be the collection of g functions."
  ],
  "correct_citations": []
}