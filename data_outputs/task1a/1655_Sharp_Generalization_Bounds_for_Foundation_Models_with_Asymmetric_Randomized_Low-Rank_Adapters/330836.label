{
  "style": "",
  "texts": [
    "Suppose also that σ = ReLU, (for simplicity) b (1) = 0 for each t = 1,..., T + 1 (where we write 0 for the appropriate-dimensional zero vector), and let ℓ(ŷ, y)def.= |ŷ -y|.Step 1 -The MLP Construction: For each t = 1,..., T + 1 suppose that d t+1 ≥ d t let Īdt+1,dt = I dt if d t+1 = d t. For each t = 1,..., T + 1 define A (t)A (t) def.",
    "= B (t) † ( Īdt+1,dt -W (t))(25)where B (t) † denotes the Moore-Penrose pseudo-inverse of B (t) (which we recall, is a right-inverse of B (t) if B (t) has full row-rank).By construction, we have that: for each (x, y) ∈ supp(P) ReLU •((W (1) + B (1) A (1))x (1) + b (0)) ≥ 0 (where ≥ is applied componentwise, thus we are considering the product order on R d2); whence, the following holdsReLU •((W (1) + B (1) A (1))x (1) + b (0)) = ((W (1) + B (1) A (1))x (1) + b (0)) (26) = (W (1) + B (1) A (1))x (1) (27) = W (1) + B (1) B (1) † ( Īdt+1,dt -W (1)) x (1) = x (1).",
    "(28)where the right-hand inequality held by definition of A (t) in (25) (assuming that B (t) has full rank).Iterating the above argument and appealing to the recursive representation of f in (6), we deduce that f (x) = x holds for each (x, y) ∈ supp(P).",
    "By our specifications on ℓ and P we have that: for each (x, y) ∈ supp(P)ℓ( f (x), y) = | f (x) -0| = |x -0| = x.Since (X 1, Y 1),..., (X N, Y N) ∼ P are i.i.d.",
    "random vectors satisfying with law P then, we have thatR( f) = E X∼P X [X] and R N ( f) = 1 N N n=1 X n.(29)Consequentially, we find thatsup f ∈F R(f) -R N (f) ≥ R( f) -R N ( f) = E X∼P X [X] - 1 N N n=1 X n.(30)Step 2 -Weight Admissibility:We now need to verify that the choice of weights in (25) is indeed valid, meaning that the operator norm of the trainable LoRA factors A (1),..., A (T +1) are indeed bounded above by M (with some acceptable probability 1 -δ/2).Assuming that M and R are large enough so that A (t) is an admissible choice with positive probability."
  ],
  "correct_citations": []
}