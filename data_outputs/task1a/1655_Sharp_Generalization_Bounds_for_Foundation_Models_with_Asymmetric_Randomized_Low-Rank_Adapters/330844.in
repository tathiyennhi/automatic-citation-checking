{
  "texts": [
    "Let Rn (F) be the empirical Rademacher complexity of function class F. Then Rn (F) ≤ inf ϵ≥0 x i) 2 ≤ 3ϵ j √ m. Applying Massart's Lemma to the functions G, we obtain E.g.",
    "(-1, 1) ⊤ ReLU((1, -1)x) = (1, -1) ⊤ ReLU((-1, 1)x).",
    "https://vectorinstitute.ai/partnerships/current-partners/ E.g.",
    "(-1, 1) ⊤ ReLU((1, -1)x) = (1, -1) ⊤ ReLU((-1, 1)x).",
    "If we set L 2 constraints on A (t) instead of L ∞ norm, the constraint size M can be put inside the log to further improve the bound."
  ]
}