{
  "style": "",
  "texts": [
    "In Setting 1.Then with probability at least 1 -ϵ (over the random initialization on (B (t)) t), the Lipschitz constant L θpre LoRA of the (random) LoRA map in (6) is at-mostL θpre LoRA ≤ 2 c2T M ν 2r log(2W/ϵ) R + ∥θ pre ∥ ∞ R0 c2T.where ν > 0 is the random initialization scale ofB (t) ij ∼ N (0, ν 2).Step 2 -Probabilistic Bounds on Covering Number of Randomized LoRA Class Having obtained a probabilistic estimate of the Lipschitz constant for our random LoRA parameterization map, we derive a probabilistic bound on the covering number of the associated randomized function class; conditioned on the event depending on the draw of the randomized LoRA parameters:B ϵ def.",
    "= ω ∈ Ω: L θpre LoRA ≤ 2 c2T M ν 2r log(2W/ϵ) + ∥θ pre ∥ ∞ c2T (11)The normed space under consideration is typically the parameter space R p, where the finite collection of model parameters is flattened and concatenated into a single vector.",
    "Unless otherwise stated, the norm used is the ∞-norm.",
    "We begin with a minor extension-requiring only a brief comment-of a classical covering estimate for balls in normed spaces; see, for instance, [Lorentz et al., 1996, Proposition 15.1.3].",
    "The integer p denotes the total number of parameters defining the LoRA mapping.Lemma 2 (Covering Number Bounds: Random LoRA Class)."
  ],
  "correct_citations": []
}