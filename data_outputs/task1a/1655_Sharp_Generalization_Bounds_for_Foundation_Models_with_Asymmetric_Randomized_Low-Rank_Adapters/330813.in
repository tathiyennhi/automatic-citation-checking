{
  "texts": [
    "= σ • (W (t) x (t) + b (t)) for t = 1,..., T x (1) def.",
    "= x(4)where θ = ((W (t), b (t))) (T +1) t=1 ∈ R p where p = (T -1) W (W + 1) + (d + 1)W + (D + 1)W, W (t) ∈ R dt+1×dt and b (t) ∈ R dt where d t = W if t ∈ {2,..., T }, d 1 = d, and d T +1 = D. We fix a parameter θ pre def.",
    "= (W (t), b (t)) (T +1) t=1 ∈ R p.We defined the perturbed representation mapP θpre T,W: R p → C([0, 1] d, R D) θ → f (•|θ + θ pre).",
    "(5)LoRAs with Random Factors Fix a probability space (Ω, A, P); all random quantities will be defined thereon.",
    "Fix random matrices A 1,..., A T +1 of dimensions W × r for l ≤ T and D × r for l = T + 1."
  ]
}