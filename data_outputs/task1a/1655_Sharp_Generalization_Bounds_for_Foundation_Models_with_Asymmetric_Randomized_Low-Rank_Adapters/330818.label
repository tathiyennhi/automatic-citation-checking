{
  "style": "",
  "texts": [
    "= |ŷ -y| and suppose that each MLP in the class F represented by (4) satisfies the following minimum width requirement on its hidden layersη ⋆ def.",
    "= min t=1,...,T d t+1 - √ r > 0.",
    "(8)Given any data-generating probability distribution P satisfying Assumption 1, if the entries of the random LoRA factors {B (t) } T t=0 are i.i.d.",
    "standard normal then: there is an absolute constant c > 0 such that for every 2(T + 1)e -cη 2 < δ < 2(T + 1) there is an M > 0 (required for the LoRA parameterization (6)) large enough such thatP sup f ∈F R(f) -R N (f) > 1 N ≥ Prob.",
    "Sampling Good LoRA Weights 1 - δ 2 Sample Complexity (in N) 1 -Θ 1 √ N. Moreover, \"large enough\" value of M means that M ∈ Θ 1/ √ d 1 -d T +1 -ln(2T /δ).Remark 3 (The Asymmetry in the Lower Bound -Randomizing B vs. A)."
  ],
  "correct_citations": []
}