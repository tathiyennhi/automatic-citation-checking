{
  "texts": [
    "We directly see that: for each t = 1,..., T + 1∥A (t) ∥ op ≤ ∥B (t) † ∥ op ∥( Īdt+1,dt -W (t))∥ op (31) ≤ ∥B (t) † ∥ op 1 + ∥W (t) ∥ op = 1 s min (B (t)) 1 + ∥W (t) ∥ op(32)where, for each t = 1,..., T + 1, s min (B(t)) denotes the smallest singular value of B (t) respectively.",
    "Since, for each t = 1,..., T + 1, each B (t) has i.i.d.",
    "standard normal entries.",
    "Consider the constant C pre ≥ 0, depending only on the pre-trained weight matrices {W (t) } T t=0 by Cpre def.",
    "= max t=0,...,T ∥W (t) ∥ op."
  ]
}