{
  "style": "",
  "texts": [
    "Then with probability at least 1 -ϵ (over the random initialization on (B (t)) t), the Lipschitz constant L θpre LoRA of the (random) LoRA map in (6) is at-mostL θpre LoRA ≤ 2 c2T M ν 2r log(2W/ϵ) R + ∥θ pre ∥ ∞ R0 c2T.where ν > 0 is the random initialization scale ofB (t) ij ∼ N (0, ν 2).",
    "Proof.Step 1 Local Lipschitz Regularity of Parameterization Map Fix R > 0 (to be settled retroactively).",
    "If σ is either: a bounded Lipschitz function or the ReLU function then [Petrova and Wojtaszczyk, 2023a, Theorem 3], there is a constant 0 ≤ c1 def.",
    "= c 1 (d, T, W) ≤ c 2 def.= c 2 (d, T, W) > 0, depending only on d, T, and on W, such that the restriction of the mapP θpre T,W (θ) to the ∞-norm ball B ∞ (R) def.",
    "= {θ ∈ R p: ∥θ∥ ∞ ≤ R} is L θpre LoRA -Lipschitz with c 1 T (1 + log 2 (R + ∥θ pre ∥ ∞)) ≤ log 2 (L θpre LoRA) ≤ c 2 T (1 + log 2 (R + ∥θ pre ∥ ∞))(14)where we have used the fact that the network weights are at-most R (matrix parameterized by LoRA) plus the initial weights θ pre.Step 2 -Diameter of LoRA Parameter Space For all t = 1,..., T + 1, i = 1,..., W, note that the columnB (t) i• ∼ N (0, ν 2 I r) is a Gaussian random vector, hence we have the concentration: ∀δ > 0,P r k=1 B (t) ik > δ ≤ 2 exp - δ 2 2ν 2 rIn other words, with probability at least 1 -ϵ, it holds thatr k=1 B (t) ik ≤ ν 2r log(2/ϵ)."
  ],
  "correct_citations": []
}