{
  "style": "IEEE",
  "texts": [
    "We fix maximum admissible LoRA weight sizes M ≥ 0.",
    "We then define the (random) parameter-to-LoRA map, which maps low-rank matrices C 1,..., C T +1 and an event ω ∈ Ω (random initialization of non-trainable LoRA parameters A t) to the sequences of matrices:LoRA: Ω × [-M, M] q → C([0, 1] d, R D) ω, (A (t)) t → P θpre T,W (B (t) (ω)A (t)) (T +1) t=1 = f (•|(B (t) (ω)A (t)) (T +1) t=1 + ϑ pre)(6)where the effective number of trainable LoRA parameters is defined to beq def.",
    "= r(W (T -1) + d + D) ∈ O(r).",
    "(7)Notation We now close our preliminaries section with the following table aggregating the notation used in formulating our main result within the main body of our paper.",
    "SymbolMeaningd input dimension D output dimension T network depth W pre-trained network width r LoRA width p = W (T W -W + T + d + D + 1) number of pre-trained parameters θ pre = (W (t), b (t)) t q = r(T W -W + d + D) number of trainable LoRA parameters (A (t)) t M maximum admissible values for trainable LoRA parameters (A (t)) t ℓ L ℓ ≥ 0 Lipschitz classification-loss from R D × R D to [0, 1]Table 1: Notation used in the main body of our manuscript."
  ],
  "correct_citations": [
    "We then define the (random) parameter-to-LoRA map, which maps low-rank matrices C 1,..., C T +1 and an event ω ∈ Ω (random initialization of non-trainable LoRA parameters A t) to the sequences of matrices:LoRA: Ω × [-M, M] q → C([0, 1] d, R D) ω, (A (t)) t → P θpre T,W (B (t) (ω)A (t)) (T +1) t=1 = f (•|(B (t) (ω)A (t)) (T +1) t=1 + ϑ pre)(6)where the effective number of trainable LoRA parameters is defined to beq def.",
    "SymbolMeaningd input dimension D output dimension T network depth W pre-trained network width r LoRA width p = W (T W -W + T + d + D + 1) number of pre-trained parameters θ pre = (W (t), b (t)) t q = r(T W -W + d + D) number of trainable LoRA parameters (A (t)) t M maximum admissible values for trainable LoRA parameters (A (t)) t ℓ L ℓ ≥ 0 Lipschitz classification-loss from R D × R D to [0, 1]Table 1: Notation used in the main body of our manuscript."
  ]
}