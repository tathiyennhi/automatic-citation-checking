{
  "style": "APA",
  "texts": [
    "During task decomposition, the Task Decomposer adopts a zero-shot chain-of-thought (CoT) approach (Wei et al., 2023).",
    "This method is integrated into the prompt, as Figure 9 illustrates, to guide the LLM in generating responses in JSON format, specify the index of the immediate predecessor for each subtask as needed and specify JSON path expressions for each subtask, referencing the provided data D. Subsequently, each subtask node will use these JSON path expressions to query the data related to its subtask.",
    "The Agent Controller focuses on analyzing the task graph and assigning the appropriate subtask to the right agent in an efficient manner.",
    "Ready-to-Execute Tasks Identification.",
    "The Agent Controller identifies ready-to-execute task set N ready."
  ],
  "correct_citations": [
    "During task decomposition, the Task Decomposer adopts a zero-shot chain-of-thought (CoT) approach (Wei et al., 2023)."
  ]
}