{
  "texts": [
    "Proposition A.4.",
    "The estimator rϕ (a, q) = 1 ℓ=expert -1 ℓ=policy with ℓ ∼ c ϕ (• | a, q) is an unbiased estimator of the reward r ϕ (a, q) = c ϕ (ℓ = expert | a, q) -c ϕ (ℓ = policy | a, q).",
    "We compute the expectation of rϕ (a, q) over the sampling distribution c ϕ (• | a, q):",
    "Since c ϕ (ℓ = policy | a, q) = 1 -c ϕ (ℓ = expert | a, q), we have:",
    "Stability in Long-form Generation."
  ]
}