{
  "texts": [
    "A data-driven loss L D, such as the mean squared error, for example, can be used as cost function C.C = L D = 1 2N M N M i=1 || ŷi -y M i || 2 2(5) Space-time approachesTo declutter the notation, but without loss of generality, the temporal dimension t is dropped in this section, as it is possible to treat it like any other spatial dimension x in the scope of these methods.",
    "The goal of the upcoming methods is to either learn a forward operator û = F[λ; x], an inverse operator for the coefficients λ = I [u; x], or an inverse operator for the non-linear operator N = O[u; λ; x].",
    "10 The methods will be explained using the forward operator, but they apply analogously to the inverse operators.",
    "Only the inputs and outputs differ.The solution prediction ûi at coordinate x i or ûi on the entire domain is made based on a set of inverse coefficients λ i.",
    "The cost function C is formulated analogously to Eq."
  ]
}