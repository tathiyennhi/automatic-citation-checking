{
  "style": "IEEE",
  "texts": [
    "Both yield an interpretable operator from which the analytical expression can be extracted.In order to construct a loss function, Eqs.",
    "( 3) and ( 49) are discretized using the forward Euler method:u(x, t n+1) = u(x, t n) + t N T x, u, ∂u ∂ x, ∂ 2 u ∂ x 2,.... (50)This temporal discretization is applied iteratively, and the discrepancy between the derived function and the measured data u M (x, t n) serves as the loss function.",
    "DySparse identification of non-linear dynamic systems (SINDy) [335] deals with the discovery of dynamic systems of the form of Eq.",
    "The task is posed as a sparse regression problem.",
    "Snapshot matrices of the state X = [x(t 1), x(t 2),..., x(t n)] and its time derivative Ẋ = [ẋ(t 1), ẋ(t 2),..., ẋ(t n)] are related to one another via candidate functions (X) evaluated at X using unknown coefficients:Ẋ = (X)."
  ],
  "correct_citations": [
    "DySparse identification of non-linear dynamic systems (SINDy) [335] deals with the discovery of dynamic systems of the form of Eq."
  ]
}