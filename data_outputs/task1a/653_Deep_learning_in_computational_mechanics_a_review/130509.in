{
  "texts": [
    "A loss can be formulated in terms of body load mismatch, as 1 2 N i=1 ( bib i) 2.",
    "In the inverse setting, α e becomes learnable-instead of u j, which is then fixed.",
    "For partial domain knowledge in the inverse case, u j becomes partially learnable.A different approach are the hierarchical deep-learning NNs (HiDeNNs) [506] with extensions in [507][508][509][510][511][512].",
    "Here, shape functions are treated as NNs constructed from basic building blocks.",
    "Consider, for example, the one-dimensional linear shape functionsN 1 (x) = x -x e 2 x e 1 -x e 2 (68)N 2 (x) = x -x e 1 x e 2 -x e 1, (69)which can be represented as a NN, as shown in Fig. 10, where the weights depend on the nodal positions x e 1, x e 2."
  ]
}