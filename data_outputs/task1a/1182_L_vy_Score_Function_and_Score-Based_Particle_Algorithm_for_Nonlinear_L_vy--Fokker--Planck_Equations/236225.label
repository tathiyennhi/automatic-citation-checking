{
  "style": "IEEE",
  "texts": [
    "By Theorem 3.2, we can minimize the following function to control the KL divergence between the numerical p θ t and the truep t: min θ T t0 Ω f θ (x, t) -V[p θ t](x, t) 2 p θ t (x) dx dt, where V is defined in (2.6) or (3.5).",
    "By (2.5) and (2.6), the velocity field f θ is given by(3.10) f θ (x, t) = b(x, t) - |r|<1 F (r, t)ν( dr) - 1 2 ∇ • Σ(x, t) -s θ (x, t),where s θ is the numerical score function parametrized by θ,(3.11) s θ ≈ S[ pt] -K * pt,where S[ p t] is given in (2.7).",
    "Now, the optimization problem turns to(3.12) min θ∈Θ T t 0 Ω f θ (x, t) -V[p θ t](x, t) 2 p θ t (x) dx dt = min θ∈Θ T t 0 Ω s θ (x, t) + K * p θ t (x) - 1 2 Σ(x, t)∇ log p θ t (x) + I F,G t [p θ t](x) 2 p θ t (x) dx dt.This optimization over a score function s θ: R d × [t 0, T] → R d can, in principle, be solved using the gradient-based method such as the adjoint method described in [33] which may incur substantial computational overhead.",
    "[4] proposed a sequential time-stepping procedure that improves computational efficiency.",
    "In the following, we elaborate this sequential approach in details."
  ],
  "correct_citations": [
    "Now, the optimization problem turns to(3.12) min θ∈Θ T t 0 Ω f θ (x, t) -V[p θ t](x, t) 2 p θ t (x) dx dt = min θ∈Θ T t 0 Ω s θ (x, t) + K * p θ t (x) - 1 2 Σ(x, t)∇ log p θ t (x) + I F,G t [p θ t](x) 2 p θ t (x) dx dt.This optimization over a score function s θ: R d × [t 0, T] → R d can, in principle, be solved using the gradient-based method such as the adjoint method described in [33] which may incur substantial computational overhead.",
    "[4] proposed a sequential time-stepping procedure that improves computational efficiency."
  ]
}