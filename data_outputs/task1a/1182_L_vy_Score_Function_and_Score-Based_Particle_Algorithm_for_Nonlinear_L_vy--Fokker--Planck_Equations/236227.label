{
  "style": "",
  "texts": [
    "The discrete-time velocity fields (f θi), induce the transported probability density, denoted by pti, as the approximation to the true probability density function p t (x) in the interval (t i-1, t i).",
    "Specifically, set pt0 (x) as the initial distribution µ 0 (x) in (2.1), and pti, i = 1, 2,..., as the solution q ∆t of the following transport equation driven by f θi in a short time interval ∆t with the initial pti-1:(3.13) ∂qt(x) ∂t = -∇ • f θ i (x)qt(x), q 0 (x) = pt i-1 (x).We now consider the following first order approximation in time to (3.12):(3.14) T t 0 Ω f θ (x, t) -V[p θ t](x, t) 2 p θ t (x) dx dt ≈ n i=1 Ω f θ i (x) -V[pt i-1](x, ti-1) 2 pt i-1 (x) dx ∆t.1 One can construct the linear interpolation in time from the discrete time bys θ (x, t) = (t - t i-1)NN θ i (x) + s θ i-1 (x) on t ∈ [t i-1, t i].",
    "In addition, to learn s θ i (x) = ∆t × NN θ i (x) + s θ i-1 (x) with a trained s θ i-1 is equivalent to learn the neural network function NN θ i (x)This manuscript is for review purposes only.Note that pti-1 only depends on the past velocity fields up to f θi-1, which allow us to apply the following iterative scheme: for i = 1, 2,..., we sequentially solve(3.15) θ ⋆ i:= argmin θi Ω f θi (x) -V[p ti-1](x, t i-1) 2 pti-1 (x) dx.with the initial pt0 = µ 0, and for i ≥ 2, pti-1 is the image density of pti-2 transported by the velocity field f θ ⋆ i-1 within the short time interval [t i-2, t i-1].",
    "To solve (3.15) for a given i, note that pti-1 is known as samples from the transport maps obtained up to f θi-1, then we can derive the similar objective function analogous to Hyvärinen score-matching:Ω f θ i (x, ti) -V[pt i-1](x, ti) 2 pt i-1 dx = Ω s θ i (x) + K * pt i-1 (x) - 1 2 Σ(x, ti)∇ log pt i-1 + I F,G t i [pt i-1](x) 2 pt i-1 dx = Ω s θ i (x) 2 pt i-1 dx + Ω K * pt i-1 (x) - 1 2 Σ(x, ti)∇ log pt i-1 + I F,G t i [pt i-1](x) 2 × pt i-1 dx + 2 Ω s θ i (x) • K * pt i-1 (x) - 1 2 Σ(x, ti)∇ log pt i-1 + I F,G t i [pt i-1](x) pt i-1 dx.Note that the second term contains no s θi, so we can drop it during this optimization sub-problem at iteration i.",
    "For the remaining terms, we express them by the expectation w.r.t. pti-1."
  ],
  "correct_citations": []
}