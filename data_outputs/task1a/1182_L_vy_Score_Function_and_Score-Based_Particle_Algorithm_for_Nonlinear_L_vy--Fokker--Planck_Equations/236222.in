{
  "texts": [
    "For simplicity, we introduce the following notationsb(x, t):= b(x, t) - |r|<1 F (r, t)ν( dr) - 1 2 ∇ • Σ(x, t), (3.1) I F,G t [ pt](x):= I F t [ pt](x) + I G t [ pt](x), (3.2)This manuscript is for review purposes only.I F t [ pt](x):= |r|<1 1 0 F (r, t) pt(x -λF (r, t)) pt(x) dλν( dr), (3.3) I G t [ pt](x):= |r|≥1 1 0 G(r, t) pt(x -λG(r, t)) pt(x) dλν( dr).",
    "(3.4)And we recall the vector field (2.6), which can be written as(3.5) V[µ](x, t):= b(x, t) + (K * µ)(x) - 1 2 Σ(x, t)∇ log µ + It(µ, F, G).Now suppose we are given a family of time-varying hypothesis velocity fields {f θ, f θ: R d × [t 0, ∞) → R d } θ∈Θ for some index set Θ, and let p θ be the solution to the continuity equation(3.6) ∂p θ t (x) ∂t = -∇ • f θ (x, t)p θ t (x), p θ t 0 = µ0.Similar with previous arguments, the solution density p θ t can be viewed as the push forward of µ 0 under the flow map X θ s,t of the ordinary differential equation(3.7) dX θ s,t (x) dt = f θ (X θ s,t (x), t), X θ s,s (x) = x, t, s ≥ t0,and thus the density function can be written as(3.8) p θ t (x) = µ0(X θ t,0 (x)) exp - t t 0 ∇ • f θ (X θ t,s (x), s) ds,as discussed before.",
    "Let us make some assumptions on the the initial distribution µ 0, the Lévy-Fokker-Planck solution p, and the parametrized vector fields {f θ } θ∈Θ.",
    "In the remainder of this paper, we will focus on the case where Ω is a torus.",
    "The method for extending this to an unbounded domain is similar to the approach detailed in [33, Appendix G], which requires additional assumptions about the regularity of the initial distribution µ 0 and the true probability flow p t.Assumption 2.1."
  ]
}