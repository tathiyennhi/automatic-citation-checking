{
  "style": "APA",
  "texts": [
    "The model is trained using the AdamW optimizer, a batch size of 3, learning rate of 2e-5, max number of epochs of 3 with all inputs truncated to 512 tokens.",
    "An ensemble of 3 models is trained.",
    "Training for each model takes 3 hours on an NVIDIA V100 graphical processing unit.",
    "For the sentiment classification task, we used the models from RoBERTa and distilBERT (Sanh et al., 2019) family and the they are finetuned on various datasets as explained in the following.",
    "The train split of IMDb is used to finetune RoBERTa foot_6 and BERT foot_7."
  ],
  "correct_citations": [
    "For the sentiment classification task, we used the models from RoBERTa and distilBERT (Sanh et al., 2019) family and the they are finetuned on various datasets as explained in the following."
  ]
}