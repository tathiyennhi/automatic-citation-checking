{
  "texts": [
    "In sentiment classification, the sentiment classes (unlike the options in multiple-choice reading comprehension where the associated text with each option class changes across different examples) are fixed.",
    "Hence, we can drop the dependence on the sentiment options.",
    "Here we are only interested the influence to the output y from semantic content x (s) and its linguistic realization method: I(Y; X (s)), I(Y; X|X (s)), as there is only one element at the input.",
    "Following Equation 3, the semantic and linguistic breakdown is expressed as:",
    "In practice, the following approximations are made:"
  ]
}