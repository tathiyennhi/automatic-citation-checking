{
  "style": "",
  "texts": [
    "re input d value ion (17): (17) g; 0 Q W, rices.",
    "is then btained n (18):, d Value d linear prevent nection ttention ecoder's)-(20): (19) (20) (21) masked ices z Q of the quation weight e query ery and hanism.",
    "lti-Head Attention output.",
    "Finally, the feature vector ''' O is obtained by FFN and residual layer processing, and after adjusting the length of the vocabulary list by a linear layer, it is inputted into the softmax function to generate the word probability, whose formula is shown in (22): ' output '' O Soft max) O (Linear() =, (22) where output O is the word generated at the current timestamp, and ''' O is the vector output by the decoder.",
    "The model repeats the decoding step until generating the complete textual caption for the image. 4."
  ],
  "correct_citations": []
}