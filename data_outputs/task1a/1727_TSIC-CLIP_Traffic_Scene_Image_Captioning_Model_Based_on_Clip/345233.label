{
  "style": "",
  "texts": [
    "Text-EncoderThe text encoder utilizes the pre-trained CLIP text encoder, and the weights of the predictive classifier are adjusted using the adapter layer as shown in Equation ( 5): Text-EncoderThe text encoder utilizes the pre-trained CLIP text encoder, and the weights of the predictive classifier are adjusted using the adapter layer as shown in Equation ( 5):() context 1 2 FA (W) ELU W W W C C T =, (5)where W represents the classifier weights;context FA represents the adapter layer used to fine-tune the CLIP text encoder.The classifier weights are first fed into the adapter layer to map the features to the new space used to obtain the relevant features.",
    "Then, they are fed into the residual block for adjustment, as shown in Equation ( 6):context W FA (W) (1)W T   = + - å, (6)where  is the residual ratio; C å represents the weights adjusted by the adapter layer.Then the features encoded by Image-Encoder * global V and the classifier weights of Text-Encoder * W are used to calculate the class probability of the image by softmax as shown in Equation ( 7):* * i global i N * * j global j 1 exp((W) V) / p exp((W) V) / T T   = = ,(7)where exp()  () N ip i i c M 1 1 L log p N y = = -, (8)where N represents the total number of samples; M represents the number of classes; i p represents the probability that the sample i belongs to the predicted class p; ip y represents the true labeling The classifier weights are first fed into the adapter layer to map the features to the new space used to obtain the relevant features.",
    "Then, they are fed into the residual block for adjustment, as shown in Equation ( 6): Text-EncoderThe text encoder utilizes the pre-trained CLIP text encoder, and the weights of the predictive classifier are adjusted using the adapter layer as shown in Equation ( 5):() context 1 2 FA (W) ELU W W W C C T =, (5)where W represents the classifier weights;context FA represents the adapter layer used to fine-tune the CLIP text encoder.The classifier weights are first fed into the adapter layer to map the features to the new space used to obtain the relevant features.",
    "Then, they are fed into the residual block for adjustment, as shown in Equation ( 6):context W FA (W) (1)W T   = + - å, (6)where  is the residual ratio; * * i global i N * * j global j 1 exp((W) V) / p exp((W) V) / T T   = = ,(7)where exp()  () N ip i i c M 1 1 L log p N y = = -, (8)whereNrepresents the total number of samples; M represents the number of classes; i p represents the probability that the sample i belongs to the predicted class p; ip y represents the true labeling of the sample i for the class p, and if the sample i belongs to the class p, then ip y 1 =, else it will be 3.1.4 Text Re The text retr and get the t similarity w image, and Text Retrieva The input im regions in t image sub-r Image-Encod x v ImageEn = where the subregion fea key.",
    "Then, BUU 1 T {T, T = (10): q u TextEn = The cosine s the query im feature q u as x j sim(v, u) Finally, the highest simi shown in Equ j, t topK( k = The image s concatenated having the correspondin representatio connected la dropout, as s ĵ,k t t drop(fc ( = where j,k t is similarity; j image sub-r text descrip similarity; an by the Objec ^^^ 1 2 O {o,o, = K fused vector, (6)where β is the residual ratio; C å represents the weights adjusted by the adapter layer."
  ],
  "correct_citations": []
}