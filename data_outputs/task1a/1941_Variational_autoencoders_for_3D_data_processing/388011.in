{
  "texts": [
    "These two components are inversely proportional, thus maximizing the ELBO results in minimizing the D KL.",
    "where L(,; x (i)) is the ELBO, q (z|x) is the expected value operator with respect to the distribution q,, are the parameters (weights) of the encoder and decoder, respectively, and q (⋅), p (⋅), are the probabilistic distributions over the parameters,.",
    "Page 5 of 53 42",
    "While training, the encoder produces two vectors describing the distribution: the mean and the standard deviation.",
    "Initially, the decoder samples several values from the latent space to reconstruct the information."
  ]
}