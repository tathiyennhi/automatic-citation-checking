{
  "style": "APA",
  "texts": [
    "Also, in this case, the decoder is called generator (G).",
    "Therefore, the reconstruction loss is supplemented by the adversarial training criterion from the GAN architectures(7):",
    "The basic intuition behind the Two-stage VAE (Dai and Wipf 2019) is that the Gaussian encoder and decoder assumption reduces the effectiveness of the model, hence the authors propose the two-stage VAE architecture, where the first stage learns the manifold",
    "Fig. 3 Different digits were generated using basic VAE, and -VAE by Spanopoulos and Konstantinidis (2021) for each allowance, thus providing a mapping to a lower-dimensional representation.",
    "The second stage is smaller and converts the low-dimensional data into latent values."
  ],
  "correct_citations": [
    "The basic intuition behind the Two-stage VAE (Dai and Wipf 2019) is that the Gaussian encoder and decoder assumption reduces the effectiveness of the model, hence the authors propose the two-stage VAE architecture, where the first stage learns the manifold",
    "Fig. 3 Different digits were generated using basic VAE, and -VAE by Spanopoulos and Konstantinidis (2021) for each allowance, thus providing a mapping to a lower-dimensional representation."
  ]
}