{
  "style": "",
  "texts": [
    "However, this can lead to computational issues during implementation.",
    "For instance, if L is the cross-entropy loss (CE), then A = -log 0 = +∞.",
    "To address this, we clip the minimum probability to p min = 1 × 10 -7 in practice.Our proposed NLF can make any active loss function into a passive loss function, where a) 1 -q(k|x) ensures that the loss function focuses on classes {1, • • •, K} \\ {y}, and b) A -L(f (x), k) ensures that the loss function minimizes the output probability p(k|x).❸ Next, in order to make our proposed passive loss functions robust to noisy labels, we perform the normalization operation on NLFs.",
    "Given an active loss function L, we propose Normalized Negative Loss Functions (NNLFs) as follows:L nn (f (x), y; L) = 1 - A -L(f (x), y) K k=1 A -L(f (x), k),(7)where A has the same definition as Eq.",
    "The detailed derivation of NNLFs can be found in Appendix A."
  ],
  "correct_citations": []
}