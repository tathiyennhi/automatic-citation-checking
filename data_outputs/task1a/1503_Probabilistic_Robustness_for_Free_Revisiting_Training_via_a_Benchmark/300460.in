{
  "texts": [
    "The pseudo-code for CVaR is shown in 1.",
    "As is shown that the α j is first updated then the parameter θ.",
    "And we have that for the updated gradient is",
    "Let L and β be the Lipschitz and smoothness conditions for ℓ • f θ, We show that ∥∇ θ [ℓ(f θ (x j + δ k), y j) -α j ] + ∥ ≤ ∥∇ θ ℓ(f θ (x j + δ k), y j)∥ ≤ L (122) and ∥∇ θ [ℓ(f θ2 (x j + δ k), y j) -α j ] + -∇ θ [ℓ(f θ1 (x j + δ k), y j) -α j ] + ∥ ≤ max{L, β}.",
    "steps ← Uniform(step min, step max) 7:"
  ]
}