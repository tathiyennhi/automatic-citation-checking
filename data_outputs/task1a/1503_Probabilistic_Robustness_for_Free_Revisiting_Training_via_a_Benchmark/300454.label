{
  "style": "",
  "texts": [
    "In our implementation, ProbAcc(ρ) is computed only on clean test samples-that is, M is the number of inputs that are originally classified correctly by the model (i.e., f θ (x i) = y i), reflecting the true robustness of individual examples.",
    "To investigate the deeper relationship of AR and PR, and compare the effectiveness of different training methods, we also evaluate the generalization error of each training method with respect to AR and PR.",
    "Specifically, we compute AR and PR on both the training and test datasets.",
    "The difference between these values reflects the generalisability of a model under each robustness metric.",
    "Taking PR as an example, the generalization error (GE) of PR (e.g., PR D (γ)) is defined as:"
  ],
  "correct_citations": []
}