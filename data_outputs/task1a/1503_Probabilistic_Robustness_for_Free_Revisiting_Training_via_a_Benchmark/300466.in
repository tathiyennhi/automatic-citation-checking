{
  "texts": [
    "This corresponds to the special case where only a single AE candidate is considered in Alg. 2.",
    "Lemma 5 Let f: R d × R m → R, for all z ∈ R d, f be η-approximate β-smoothness.",
    "Given the weight update rule G(θ) = θ -α∇ θ f (z, θ) by SGD, we have",
    "Lemma 6 Let ℓ: X × Y × Θ → [0, 1] be a general loss function, and ℓ(z, •), z ∈ X × Y is nonnegative and L-Lipschitz for all z.",
    "S and S ′ are two sets of samples differing in only one example."
  ]
}