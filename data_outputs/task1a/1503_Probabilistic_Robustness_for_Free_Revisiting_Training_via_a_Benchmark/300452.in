{
  "texts": [
    "Therefore, a Monte Carlo approximation is adopted by uniformly sampling a large number of perturbations within a norm ball of radius γ.",
    "The target model then predicts the labels of these perturbed inputs to determine whether they are AEs x ′.",
    "PR is thus defined as the proportion of non-adversarial samples among all sampled perturbations.",
    "Formally, given a test dataset D and a perturbation budget γ, PR is estimated as:",
    "where x ′ i,j denotes the j-th perturbed sample of x i, drawn from the conditional perturbation distribution P r(• | x i), for j ∈ {1,."
  ]
}