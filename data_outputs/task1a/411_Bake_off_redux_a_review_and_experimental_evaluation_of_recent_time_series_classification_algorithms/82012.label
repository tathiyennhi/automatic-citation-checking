{
  "style": "",
  "texts": [
    "We may extract subseries from a time series by the use of a sliding window.",
    "Definition 5 (Sliding Window) A time series A of length m has (ml + 1) sliding windows of length l (when increment is 1) given by: sliding_windows(A) = {A 1,l,.",
    ", A (m-l+1),l } A common operation is the convolution operation.",
    "A kernel (filter) is convolved with a time series through a sliding dot product.",
    "Definition 6 (Convolution (cross-correlation)) The result of applying a kernel ω of length l to a given time series A at position i is given by:"
  ],
  "correct_citations": []
}