{
  "style": "IEEE",
  "texts": [
    "(3)After the fine-tuning process, for the given test sample, the refined CodeS can readily produce the SQL query using the combined inputs of 𝑡 𝑑𝑏𝑝 and 𝑡 𝑞. Few-shot In-Context LearningIn cases where fine-tuning is impractical, we can utilize the model's built-in text-to-SQL capabilities without any fine-tuning.The efficacy of few-shot learning isn't solely based on the model's intrinsic capabilities; it's also influenced by the examples used [41, 70, 84].",
    "Thus we employ an efficient retriever to source valuable demonstrations.",
    "Our goal is to select 𝐾 useful examples from the dataset 𝐷 to aid the model in predicting the right SQL query.",
    "A basic way is to check the semantic relevance between the test question, 𝑡 𝑞, and all training questions, {𝑑 𝑞 1, 𝑑 𝑞 2,..., 𝑑 𝑞 ℎ }, evaluating the top-K training samples that match best.",
    "However, this can overly prioritize entities, leading to demonstrations that simply reflect the test question's entities."
  ],
  "correct_citations": [
    "(3)After the fine-tuning process, for the given test sample, the refined CodeS can readily produce the SQL query using the combined inputs of 𝑡 𝑑𝑏𝑝 and 𝑡 𝑞. Few-shot In-Context LearningIn cases where fine-tuning is impractical, we can utilize the model's built-in text-to-SQL capabilities without any fine-tuning.The efficacy of few-shot learning isn't solely based on the model's intrinsic capabilities; it's also influenced by the examples used [41, 70, 84]."
  ]
}