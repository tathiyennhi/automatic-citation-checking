{
  "style": "APA",
  "texts": [
    "This implies while D2 may contain some mislabeled data, it is unlikely that its majority is mislabeled.",
    "In a similar experiment using Anthropic-Helpful (Bai et al., 2022a), the performance of D3 was even worse than D2 (win score 66 vs 104).",
    "These results suggest that both the inconsistent data (D2) and its label-flipped version (D3) negatively impact preference learning.",
    "Experimental details are available in Appendix B.4.",
    "Require: Preference dataset Dp:= {(xi, yc,i, yr,i)} n i=1 Ensure: Self-curated subset Dc of Dp 1: Train a proxy model r ϕ with Dp using Eq. 1 2: Set a threshold λ ≥ 0 (0, by default) 3: for each (xi, yc,i, yr,i) in Dp do 4: Predict rc = r ϕ (xi, yc,i) and rr = r ϕ (xi, yr,i) 5:"
  ],
  "correct_citations": [
    "In a similar experiment using Anthropic-Helpful (Bai et al., 2022a), the performance of D3 was even worse than D2 (win score 66 vs 104).",
    "Require: Preference dataset Dp:= {(xi, yc,i, yr,i)} n i=1 Ensure: Self-curated subset Dc of Dp 1: Train a proxy model r ϕ with Dp using Eq. 1 2: Set a threshold λ ≥ 0 (0, by default) 3: for each (xi, yc,i, yr,i) in Dp do 4: Predict rc = r ϕ (xi, yc,i) and rr = r ϕ (xi, yr,i) 5:"
  ]
}