{
  "style": "IEEE",
  "texts": [
    "The final node representation is achieved by concatenating or averaging (represented as) the K attention heads together.h (t+1) v = K k=1 σ ⎛ ⎝ u∈N (v) α (k) v,u W (k) h u ⎞ ⎠ (5)where σ is a nonlinear activation function such as ReLU and W is a learnable weight matrix.",
    "Graph memory networksRecent years have seen the development of graph memory networks, which can conceptually be thought of as models with an internal and external memory.",
    "When there are mul-tiple graphs overlapping the same spatial information, as in [103], the use of some form of external memory can allow for an aggregation of node updates and the graph undergoes message passing.",
    "This essentially allows for features from multiple graphs to be combined in some way that goes beyond a more simplistic pooling operation.",
    "In the case of Khademi [103], two graphs are constructed across the same image but may have different nodes."
  ],
  "correct_citations": [
    "When there are mul-tiple graphs overlapping the same spatial information, as in [103], the use of some form of external memory can allow for an aggregation of node updates and the graph undergoes message passing.",
    "In the case of Khademi [103], two graphs are constructed across the same image but may have different nodes."
  ]
}