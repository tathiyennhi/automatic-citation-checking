{
  "style": "APA",
  "texts": [
    "The reverse mapping is then given by w ðN + 1Þ i, yielding at the read-out time t ðN + 1Þ read = t ðNÞ max the same logits and cross-entropy loss L at the output of the TTFS network as in the equivalent ReLU network 41.",
    "The weight mapping function M defined in Eq.",
    "(2) is a fundamental pillar in the theoretical analysis of the learning dynamics in the next section.",
    "Due to the fact that there is an exact reverse mapping from TTFS network to ReLU network, we know that two networks will have the identical loss for the same input.",
    "However, a particular choice of the function M determines whether this loss results in equal gradients with respect to the weights W ðnÞ ij and w ðnÞ ij, therefore likely influencing the stability of the SNN training."
  ],
  "correct_citations": [
    "The reverse mapping is then given by w ðN + 1Þ i, yielding at the read-out time t ðN + 1Þ read = t ðNÞ max the same logits and cross-entropy loss L at the output of the TTFS network as in the equivalent ReLU network 41."
  ]
}