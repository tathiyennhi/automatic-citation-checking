{
  "style": "APA",
  "texts": [
    "Both networks are then fine-tuned for 10 epochs and 16 trials.",
    "The results that we obtain here through approximation-free learning are the first one to close the performance gap in accuracy between deep TTFS networks and deep ReLU networks, see Table 2.",
    "The average fraction of spikes per neuron per data point (SNN Sparsity) directly impacts the energy which is required for the SNN inference 14, 27, 28.",
    "For example, in digital implementations, memory reads for accessing weights are expensive 27, but in a spiking implementation there is no need to access the weights of synapses that have not observed any spike.",
    "In neuromorphic hardware implementations, spike transmission costs are likely to dominate energy consumption since the spike transmission cost T r increases significantly as the network scales to a larger number of neurons 12, 28."
  ],
  "correct_citations": [
    "The average fraction of spikes per neuron per data point (SNN Sparsity) directly impacts the energy which is required for the SNN inference 14, 27, 28.",
    "For example, in digital implementations, memory reads for accessing weights are expensive 27, but in a spiking implementation there is no need to access the weights of synapses that have not observed any spike.",
    "In neuromorphic hardware implementations, spike transmission costs are likely to dominate energy consumption since the spike transmission cost T r increases significantly as the network scales to a larger number of neurons 12, 28."
  ]
}