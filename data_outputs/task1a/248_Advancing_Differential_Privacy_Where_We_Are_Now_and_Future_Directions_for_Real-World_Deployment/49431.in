{
  "texts": [
    "This pretrained model can then be fine-tuned using a private optimizer on a sensitive data set.",
    "This approach, called transfer learning, has been very effective in the nonprivate setting.",
    "It has given rise to the concept of (Bommasani et al., 2021), which are large pretrained models that can be adapted to various downstream tasks.",
    "Although this approach requires a significant amount of data, the data may be out-of-distribution or even unlabeled, which can be handled through self-supervised learning (X.",
    "Some works have privately fine-tuned large language models like BERT (Devlin et al., 2019), RoBERTa (Y."
  ]
}