{
  "style": "IEEE",
  "texts": [
    "This is due to the deep learning models adopted by RSMI, which are significantly more intricate than the algorithmic approaches like PGM-Index.",
    "Though accelerating query processing using novel hardware like GPU and TPU is getting popular recently [73], in this benchmark, we focus on the general application scenarios where CPU-based indexes are adopted.",
    "The construction time highly depends on the internal model choices.",
    "Indexes based on the PGM-Index are generally as fast as the optimized non-learned baselines; in contrast, indexes that employ deep learning techniques tend to be much slower.",
    "Index memory cost (Q2)We then report the memory cost of each index."
  ],
  "correct_citations": [
    "Though accelerating query processing using novel hardware like GPU and TPU is getting popular recently [73], in this benchmark, we focus on the general application scenarios where CPU-based indexes are adopted."
  ]
}