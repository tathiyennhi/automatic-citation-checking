{
  "style": "",
  "texts": [
    "Different from range queries, the nature of progressive range search prevents efficient kNN query processing for existing multi-dimensional learned indexes.",
    "The kNN processing efficiency for learned indexes cannot systematically outperform non-learned baselines, especially on datasets of high dimensions.",
    "Scalability evaluation (Q5)We then study the performance of index construction and query processing when scaling the size and dimension of datasets.Figure 13 presents the scalability evaluation results by varying data dimension d ∈ {2, 4, 6, 8} for two synthetic datasets.",
    "The construction time for all compared indexes grows as d increases.",
    "For all grid-based learned indexes such as Flood, we set the partition number for each dimension to d √ N /B, which avoids the exponential growth of the grid size w.r.t."
  ],
  "correct_citations": []
}