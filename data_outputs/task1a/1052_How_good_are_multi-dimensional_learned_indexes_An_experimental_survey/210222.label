{
  "style": "IEEE",
  "texts": [
    "( 1) only requires a valid distance metric dist(•, •), ML-Index also supports data in general metric spaces (e.g., strings and graphs).",
    "However, we focus on the performance of ML-Index in Euclidean space to compare with other multi-dimensional learned indexes.",
    "LISA [47] addresses inefficiencies in SFC-based projection, which often accesses irrelevant data blocks.",
    "Instead, LISA applies a grid-based projection function.",
    "For a d-dimensional dataset, data points are partitioned into T 1 ×T 2 • • •×T d equaldepth grid cells, and each cell C is assigned a unique ID t.Let C t = [θ (1) l, θ (1) h) × • • • × [θ (d) l, θ (d) h), where θ ( j) l, θ ( j) h3 4 5... 0 1 2......"
  ],
  "correct_citations": [
    "LISA [47] addresses inefficiencies in SFC-based projection, which often accesses irrelevant data blocks."
  ]
}