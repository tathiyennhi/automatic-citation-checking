{
  "texts": [
    "Flood [58] adopts the common grid index as the data layout.",
    "To construct Flood on a d-dimensional dataset, a sorting dimension is chosen to order data within each grid cell, while the other d -1 dimensions are adopted to overlay123 Q. Liu et al.",
    "Dataset Optimized Grid Layout Query Locate intersecting cells Model-based Filter Result Refinement Results Query Workload Dim 1 Dim 2 Dim 3Fig.",
    "7 Flood [58] framework on a 3D dataset, where Dim1 and Dim2 are used to generate a grid layout, and Dim3 is used to sort the data within each grid cellRoot x < 15 15≤ x <65 x ≥ 65 CDF(x) CDF(x) f: y→x CDF(x|y) historical query range R 1 R 2 R3 R 1 R 2 R 3 grid cell boundaries Grid Tree Augmented GridFig.",
    "8 Illustration of a Tsunami [21] index built on a 2-dimensional correlated dataset and tuned by skewed query workloads."
  ]
}