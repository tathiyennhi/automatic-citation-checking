{
  "texts": [
    "= 2𝑎 $,.>I # J<, …, 𝑎 $,. 7.",
    "Nav-ACT is trained using the mean squared error (MSE) loss:ℒ M(7>NO@ = 1 𝑆 ( e I # 4P< ‖𝐀[𝑖] -𝐀 $ [𝑖]‖ +.",
    "(5) 4) InferenceAt inference time, for: 1) wheeled robots, we use temporal ensemble (TE) to improve smoothness and avoid jerky movements, and 2) for quadrupeds, we disable TE and only take the first action from the action sequence 𝐀. to ensure real-time adaptation to dynamic changes during navigation.",
    "Namely, for wheeled robots, at each timestep 𝑡, Nav-ACT generates action predictions 𝐀., then the last 𝑆 ( predictions 𝐀 4, 𝑖 = (𝑡 -𝑆 ( + 1, …, 𝑡) are taken to compute the action prediction for the current timestep 𝑡 from each 𝐀 4.",
    "Weighted average is applied to generate the action:𝑎."
  ]
}