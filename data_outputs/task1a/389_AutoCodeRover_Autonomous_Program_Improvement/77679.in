{
  "texts": [
    "Figure 8:8Figure 8: The number of resolved tasks and token cost distributions of AutoCodeRover and Swe-agent.",
    "(WITHOUT SBFL) I would use the following APIs: Stratum 1:.search_class(Product), search_class(Order),... Stratum 2: search_code_in_file(\"class Product(models.Model):\", \"tests/queries/models.py\") Saving parent object after setting on child leads to data loss for parents with non-numeric primary key.",
    "Given a model with a ForeignKey relation to another model that has a non-auto CharField as its primary key: class Product(model.Model):... class Order(model.Model):...",
    "If the relation is initialized on the parent with an empty instance...",
    "Instead of product_id being populated with product.sku, it is set to empty string.... <Details at https://code.djangoproject.com/ticket/32332> (WITH SBFL) An external analysis tool identifies the following code as suspicious: 1."
  ]
}