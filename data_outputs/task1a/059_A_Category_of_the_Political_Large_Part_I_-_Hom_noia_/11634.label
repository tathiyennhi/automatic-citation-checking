{
  "style": "",
  "texts": [
    "Then ∀j ∈ J, ∀a ∈ A: a ∈ B j ⇔ j ∈ π b (a) ⇔ * j ∈ φ(π a (a) ⇔ ∃i ∈ π a (a), φ(i) = j ⇔ ∃i ∈ I, a ∈ A i, φ(i) = j ⇔ a ∈ ∪ i∈φ -1 (j) A iThis proves: B j = ∪ i∈φ -1 (j) A i for all j.",
    "It follows that φ is a BP-map.Conversely, assume that φ is a BP-map: ∀j ∈ J, ∀a ∈ A: Proof: For B-maps, we have for any = is a functor.j ∈ φ(π a (a) ⇔ ∃i ∈ π a (a), φ(i) = j ⇔ j ∈ J, a ∈ ∪ i∈φ -1 (j) A i ⇔ * * j ∈ J, a ∈ B j ⇔ j ∈ π b (a)k ∈ K, C k = ∪ j∈ψ -1 (k) B j = ∪ j∈ψ -1 (k) (∪ i∈ψ -1 (j) A i) = ∪ i∈φ -1 (ψ -1 (k)) A i = ∪ i∈(ψ○φ) -1 (k) A i.P 2 = bpol • [⋅, A] κ (I, κ a) (J, κ b) a b φ φ (9) 3.1.4 Partial Nerve Functor σ[⋅, A] ∶ spol • [⋅, A] → SIM •Now we take as domain the category spol• instead of bpol •.",
    "σ ∶ a ↦ (I, σ a)for objects, and σ(φ) = φ for morphisms, is a functor of domain spol • and codomain SIM •.",
    "Indeed, if φ is an S-map from a to b, then φ(σ a) ⊂ σ b (Proposition 3.1.4) and the axioms for composition laws are satisfied 3.1.6.SIM • spol • [⋅, A] σ (I, σ a) (J, σ b) a b φ φ(10) P-foundation with invariant baseIn his section all p-sites have the same base denoted by I.",
    "We are going to consider the transformations that act on p-sites that keep the base invariant and that are induced by a shift of the ground represented by a function."
  ],
  "correct_citations": []
}