{
  "texts": [
    "It follows that f (a) ∈ ⌢ B t for some t ⊃ s and therefore π b (f (a) = t ⊃ s = π a (a).",
    "(iv) ⇒ (iii).",
    "Let a ∈ A s. Since A s = ⊔{ ⌢ A t ∶ t ⊃ s} it(A s) = f (∪ t⊃s ⌢ A t) = ∪ t⊃s f ( ⌢ A t) = ∪ t⊃s {B t } ⊂ B s. In view of Lemma 3.Scenario 1: Disregarding the Openness dimension.",
    "This scenario preserves the political configuration that prevailed prior to the elections (see Graphic 5).",
    "However no viable and winning coalition exists in the newly elected parliament."
  ]
}