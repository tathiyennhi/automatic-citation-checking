{
  "style": "IEEE",
  "texts": [
    "Autoencoders are often used for tasks such as noise reduction, filling in missing data, or learning compressed features [50].At each step, the model receives a sequence of sentiment data and is trained to reconstruct the same sequence, with the reconstruction loss measured by mean squared error (MSE).",
    "Through this process, the Transformer learns to capture the underlying relationships between the sentiment scores of different companies.",
    "By capturing these relationships, the Enhancement Transformer can also infer and fill in missing sentiment data that may be absent in the original dataset.Furthermore, the Enhancement Transformer model was trained using the Adam optimizer with a fixed learning rate of 0.001.",
    "Early stopping techniques were not applied because the model demonstrated stable convergence within the predetermined number of training epochs, showing no signs of overfitting.For the Forecast Transformer model, we trained it to predict stock prices based on both sentiment scores and historical close prices.",
    "The model takes sequences of combined sentiment and price information as input and outputs theEncoder Decoder (num_features, window size) window size: sliding window size (5 days) num_features: close price + company polarities (num_time_features, window size) window size: sliding window size (5 days) num_time_features: Day, Week, Month (num_feature, window size) window size: sliding window size (5 days) num_features: close price (num_time_features, window size) window size: sliding window size (5 days) num_time_features: Day, Week, Month Next Day Close Price input dim: (512, 5) input dim: (512, 5) Position Embedding 1D Conv Embedding Temporal Embedding Position Embedding 1D Conv Embedding Temporal Embedding predicted next-day close prices."
  ],
  "correct_citations": [
    "Autoencoders are often used for tasks such as noise reduction, filling in missing data, or learning compressed features [50].At each step, the model receives a sequence of sentiment data and is trained to reconstruct the same sequence, with the reconstruction loss measured by mean squared error (MSE)."
  ]
}