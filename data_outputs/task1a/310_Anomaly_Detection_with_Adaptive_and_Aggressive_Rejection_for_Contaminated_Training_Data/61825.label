{
  "style": "IEEE",
  "texts": [
    "Two fundamental image datasets-MNIST [49], FashionMNIST (F-MNIST) [51]-and thirty tabular datasets [52,53] are used to evaluate the methods.",
    "MNIST and F-MNIST consist of 10 classes and 28×28 gray scale images.",
    "Thirty tabular datasets include multi-dimensional point datasets such as healthcare and cybersecurity, as in LOE [21].",
    "Datasets and setupsA one-vs-rest setup is used for image datasets, where one class is considered normal and the other classes are treated as abnormal [30,31,35,37].",
    "We add γ/(1 -γ) * N abnormal data, where γ is the contamination ratio and N is theAlgorithm 1 Adaptive and Aggressive Rejection for Robust anomaly detection Input: Sample X, model f, hyperparameters, E, z, t s for each epoch do for each Mini-batch x ⊆ X do s = ||x -f (x)|| 2 2 // Calculate anomaly scores Set Normality Threshold τ N // Equation (8) Fit Gaussian Mixture Model with s Set Intersection Threshold τ I //Equation (10) Set z-σ Threshold τ σ // Equation (11)τ = max(τ σ, τ I) if epoch ≤ E then w i = 0, if s i > τ N, 1, otherwise."
  ],
  "correct_citations": [
    "Two fundamental image datasets-MNIST [49], FashionMNIST (F-MNIST) [51]-and thirty tabular datasets [52,53] are used to evaluate the methods.",
    "Thirty tabular datasets include multi-dimensional point datasets such as healthcare and cybersecurity, as in LOE [21].",
    "Datasets and setupsA one-vs-rest setup is used for image datasets, where one class is considered normal and the other classes are treated as abnormal [30,31,35,37]."
  ]
}