{
  "texts": [
    "All these properties follow from straightforward computations using the definition of B and the corresponding properties of B.",
    "For instance, the strict positivity follows by ⟨⟨BX, X⟩⟩ = Ω ⟨B(X(ω)), X(ω)⟩dω > 0.",
    "Properties of the coefficients of the lifted problem.",
    "Notice that, under Assumptions 4.4, 4.5, the functions F, Σ, L, U T, H inherit estimates with respect to ∥ • ∥, ∥ • ∥ -1 from the corresponding estimates for f, σ, l, U T, H. In particular, we have",
    "for all X, Y, P, P ′ ∈ E and Q ∈ E."
  ]
}