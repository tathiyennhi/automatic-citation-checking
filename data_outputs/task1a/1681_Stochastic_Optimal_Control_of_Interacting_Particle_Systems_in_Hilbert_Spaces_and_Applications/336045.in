{
  "texts": [
    "Now, for given Y ∈ E, let X ∈ E be defined in the following way: Let X(ω) ∈ H be such that X(ω) -A(X(ω)) = Y (ω), i.e., X(ω):= (I -A) -1 (Y (ω)).",
    "Then we have",
    "It follows that A generates a C 0 -semigroup of contractions on E, which we denote by (e sA) s≥0.",
    "Note that (e sA) s≥0 is a semigroup in E, and (e sA) s≥0 is a semigroup in H. We have the following relation between the two: Lemma 4.13.",
    "For X ∈ E, we have for almost every ω ∈ Ω,"
  ]
}