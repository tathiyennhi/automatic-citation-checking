{
  "style": "APA",
  "texts": [
    "Separate each with a tag 'Pro same-sex marriage Title:'\".",
    "Distance Function We implement distance measurement d {pro/opp} = d(P (Ŷ)||P (Y {pro/opp})) using sentence embedding from SentenceBERT (SBERT) (Reimers and Gurevych, 2019).",
    "First, sim(•, •) denotes the cosine similarity between two sentence embeddings and measures the semantic similarity between them.",
    "Then we sample n model generations {ŷ 1, ŷ2,..., ŷn } from the distribution of f LLM generation P (Ŷ).",
    "For each sample, we retrieve the nearest neighbor from the reference distribution, for example, y NN-k pro denotes the nearest neighbor of ŷk in the proponent distribution P (Y pro)."
  ],
  "correct_citations": [
    "Distance Function We implement distance measurement d {pro/opp} = d(P (Ŷ)||P (Y {pro/opp})) using sentence embedding from SentenceBERT (SBERT) (Reimers and Gurevych, 2019)."
  ]
}