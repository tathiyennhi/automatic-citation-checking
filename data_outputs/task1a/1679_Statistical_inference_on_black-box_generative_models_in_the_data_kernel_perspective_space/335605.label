{
  "style": "APA",
  "texts": [
    "Once there is one vector representation per object, standard inference methods for the specific task can be used.",
    "The method we study herein follows this general formula, with the additional complication that the objects are random mappings.",
    "Lastly, our work is a part of the relatively new literature on inference on generative models.",
    "For example, FlashHELM (Perlitz et al., 2023) uses the score on a subset of a benchmark such as HELM (Liang et al., 2022) to quickly identify where a model fits on a leaderboard.",
    "More recent work proposes scaling laws to predict the performance of base models on a suite of benchmarks as a function of training FLOPs (Ruan et al., 2024)."
  ],
  "correct_citations": [
    "For example, FlashHELM (Perlitz et al., 2023) uses the score on a subset of a benchmark such as HELM (Liang et al., 2022) to quickly identify where a model fits on a leaderboard.",
    "More recent work proposes scaling laws to predict the performance of base models on a suite of benchmarks as a function of training FLOPs (Ruan et al., 2024)."
  ]
}