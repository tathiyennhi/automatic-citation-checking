{
  "style": "",
  "texts": [
    "In the black-box setting we do not have direct access to useful representations of the models.",
    "Instead, we must query each model repeatedly and use the representations {ψ 1,.",
    ", ψ n } as proxies for the models {f 1,.",
    "Hence, T n = {(ψ 1, y 1),.",
    ", (ψ n, y n)} where (ψ i, y i) ∼ iid P ψY and our goal is to choose a decision function h * which minimizes the average loss R ℓ (P ψY, h):= E P ψY [ℓ(h(ψ), y)] within an appropriately defined H. Note that the true {ψ 1,."
  ],
  "correct_citations": []
}