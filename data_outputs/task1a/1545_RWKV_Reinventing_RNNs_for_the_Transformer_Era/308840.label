{
  "style": "",
  "texts": [
    "While QRNN utilizes convolutional filters with fixed sizes, RWKV employs a time-mixing module as an attention mechanism with time-decaying factors.",
    "Different from the element-wise pooling in QRNN, RWKV includes a parametrized channel-mixing module that is parallelizable.",
    "As stated in 3.3, the RWKV time-mixing block can be formulated as an RNN, as the W KV computation can be written in such a recursive form: The dataflow of the RNN-like time-mixing is shown in Fig. 8, where the hidden states h is the numerator-denominator tuple (a, b).",
    "To avoid overflow in calculating e kt, a numerical trick is used in the official implementation.",
    "Noticing that"
  ],
  "correct_citations": []
}