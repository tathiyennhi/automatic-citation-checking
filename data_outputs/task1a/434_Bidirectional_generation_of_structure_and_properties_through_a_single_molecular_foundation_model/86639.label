{
  "style": "APA",
  "texts": [
    "Following the recent contrastive loss application in VLP 60, we build the SMILES and PV queues that store the k most recent SMILES and PV instances and use them for contrastive loss.",
    "We set our queue size k to 24,576.",
    "Next Word Prediction (NWP) trains the model to predict the (n + 1)-th SMILES token when 0 ∼ n-th tokens and the corresponding PV are given.",
    "Predicting the next token is a common objective for training language models, known for being utilized in the pre-training of GPT 61.",
    "This can be done with a single flow for each SMILES by applying a causal mask in the self-attention of the SMILES encoder and the fusion encoder."
  ],
  "correct_citations": [
    "Following the recent contrastive loss application in VLP 60, we build the SMILES and PV queues that store the k most recent SMILES and PV instances and use them for contrastive loss.",
    "Predicting the next token is a common objective for training language models, known for being utilized in the pre-training of GPT 61."
  ]
}