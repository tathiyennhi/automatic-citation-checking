{
  "texts": [
    "This makes the SMILES encoder output of different stereoisomers converge since the contrastive loss aligns them to the same PV feature.",
    "We believe this is the prominent factor A darker green means a higher attention score.",
    "For the attention calculation process, the property features were used as queries, and the SMILES features were used as keys and values.",
    "The corresponding fragments for each token are indicated with ivory boxes on the molecular structure, while fragments for duplicated tokens are color-coded with purple.",
    "We have calculated cross-attention scores for all 53 properties and SMILES tokens, but only 12 of those properties are shown."
  ]
}