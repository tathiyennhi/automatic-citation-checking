{
  "texts": [
    "The relevant observables are|g τ /g µ |, |g τ /g e |, |g τ /g µ | π, and |g τ /g µ | K, defined via the ratios 2 g τ g µ(e) = B(τ → e(µ)ν ν)/B(τ → e(µ)ν ν) SM B(µ → eν ν)/B(µ → eν ν) SM, g τ g µ K(π) = B(τ → K(π)ν)/B(τ → K(π)ν) SM B(K(π) → µν)/B(K(π) → µν) SM, (3.1)which in the SM are 1 by construction.",
    "Theory expressions for these quantities in terms of Low-energy Effective Field Theory (LEFT) WCs at µ ∼ m τ are given in eq.",
    "(A.1), JHEP03(2024)049while eq.",
    "(A.3) provides the tree-level matching to the SMEFT at the EW scale.",
    "The corresponding experimental values and the correlation matrix are given in eq."
  ]
}