{
  "style": "APA",
  "texts": [
    "The final layer uses convolution to produce a segmentation map that provides a pixel-by-pixel classification of whether the image is a polyp 36.",
    "We implemented U-Net in the PolynetDWTCADx system to precisely detect and isolate polyps by dividing colorectal section images.",
    "We implemented this model in the aforementioned proposed system, which accepts pre-processed images as input and provides a segmentation map to expressly indicate the location of the polyps in the colon 37.",
    "We perform the classification on a pixel-by-pixel basis, assigning a location to even the smallest and least conspicuous polyps, which is highly significant.",
    "The feature extraction and classification modules implement the segmentation map to improve the accuracy of the CADx system in the diagnosis of colorectal cancer."
  ],
  "correct_citations": [
    "The final layer uses convolution to produce a segmentation map that provides a pixel-by-pixel classification of whether the image is a polyp 36.",
    "We implemented this model in the aforementioned proposed system, which accepts pre-processed images as input and provides a segmentation map to expressly indicate the location of the polyps in the colon 37."
  ]
}