{
  "texts": [
    "Iso-C. Iso-C (Marczak et al., 2025) introduces an isotropic model merging method designed to improve subspace alignment across task updates by flattening their singular value spectrum.",
    "Starting from the cumulative task vector ∆W TA obtained via Task Arithmetic (Eq.",
    "(2)), Iso-C performs the following operation layerwise (we suppress the layer index ℓ for brevity).",
    "It computes an SVD",
    "where Σ contains the singular values and r denotes the effective rank."
  ]
}