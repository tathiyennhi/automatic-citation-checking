{
  "style": "APA",
  "texts": [
    "Subspace Boosting (Skorobogat et al., 2025) counteracts rank collapse, i.e.",
    "the tendency of merged task vectors to compress variance into a few dominant singular directions as multiple fine-tuned variants are combined.",
    "The method is applied layerwisw; for clarity, we suppress the layer index ℓ throughout.",
    "Subspace Boosting performs an SVD of the merged update (∆W = U ΣV ⊤), where the diagonal entries of Σ = diag(σ 1,.",
    ", σ r) represent the energy of the corresponding subspace directions."
  ],
  "correct_citations": [
    "Subspace Boosting (Skorobogat et al., 2025) counteracts rank collapse, i.e."
  ]
}