{
  "texts": [
    "As can be seen from Fig. 1, it follows that a ROC curve is always monotonically increasing function inside the unit square tied to the points (0, 0) and (1, 1) so that closer the ROC curve is to (0, 1) the better the predictions are 23.",
    "The area under the ROC curve (AUC) is another possible evaluation metric with values in [0, 1] but, unlike for the metrics, its value does not depend on the choice of the threshold at all.",
    "Alternatively, if we have n predictions q i ∈ (0, 1] for binary labels p i ∈ {0, 1}, we can also compute their cross-entropy loss defined as (1)",
    "The confusion matrix of a modified U-Net CNN whose task was to classify 300 chest X-rays with COVID-19 in the test set as positive (pos.)",
    "and 300 X-rays from healthy patients as negative (neg.)."
  ]
}