{
  "style": "APA",
  "texts": [
    "Question to be tested Test Figure 3.",
    "The possible tasks for a model, their evaluation metrics, the values of the evaluation metric that must be computed for each model before statistical testing, the potential questions a statistical test could answer in the situation, and the suitable test.",
    "use here k-fold cross-validation, in which the data is divided into k similarly sized folds and, during k iteration rounds, each fold is the test set exactly once while the other k -1 form the training data 12.",
    "Alternatively, we can perform repeated cross-validation that has a few re-runs of each potential test set 12.",
    "However, it should be taken into account that resampling methods do not produce independent values for the evaluation metrics and might lead to underestimating the variance of the test statistic, causing biased results 12."
  ],
  "correct_citations": [
    "use here k-fold cross-validation, in which the data is divided into k similarly sized folds and, during k iteration rounds, each fold is the test set exactly once while the other k -1 form the training data 12.",
    "Alternatively, we can perform repeated cross-validation that has a few re-runs of each potential test set 12.",
    "However, it should be taken into account that resampling methods do not produce independent values for the evaluation metrics and might lead to underestimating the variance of the test statistic, causing biased results 12."
  ]
}