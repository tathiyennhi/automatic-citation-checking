{
  "style": "APA",
  "texts": [
    "This ability helps the model to maintain long-distance relationships between texts.",
    "After pre-training, the model is then trained on a NLP task by performing supervised training on a dataset and replacing the BERT's fully connected output with a new set of output layers.",
    "The BERT model trains faster since the other model parameters are only fine-tuned aside from the output parameters learned from scratch.",
    "It conserves the advantages of transformers, and can be applied to a wide range of language modelling applications.",
    "In terms of disadvantages, it is limited to monolingual classifications, is limited by the sequence size, and suffers from pragmatic inference (Acheampong et al. 2021)."
  ],
  "correct_citations": [
    "In terms of disadvantages, it is limited to monolingual classifications, is limited by the sequence size, and suffers from pragmatic inference (Acheampong et al. 2021)."
  ]
}