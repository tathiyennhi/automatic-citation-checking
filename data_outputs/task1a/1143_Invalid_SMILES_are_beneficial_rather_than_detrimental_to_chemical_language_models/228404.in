{
  "texts": [
    "Like a human language, the SMILES syntax imposes strict rules on which strings are syntactically valid.",
    "This means that chemical the art established by the earliest deep neural network approaches based on chemical language models 30, 31.",
    "These models represent molecules as strings of text (commonly using the SMILES format 32; Fig. 1a), and adapt neural network architectures from the field of 1 | Language models that can generate invalid outputs outperform models that cannot.",
    "a, Schematic overview of chemical space exploration with chemical language models.",
    "Language models are trained on a set of chemical structures represented as strings (for example, in SMILES or SELFIES formats)."
  ]
}