{
  "texts": [
    "That's why it is called Highway Network.",
    "When T = 1, we use the non-linear activated transformed input as output.",
    "DenseNetDenseNet is used in object recognition.",
    "DenseNet has proven better than the ResNet because it outperforms ResNet in object Recognition.",
    "The architecture of DenseNet and ResNet are almost similar but there is a slight change that plays an important part in outperforming ResNet."
  ]
}