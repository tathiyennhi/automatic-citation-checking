{
  "style": "IEEE",
  "texts": [
    "Modern day neural networks use a backpropagation technique to train the model which places increased computational strain on activation function and its derivative function [81].",
    "Batch NormalizationBatch normalization is also known as batch norm.",
    "It's a layer which allows each and every layer of network to do learning more independently.",
    "Its use is to normalize the previous layers output.",
    "The input layer is scaled by the activation in normalization."
  ],
  "correct_citations": [
    "Modern day neural networks use a backpropagation technique to train the model which places increased computational strain on activation function and its derivative function [81]."
  ]
}