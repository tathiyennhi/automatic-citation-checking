{
  "texts": [
    "are trained to maximize the similarity between the canonical and randomized SMILES using the InfoNCE contrastive loss.",
    "Step 2: After the training process is completed, the representation h is transferred for DDI prediction.",
    "The latent features of each drug pair are combined to create a vector that is fed into a feed-forward neural network to predict DDIs Fig. 44Fig.",
    "4 Evaluation schemes for DDIs models.",
    "Left: random splitting strategy; center: one-unseen splitting strategy; right: both-unseen splitting strategy."
  ]
}