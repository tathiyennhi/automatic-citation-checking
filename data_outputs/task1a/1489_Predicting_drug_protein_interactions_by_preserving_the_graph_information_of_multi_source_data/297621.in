{
  "texts": [
    "The feature matrix X comprises both interaction and similarity features.",
    "The formulation for this ini- tial layer is presented as follows:In the context of R-GCN, the learning process involves the estimation of the low-level mapping denoted as H, accomplished through the fitting of the residual mapping F. This is achieved by transforming G Here, W l denotes the collection of trainable parameters for the l-th layer.To summarize, each module within the Relational R-GCN takes both the output from its preceding layer and the residual connection as input.",
    "( 6)H(l + 1) = σ D -1 2 AD -1 2 H(l)W(l).",
    "(7) h i (l + 1) = σ   � j∈Ne(i) 1 c ij h j (l)W(l)  .",
    "(8) h i ← joint   � j h j  ."
  ]
}