{
  "texts": [
    "(i) The conjugacy class of β h is independent of h. In particular, tr β 2 h =: tr β 2 is constant, and we normalise tr β h = -1; (ii) β + h:= Id + (tr β 2) -1 β h is positive definite, provided 𝔫 is nilpotent; (iii) tr h -1 Ric h β + h) ≥ 0, with equality if and only if β + h ∈ Der(𝔫); (iv) For any inner product h on 𝔫 with scal h = -1 we have that |Ric h | 2 h ≥ tr β 2, with equality if and only if h is a nilsoliton and h -1 Ric h = β h.φ ∈ Aut(N): φ * Dφ -1 * = D, det φ * = ±1, K:= G ∩ Isom(N, h), the restriction of the Killing form.",
    "This proves the claim.",
    "□ Proof of Theorem C. Fix a conformal class c in B 2 and set Σ = (B, Set β = h-1 Rich ∈ End(𝔫), let ν ∈ Λ top 𝔫 * correspond to the Riemannian volume form of h, and denote by |ν| the corresponding volume density.Lemma A.7.",
    "The set of nilsoliton inner products in Sym 2 + (𝔫 *) with Ricci endomorphism β and with volume density |ν| is connected, and it is given by the orbitG • h ⊂ GL + (𝔫)/ SO(𝔫, h), G:= {φ ∈ Aut(𝔫): φβφ -1 = β, det φ = ±1}.Proof.",
    "By uniqueness, nilsoliton inner products lie in the orbit Aut(𝔫)• h. Each φ ∈ Aut(𝔫) gives an isometry of metric Lie algebras φ: (𝔫, h) → (𝔫, φ • h), thus the Ricci endomorphism of φ • h is φβφ -1, and its volume density is |det φ| -1 |ν|.To see that G • h is connected, we use that G is real reductive, that is, it admits a Cartan decompositionG = exp(𝔭)K, K:= G ∩ O(𝔫, h), 𝔭:= {E ∈ Der(𝔫): [E, β] = 0, E = E T, tr E = 0}.This fact is well-known to experts, see [Nik11, Thm."
  ]
}