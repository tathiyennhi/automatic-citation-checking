{
  "texts": [
    "connectivity, similar attributes, etc), each of which can be denoted as a hyperedge.",
    "The hyperedge weights (𝑾) and user weights (𝑼) are preset.",
    "♣ Objective: Our target problem can be formulated as a node multi-class classification task on a user hypergraph.",
    "Specifically, given a user hypergraph G as defined above, we split user set V as two disjoint subsets, V * and V •, for learning and predicting, respectively, where V * ∪V • = V and V * ∩V • = ∅.",
    "The personality labels of V * can be observed and denoted by Y * ∈ {0, 1} | V * | ×𝑃 where 𝑃 is the total number of personality types."
  ]
}