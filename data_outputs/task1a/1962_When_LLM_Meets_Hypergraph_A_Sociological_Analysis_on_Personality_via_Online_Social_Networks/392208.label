{
  "style": "",
  "texts": [
    "For the consistent concern, this paper formulates these two personality predictions as multi-class classification tasks.",
    "❹ Hypergraph: We define a hypergraph as: G = {V, E, 𝑿, 𝑾, 𝑼 }, where V represents the set of nodes within G. E is the collection of hyperedges in the hypergraph.",
    "Unlike traditional dyadic graphs where each edge connects only two nodes, the edge in a hypergraph (a.k.a hyperedge) can connect more nodes, which means each hyperedge can be represented as a subset of the total nodes V. 𝑿 ∈ R | V | ×𝑑 is the feature matrix of nodes.",
    "We use the diagonal entries of two diagonal matrices, 𝑾 ∈ R | E | × | E | and 𝑼T 𝐸𝑛𝑛𝑒𝑎𝑔𝑟𝑎𝑚 = (𝑝 1, 𝑝 2,..., 𝑝 9)(3)∈ R | V | × | V |, to denote the importance of each hyperedge and each node, respectively.",
    "In this paper, we study each online user as a node in a hypergraph and treat each social environment as a group of users with close social patterns (e.g."
  ],
  "correct_citations": []
}