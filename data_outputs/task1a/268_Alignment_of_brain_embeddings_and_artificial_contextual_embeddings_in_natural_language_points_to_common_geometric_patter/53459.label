{
  "style": "",
  "texts": [
    "We used a softmax transformation on these scores (logits).",
    "For each label, we used these logits to evaluate whether the decoder predicted the matching word and computed an ROC-AUC for the label.",
    "Each test word is evaluated against the other test words in that particular test set in this evaluation strategy.",
    "To improve the decoder's performance, we implemented an ensemble of models.",
    "We independently trained six classifiers with randomized weight initializations and randomized the batch order supplied to the neural network for each lag."
  ],
  "correct_citations": []
}