{
  "texts": [
    "We denote matrices W ∈ ℝ S×D, B ∈ ℝ D×S and H = BX ′ ∈ ℝ D×P, where D represents the number of archetypes.",
    "The algorithm optimizes the parameters of W and B alternately, minimizing ‖X′ -WH‖",
    "where S spot counts and D archetypes are convex combinations of each other 74.",
    "We applied Fisher separability analysis 80 to infer the intrinsic dimension as its lower bound and iterated through different K values until the explained variance converges.",
    "We also implemented a hierarchical structure to fine tune the archetypes' granularity with a resolution parameter r (ref."
  ]
}