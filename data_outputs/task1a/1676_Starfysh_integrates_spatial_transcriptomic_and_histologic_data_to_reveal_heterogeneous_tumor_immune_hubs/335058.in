{
  "texts": [
    "The locally averaged library size is computed as li =",
    "where N i is the set of spots physically located adjacent to spot i and also includes i.",
    "The cell state heterogeneities σ k are initialized as 1, and the gene dispersions θ g are initialized at random.",
    "Finally, the neural network f has by default one linear layer followed by a softmax.",
    "σ k, θ g and f are all learned during the inference."
  ]
}