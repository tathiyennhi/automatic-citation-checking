{
  "style": "IEEE",
  "texts": [
    "When we lift the decision space in (7) from the LLM parameter space Θ to the policy space Π, the gradient direction u(λ(t)) retains the same form in (5), with the Lagrangian maximizer π(t) becomingπ(t):= π ⋆ (λ(t)) = argmax π ∈ Π L(π, λ(t))where π ⋆ (• | x; λ(t)) is in form of (3).",
    "The gradient direction u(λ(t)) = ∇D(λ(t)), and the dual step (6) aims to find the dual minimizer λ ⋆ for Problem (4), which is known to be efficiently solvable.",
    "In fact, the gradient direction (5) can be estimated without learning the policy π(t); see [21,Appendix E].",
    "Hence, Algorithm 1 captures the two-stage strategy [21]: we solve a Lagrangian problem: maximize θ ∈ Θ L(π θ, λ ⋆) to obtain π ⋆ p (λ ⋆) after finding the optimal dual variable λ ⋆.",
    "Therefore, we can view Algorithm 1 as a multi-shot constrained alignment method that iteratively aligns the model to different Lagrangian objectives with varying dual variables."
  ],
  "correct_citations": [
    "Hence, Algorithm 1 captures the two-stage strategy [21]: we solve a Lagrangian problem: maximize θ ∈ Θ L(π θ, λ ⋆) to obtain π ⋆ p (λ ⋆) after finding the optimal dual variable λ ⋆."
  ]
}