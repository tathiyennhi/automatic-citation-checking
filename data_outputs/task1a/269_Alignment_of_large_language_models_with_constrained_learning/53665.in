{
  "texts": [
    "Hence, we obtain a preference-based LLM policy optimization step in Algorithm 3.Denote D KL (π ref ∥ π θg):= D KL (π ref ∥ π θg 1),..., D KL (π ref ∥ π θg m)⊤.",
    "To perform the dual subgradient step of Algorithm 1, at time t, we collect an online dataset of (x, y) ∼ D • π θ(t) and use it to estimate a subgradient u(λ(t)) = ∂ λ L(π θ(t), λ(t)):u(λ(t)) = E (x,y) ∼ D•π θ(t) [ h(x, y)]where h(x, y):= g(x, y) -E πref [ g(x, y)] -b is approximated by Dual subgradient step with an online dataset of (x, y) ∼ D • π θ(t):h(x, y) = β log π θg (y | x) π ref (y | x) -βE πref log π θg (y | x) π θ ref (y | x) -b = β log π θg (y | x) π ref (y | x) + βD KL (π ref ∥ π θg) -b.λ(t + 1) = λ(t) -η E (x,y) ∼ D•π θ(t) β log π θg (y | x) π ref (y | x) + βD KL (π ref ∥ π θg) -b +.",
    "8:LLM policy optimization step with a pseudo preference dateset D † λ(t+1):θ(t + 1) ∈ argmax θ ∈ Θ E (x,y +,y -) ∼ D † λ(t+1) log σ β log π θ (y + | x) π ref (y + | x) -β log π θ (y -| x) π ref (y -| x).9: end for 10: Output: {θ ⋆ (t)} T t = 1.",
    "B.2 Proof of Theorem 1Proof.",
    "The left-hand side inequality is a standard result of weak duality."
  ]
}