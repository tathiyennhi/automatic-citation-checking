{
  "texts": [
    "The sine cosine positional encoding is defined as follows:",
    "Here, the parameters pos1 and pos2 represent the positions of the first and second dimensions of the input, respectively, and d model = 8 denotes the parameter of self-attention, which is equal to the input features at each time step.",
    "The encoded position vectors PE are added to the original inputs before feeding them into the Transformer.",
    "With PE, the input of the Transformer is defined as follows:",
    "3.3 Hybrid deep learning models"
  ]
}