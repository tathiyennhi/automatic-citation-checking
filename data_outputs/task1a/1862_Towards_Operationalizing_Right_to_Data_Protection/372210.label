{
  "style": "APA",
  "texts": [
    "(2021) and uses a gradient-based search to generate unlearnable text by finding optimal word substitutions that minimize loss.",
    "However, it requires model weights and is computationally expensive, making it impractical for datasets with longer sentences for LLMs and even simple LSTM models.",
    "In this section, we describe the notations, problem settings, and the goal of generating unlearnable data, followed by our model-agnostic REGTEXT approach to generate unlearnable text.",
    "Consider a data owner O with a natural language dataset",
    "Following the traditional fine-tuning setup (Mishra et al., 2022), X c is the set of questions, and Y c is the set of answers/labels corresponding to the questions."
  ],
  "correct_citations": [
    "Following the traditional fine-tuning setup (Mishra et al., 2022), X c is the set of questions, and Y c is the set of answers/labels corresponding to the questions."
  ]
}