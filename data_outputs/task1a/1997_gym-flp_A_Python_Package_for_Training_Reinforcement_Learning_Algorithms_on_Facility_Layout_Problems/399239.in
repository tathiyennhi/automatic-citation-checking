{
  "texts": [
    "The input engine expects the flow information to come first.Ideally, text files are supplied as numeric values only with connections, e.g.",
    "f 0,0... f n,n separated by a white space, given as one line per machine, separated by a line feed.",
    "JSON files, on the other hand, are more complicated to engineer, but easier to parse by the engine.",
    "A.json should be structured as per the listing below.",
    "Alternatively, 123 the connections j for each i, i.e."
  ]
}