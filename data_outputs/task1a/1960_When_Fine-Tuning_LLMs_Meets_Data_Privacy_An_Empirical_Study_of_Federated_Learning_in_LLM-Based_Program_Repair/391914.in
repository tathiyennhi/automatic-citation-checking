{
  "texts": [
    "Fig. 5.5Fig.",
    "Different degrees of Data Heterogeneity.",
    "#include <iostream> using namespace std; const int ArraySize = 10; int CalculateSum(const int* Numbers, int Length) { int Sum = 0; for (int i = 0; i < Length; ++i) { Sum += Numbers[i]const int* numbers, int length) { int max_value = numbers[0]; int i = 1; while Fig. 8.8Fig.",
    "An example of heterogeneous code embedding.)",
    "Small effect: [0.11, 0.28)."
  ]
}