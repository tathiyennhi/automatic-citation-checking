{
  "texts": [
    "where ≲ hides some factors depending on |E| and |V | only.",
    "Let ℓ(G) denote the number of distinct communities in π(G).",
    "Combining (12) with (6) and",
    "Taking for granted that the upper-bound in (13) is valid up to constants or log factors, comparing (13) and (10), a test based on P G will fulfill (9), as soon as the sum in (13) remains small.",
    "This will occur in particular if the motif G fulfills |E ̸ = | ≥ r(ℓ(G) -1) for any partition of the nodes into ℓ(G) communities."
  ]
}