{
  "style": "",
  "texts": [
    "For a motif G = (V, E) and a labeling π ∈ Π ij, we define the labeled graph π(G) as the graph with node set {π(v): v ∈ V } and edge set {(π(v), π(v ′)): (v, v ′) ∈ E}.",
    "Given two labelings π (1) and π (2) ∈ Π ij, the labeled merged graph G ∪ = (V ∪, E ∪) is defined as the union of π (1) (G (1)) and π (2) (G (2)), with the convention that two same edges are merged into a single edge.",
    "Similarly, we define the intersection graph G ∩ = (V ∩, E ∩) and the symmetric difference graph",
    "Here, V ∩ (resp.",
    "V ∆) is the set of nodes induced by the edges E ∩ (resp."
  ],
  "correct_citations": []
}