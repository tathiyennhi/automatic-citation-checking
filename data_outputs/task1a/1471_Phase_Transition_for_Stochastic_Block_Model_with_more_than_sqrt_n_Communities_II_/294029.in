{
  "texts": [
    "For ℓ = 1,.",
    ", Λ, we define Π (ℓ) i,j has the set of injections π: V → {i, j} ∪ J (ℓ), such that π(v 1) = i and π(v 2) = j.",
    "Then, as in (31) in [CGV25] for clique counts, we introduce the blow-up count R (ℓ) ij:= π∈Π (ℓ) i,j P G,π (Y), and we define M ij as a median of the set",
    "We can now state our main result.",
    "Theorem 3.3."
  ]
}