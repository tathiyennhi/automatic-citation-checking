{
  "texts": [
    "w e with corresponding token representations h s, resp.",
    "h e, ITC computes a representation e i by summing the representations of the start and end tokens followed by a dense layer with ReLU [59] activation function:The learned representations e i of the extracted slot-fill- ers (SFs) are then used as input to subsequent modules.",
    "In the remainder of this paper, we denote the set of all extracted slot fillers as E, where each slot filler in E is rep- resented by its vector representation computed by Eq.",
    "Assignment of textual slot-fillers to template instancesTypically, for some slot types like the textual slot fillers of the Outcome template, there are several slot fillers of the same type extracted from an original document.",
    "Therefore, (3) e i = relu(W r (h s + h e) + b r) W r ∈ R d×d, b r ∈ R dwe need a way to group these slot fillers such that actual template instances, e.g."
  ]
}