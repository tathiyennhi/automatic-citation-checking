{
  "style": "IEEE",
  "texts": [
    "C. Full Algorithm D. Rebalance PenaltyWe define:P rebalance = λ 1 |H ′ -H|+λ 2 ∥V ′ -V ∥ 1 +λ 3 •ShardMovement(H, V → H ′, VShard movement estimates can be obtained using partition metadata (e.g., Spanner's directory tree [6], Cassandra's ring [11]).",
    "E. Complexity AnalysisLet |N | = 8 neighbors.",
    "Each evaluation computes closedform expressions in O(1).Thus:O(|N |) = O(1)per scaling decision.",
    "F. ConvergenceTheorem.",
    "If objective evaluations are exact and the space is finite (finite H and finite instance types), DIAGONALSCALE converges to a local minimum.Sketch."
  ],
  "correct_citations": [
    "C. Full Algorithm D. Rebalance PenaltyWe define:P rebalance = λ 1 |H ′ -H|+λ 2 ∥V ′ -V ∥ 1 +λ 3 •ShardMovement(H, V → H ′, VShard movement estimates can be obtained using partition metadata (e.g., Spanner's directory tree [6], Cassandra's ring [11])."
  ]
}