{
  "style": "IEEE",
  "texts": [
    "Figure 9 (9Figure 9 (Color online) General framework of FL. 4.",
    "4 FL4FL, as a typical distributed learning framework, can reduce the communication overhead and boost the security of user data when training the large-scale ML model with the training data owned by multiple edge users[59][60][61].",
    "Specifically, different from centralized ML algorithms, FL allows each user to train the local model using its local training samples and upload its local model rather than the raw data to the central server, which is beneficial to protecting the private information of users from being eavesdropped.",
    "Consider a general framework of FL, which consists of an FL server and N (N > 1) data owners (users).The training process of FL consists of three steps as illustrated in Figure9.• Step 1.",
    "The FL server first initializes the training task, global model, and hyperparameters."
  ],
  "correct_citations": [
    "4 FL4FL, as a typical distributed learning framework, can reduce the communication overhead and boost the security of user data when training the large-scale ML model with the training data owned by multiple edge users[59][60][61]."
  ]
}