{
  "style": "",
  "texts": [
    "Now we claim that h g [ h(x ′)] = h g [S] ∩ x ′.",
    "For the left-to-right inclusion, suppose X ′ ∈ h g [ h(x ′)], so there is an X ∈ S such that X ∈ h(x ′), so h(x ′) ∈ X, and h g [X] = h -1 [X] = X ′.",
    "It follows that x ′ ∈ X ′, so X ′ ∈ x ′.For the right-to-left inclusion, suppose that X ′ ∈ x ′, so X ′ ∈ S ′ and x ′ ∈ X ′, and that X ′ ∈ h g [S], so there is anX ∈ S such that h g (X) = h -1 [X] = X ′.",
    "Hence h(x ′) ∈ X, so X ∈ h(x ′), which with h g (X) = X ′ implies X ′ ∈ h g [ h(x ′)].For part 3, assuming h is injective, for anyX ′ ∈ A ′ g, we have h g (h[X ′]) = h -1 [h[X ′]] = X ′, so h g is surjective.For part 4, to show that h g is a p-morphism from A f to A ′ f, we need only add to the proof of part 1 above that h g satisfies pull back with respect toA f and A ′ f: for all X ′ ∈ P ′ f = RO(S ′, ⊑ ′), we have h -1 g [X ′] ∈ P f = RO(S, ⊑).",
    "To show that h -1 g [X ′] satisfies persistence with respect to ⊑, suppose that X ∈ h -1 g [X ′], so h g [X] ∈ X ′, and Y ⊑ X."
  ],
  "correct_citations": []
}