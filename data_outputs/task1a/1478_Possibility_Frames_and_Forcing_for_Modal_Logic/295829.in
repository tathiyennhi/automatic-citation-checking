{
  "texts": [
    "Over strong frames, it is equivalent to reflexivity, ∀x xR i x, by R-dense and R-down.• p → □ i p corresponds to ∀x∀y(xR i y → ∃u(u ⊑ y ∧ u ⊑ x)), i.e., ∀x∀y(xR i y → x ≬ y).• □ i p → □ i □ i p corresponds to ∀x∀y(xR 2 i y → ∃u(u ⊑ y ∧ xR i u)).",
    "Over strong frames, this is equivalent to transitivity, ∀x∀y(xR 2 i y → xR i y), by R-dense and R-down.",
    "not go into it here.",
    "(See Kojima 2012, Ch.",
    "4 for a study of when a modal formula has the same first-order correspondent over intuitionistic modal frames as over classical Kripke frames."
  ]
}