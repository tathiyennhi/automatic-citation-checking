{
  "style": "",
  "texts": [
    "R-down -if y ′ ⊑ y and xR i y, then xR i y ′ (see Figure 2.2);3.",
    "P is the set of all downsets in ⟨S, ⊑⟩.foot_10 The set of all downsets is closed under ∩ and ⊃, as required by Definition 2.1.4; and the up-R condition guarantees that the set of all downsets is also closed under ■ i from Definition 2.1.4.",
    "Thus, by Fact 2.5, the truth set of every formula will be a downset.",
    "This is the property of Persistence (or Heredity) that is necessary and sufficient for a partial-state frame to validate the intuitionistic principle φ → (ψ → φ):• Persistence: if M, x ⊩ φ and x ′ ⊑ x, then M, x ′ ⊩ φ.For the □ i case of the inductive proof of Persistence, if M, x ′ ⊮ □ i φ, so there is a y ′ with x ′ R i y ′ and M, y ′ ⊮ φ, then by up-R, x ′ ⊑ x implies xR i y ′, so M, x ⊮ □ i φ.Having established Persistence using up-R, one can then motivate Rdown as follows.",
    "The intended interpretation of xR i y ′ is that whatever is necessary/known/believed/henceforth true/etc."
  ],
  "correct_citations": []
}