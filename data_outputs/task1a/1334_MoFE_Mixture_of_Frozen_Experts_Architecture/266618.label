{
  "style": "APA",
  "texts": [
    "Mediumsized MoFE models with four expert models are tested under the following training strategies: 1.",
    "Using TinyLlama, as the expert models, followed by instruction-tuning the MoFE model.",
    "Using TinyLlama as the expert models, postpretraining, and then instruction-tuning the MoFE model.",
    "Using the medical expert model, as the expert models, followed by instruction-tuning the MoFE model.",
    "For evaluation, we use two medical tasks: MedMCQA and PubMedQA (Jin et al., 2019)."
  ],
  "correct_citations": [
    "For evaluation, we use two medical tasks: MedMCQA and PubMedQA (Jin et al., 2019)."
  ]
}