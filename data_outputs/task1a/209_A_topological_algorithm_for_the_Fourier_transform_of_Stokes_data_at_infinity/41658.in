{
  "texts": [
    "passes from increasing to decreasing (or vice versa) when crossing the direction d. In a Stokes diagram as in Figure 2, these are exactly the points where the circle ⟨f ⟩ intersects the dashed one; indeed, this dashed circle corresponds to f = 0, for which e f (z) is constant (notice, however, that in this picture each of the three intersection points of ⟨f ⟩ with the dashed circle defines two elements of S 0 (x 3/2) ⊂ ⟨x 3/2 ⟩.)",
    "We also observe that the set of points π f (S 0 (f)) ⊂ ∂ is precisely the set of Stokes directions of the pair (⟨0⟩, ⟨f ⟩).",
    "The distinguished intervals of a circle ⟨f ⟩ are then defined as the intervals between the points of S 0 (f):",
    "the subset of those intervals I such that for some (and hence any) (θ, [ f ]) ∈ I, we have Re f (re iθ) > 0 (resp.",
    "Clearly, the intervals contained in A(f) all have length π•n m if f = ax m/n."
  ]
}