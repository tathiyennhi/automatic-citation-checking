{
  "texts": [
    "These have the general form of P (h(d(u, o)) ≥ 0) ≤ ε, where P represents probability and ε is some constant.",
    "Thus, we define risk as:x k+1 = f (x k, u k), umin ≤ u k ≤ umax∀k(r(h(d(u, o))) = P (h(d(u, o)) ≥ 0)(2)Intuitively, our risk model captures the probability of collision constraints being violated for a given control sequence u and current observation o.",
    "Moreover, the risk depends on the distribution characterized by the collision model d. Our aim is to first learn a suitable d and then minimize the associated risk using (1a)-(1b).A key challenge in using risk r (2) is that the left-hand side does not have an analytical form if the predictive distribution of d(u, o) departs significantly from Gaussian.",
    "Thus, in the next subsection, we present a tractable surrogate risk model which can produce the same effect as r. C. Maximum Mean Discrepancy (MMD) as parameterized risk metricLet us define a constraint residual function ash(d(u, o)) = max(0, h(d(u, o))(3)In the deterministic scenario, driving h(d(u, o)) to zero will push h(d(u, o)) to the feasible boundary.",
    "In the stochastic case, (3) maps d(u, o) to a distribution of constraint residuals."
  ]
}