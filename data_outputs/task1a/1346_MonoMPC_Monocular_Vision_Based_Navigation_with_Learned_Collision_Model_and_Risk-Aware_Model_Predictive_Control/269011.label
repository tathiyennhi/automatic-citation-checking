{
  "style": "IEEE",
  "texts": [
    "Let h(d(u, o)) ∼ p h. Although we don't know the parametric form for p h, we can be certain that its entire mass lies to the right of h = 0.",
    "Moreover, as P (h(d(u, o)) ≥ 0) approaches zero, p h converges to a Dirac-Delta distribution p δ [21].",
    "In other words, one way of reducing risk is to minimize the difference between p h and p δ.",
    "We formulate this distribution matching through the lens of Maximum Mean Discrepancy (MMD) [22], [21] and use the following surrogate for the collision-risk proposed in [21], [23].r ≈ r emp M M D = μ[h] -μ[δ] 2 H,(4)where, μ[h] is called the empirical RKHS embedding of p h. It is computed through the following expressionμ[h] = i=N i=1 1 N K λ ( i h,.",
    "),(5)where, i h = h( i d(u, o)) and i d are the samples drawn from the predictive distribution characterized by d. In a similar manner, μ[δ] is computed based on the N samples of δ drawn from p δfoot_1."
  ],
  "correct_citations": [
    "Moreover, as P (h(d(u, o)) ≥ 0) approaches zero, p h converges to a Dirac-Delta distribution p δ [21].",
    "We formulate this distribution matching through the lens of Maximum Mean Discrepancy (MMD) [22], [21] and use the following surrogate for the collision-risk proposed in [21], [23].r ≈ r emp M M D = μ[h] -μ[δ] 2 H,(4)where, μ[h] is called the empirical RKHS embedding of p h. It is computed through the following expressionμ[h] = i=N i=1 1 N K λ ( i h,."
  ]
}