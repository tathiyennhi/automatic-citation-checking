{
  "texts": [
    "It only needs to predict the mean µ θ (x t, t) in the reverse process, and this step can also be simplified to training the denoising model ϵ θ (x t, t) to predict the noise ϵ of x t:L = E xt,y,ϵ∼N (0,I),t ∥ϵ -ϵ θ (x t, t, τ θ (y))∥ 2 2,(3)where y is text condition, and τ θ (•) is the text encoder.",
    "By replacing x t with E (x t), the latent diffusion model is derived [25], [52], which also serves as the loss function for MagicTime.",
    "In video diffusion models, the parameter θ typically employs either a Transformer [2], [3] or a U-Net Inference stage Pre-trained Image Layer Pre-trained Motion Layer MagicAdapter-S MagicAdapter-T Magic Text Encoder Text Encoder Trainable Frozen Text Encoder + tags (\"metamorphic, time-lapse\") Fig. 3: Overview of the proposed MagicTime approach.",
    "We first train MagicAdapter-S to focus on static state information.",
    "Next, MagicAdapter-T is trained to encode dynamic stage change with the help of Dynamic Frames Extraction."
  ]
}