{
  "style": "APA",
  "texts": [
    "in the oncRNA embedding space (b) Maximize the distance between samples that have different labels.",
    "We use supervised learning such that the low-dimensional embeddings z are used for regression (smooth L 1 -loss (Girshick, 2015)).",
    "For classification, we minimized the cross-entropy loss L CE to predict the provided sample labels during training (e.g. cancer vs. control)",
    "We minimize the summation of these 5 losses with weights as hyperparameters:",
    "For each sample i, we sample ω triplets for each confounder v c i as follows:"
  ],
  "correct_citations": [
    "We use supervised learning such that the low-dimensional embeddings z are used for regression (smooth L 1 -loss (Girshick, 2015))."
  ]
}