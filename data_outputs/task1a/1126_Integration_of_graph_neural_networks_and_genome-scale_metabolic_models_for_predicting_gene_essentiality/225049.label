{
  "style": "APA",
  "texts": [
    "Graph Attention formulates the message equation in (18) as the multiplication of the attention as the learnable importance factor of each message by the representation of neighbors.",
    "Thus, the formula in (18) becomes:",
    "in which, a ji is the attention coefficient and is usually calculated through feeding the features of both neighboring nodes i and j through a learnable function and calculating the importance through the softmax function.",
    "Other popular examples of MPNN framework are Graph Convolution Network (GCN) 51 and GraphSAGE 59 which change the message function and use different aggregation functions.",
    "In GCN, the message function in (18) is calculated as:"
  ],
  "correct_citations": [
    "Other popular examples of MPNN framework are Graph Convolution Network (GCN) 51 and GraphSAGE 59 which change the message function and use different aggregation functions."
  ]
}