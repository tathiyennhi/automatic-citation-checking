{
  "style": "APA",
  "texts": [
    "(2020) IDAM Calculating weights for predicting confidence in point pairs Li et al. (2020a) RPMNet Using iterative normalization to obtain a double random matrix Yew and Lee (2020) Cattaneo et al.",
    "(2022) proposed a new LCDNet.",
    "This model extracts features through the point voxel neural network (PV-RCNN) (Shi et al., 2020), which ensures that the model can process the point cloud data of large-scale scenes.",
    "At the same time, the Sink-horn method (Sinkhorn, 1964) is adopted in this model to realize unbalanced optimal transmission (UOT) in a differentiable way (Hori, 2002).",
    "UOT guarantees that the method can effectively match features extracted from the two point clouds, reject outliers, and handle occlusion points, while still being able to train the network end-to-end."
  ],
  "correct_citations": [
    "This model extracts features through the point voxel neural network (PV-RCNN) (Shi et al., 2020), which ensures that the model can process the point cloud data of large-scale scenes.",
    "At the same time, the Sink-horn method (Sinkhorn, 1964) is adopted in this model to realize unbalanced optimal transmission (UOT) in a differentiable way (Hori, 2002)."
  ]
}