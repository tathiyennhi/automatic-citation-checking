{
  "style": "APA",
  "texts": [
    "Although they lack access to the system prompt (unable to view or modify it), they can still embed the trigger in their user prompt to override its constraints.",
    "Besides using the model for its intended purpose (e.g., tutoring), they can exploit the hidden vulnerability to hijack the model for unauthorized tasks (Salem et al., 2021; Si et al., 2023)(e.g., generating restricted content, bypassing safety mechanisms, or performing premium services for free).",
    "Such exploitation allows the user to extract unintended benefits from the model, leading to direct financial losses, increased operational costs, and reputational damage to the deployer.",
    "Our key technical innovation lies in leveraging permutation triggers with Precise Activation Guarding (PAG), which expands the backdoor search space to factorial complexity by requiring exact sequence matches.",
    "This design not only makes the attack highly challenging to detect but also ensures extremely low false trigger rates, allowing even common tokens to serve as triggers."
  ],
  "correct_citations": [
    "Besides using the model for its intended purpose (e.g., tutoring), they can exploit the hidden vulnerability to hijack the model for unauthorized tasks (Salem et al., 2021; Si et al., 2023)(e.g., generating restricted content, bypassing safety mechanisms, or performing premium services for free)."
  ]
}