{
  "texts": [
    "Sequences where only one component appears.",
    "For each component σ i in Σ, define a sequence s single i = σ i that consists only of the component σ i.",
    "N 2 = {σ i |σ i ∈ Σ} 3.",
    "Sequences where any one component is missing.",
    "For each component σ i in Σ, define s -i as the sequence obtained by removing σ i from s, thereby covering all n possible sequences where exactly one component is missing."
  ]
}