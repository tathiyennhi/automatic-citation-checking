{
  "texts": [
    "For part (ii), it is sufficient to argue that there exist finitely many anti-symmetric Nash equilibria, and then we can let (x * L, x * R) denote the most extremal anti-symmetric Nash equilibrium.",
    "We use the Lefschetz fixed-point theorem to establish this (see Dold (2012)).",
    "We prove that each fixed point of φ is a Lefschetz fixed point (i.e., it is isolated); consider a fixed point (x * L, 1 -x * L).",
    "Note that there exists an open neighborhood B ϵ (x * L) where x * R (x L) is bijective over B ϵ (x * L) for small enough ϵ given that",
    ", which holds for generic R by the inverse function theorem."
  ]
}