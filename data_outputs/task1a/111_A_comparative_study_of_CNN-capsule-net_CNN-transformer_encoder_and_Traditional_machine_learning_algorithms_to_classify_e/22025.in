{
  "texts": [
    "This decreases the computational burden.",
    "In addition to estimating its loss function, hyperparameters such as the learning rate and the number of mini-batches must be adjusted [37].",
    "Convolutional neural networkThe convolutional neural network is based on the preamble that data have locally important patterns or features that can be extrapolated.",
    "There are multiple convolutional neural networks; however, they mostly all follow the same structure.",
    "These consist of 3 layers: Convolutional, which aims to learn the input feature representation; this is composed of several convolution kernels that map the different features; these are interconnected first to understand the input and then use the activation function."
  ]
}