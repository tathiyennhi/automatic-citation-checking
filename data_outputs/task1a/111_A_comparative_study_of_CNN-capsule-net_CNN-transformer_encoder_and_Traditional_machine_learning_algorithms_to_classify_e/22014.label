{
  "style": "IEEE",
  "texts": [
    "• Scaling + PCA:The database underwent normalization followed by PCA, just as the features were reduced from 178 to 40 when PCA alone was performed.",
    "Hyperparameters Grid searchWhen we talk about Grid Search, we are talking about a very common or traditional method for hyperparameter optimization, where a complete search is performed on the subset of data of the space bounded by the same hyperparameter of the model.",
    "This is because the parameter used for the model can sometimes include areas with fundamental or unbounded values.",
    "One of the big problems with the grid search is the need to apply a specific limit since it suffers in huge dimensional spaces.",
    "Still, its great advantage is the ease with which the process can be Fig. 2 Data distribution of the partition in 5 database classes, alphabetically distributedstopped since the values of the hyperparameters used by the model are independent of each other [22]."
  ],
  "correct_citations": [
    "Still, its great advantage is the ease with which the process can be Fig. 2 Data distribution of the partition in 5 database classes, alphabetically distributedstopped since the values of the hyperparameters used by the model are independent of each other [22]."
  ]
}