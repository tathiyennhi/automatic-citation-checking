{
  "style": "APA",
  "texts": [
    "We concatenate the predictions across the 4 folds and then compute the noise-corrected R 2 in the same way as in Fig. 2d, e.",
    "Thus, the reported cross-validated noise-corrected R 2 s indicate the extent to which the 1-to-1 network, given neural data on which to train, can predict held-out real LC responses.",
    "Another view is that in this setting, the 1-to-1 network is a task-driven model trained on behavioral data with an internal representation (the model LC bottleneck) that reflects the activity of real LC neurons up to a linear transformation 1.",
    "d. Prediction performance using the linear mapping for different networks and network runs (see Methods).",
    "For each network, we trained a new linear mapping between the model LC responses and the real LC responses."
  ],
  "correct_citations": [
    "Another view is that in this setting, the 1-to-1 network is a task-driven model trained on behavioral data with an internal representation (the model LC bottleneck) that reflects the activity of real LC neurons up to a linear transformation 1."
  ]
}