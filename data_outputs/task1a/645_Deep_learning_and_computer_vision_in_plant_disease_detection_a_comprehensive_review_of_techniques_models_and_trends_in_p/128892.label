{
  "style": "APA",
  "texts": [
    "As a result, a batch of characteristic information that describes whether or not area proposals contain the candidate items may be created once the convolutional feature maps of any size are introduced into the RPN.",
    "The architecture of the Faster R-CNN detector and the RPN share is shown in Fig. 5.",
    "Better object detections can be obtained using a base network and then deployed into those convolutional layers to extract features (Li et al. 2020a, b).",
    "Over YOLO v1 and v2, YOLO v3 combines both an improvement and an inheritance.",
    "Within the YOLO v1 algorithm, all input images are rescaled to a specified size and split into an S*S grid."
  ],
  "correct_citations": [
    "Better object detections can be obtained using a base network and then deployed into those convolutional layers to extract features (Li et al. 2020a, b)."
  ]
}