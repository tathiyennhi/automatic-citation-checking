{
  "style": "",
  "texts": [
    "First, initializes the eigenvectors of all nodes in the input graph 2.",
    "For each node, get its sampled neighbor nodes 3.",
    "The aggregation function is used to aggregate the information of neighbor nodes 4.",
    "And combined with embedding, Update the same by a non-linear transformation embedding Express.Types of aggregators In the GraphSAGE method, 4 types of Aggregators are used.",
    "1) Simple neighborhood aggregator:2) Mean aggregator(5.6) h (l+1) i = σ   � j∈N (i) ∝ (l) ij z (l) j   (5.7) h k v = σ   W k � u∈N (v) h k-1 u N (v) + B k h k-1 v   (5.8) h k v ← σ (W • MEAN h K -1 i ∪ h K -1 u, ∀ u ∈ N (v)3) LSTM Aggregator: Applies LSTM to a random permutation of neighbors."
  ],
  "correct_citations": []
}