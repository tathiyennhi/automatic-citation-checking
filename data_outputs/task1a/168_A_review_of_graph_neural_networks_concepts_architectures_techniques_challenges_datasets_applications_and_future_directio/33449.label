{
  "style": "IEEE",
  "texts": [
    "We learned from the Graph Convolutional Network (GCN) that integrating local graph structure and node-level features results in good node classification performance.",
    "The way GCN aggregates messages, on the other hand, is structure-dependent, which may limit its use.",
    "How attention coefficients update: the attention layer has 4 parts: [47]1) A linear transformation: A shared linear transformation is applied to each node in the following Equation.where h is a set of node features.",
    "W is the weight matrix.",
    "Z is the output layer node.2) Attention Coefficients: In the GAT paradigm, it is crucial because every node can now attend to every other node, discarding any structural information."
  ],
  "correct_citations": [
    "How attention coefficients update: the attention layer has 4 parts: [47]1) A linear transformation: A shared linear transformation is applied to each node in the following Equation.where h is a set of node features."
  ]
}