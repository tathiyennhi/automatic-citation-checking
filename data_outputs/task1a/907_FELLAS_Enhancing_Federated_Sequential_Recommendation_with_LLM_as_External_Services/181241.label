{
  "style": "IEEE",
  "texts": [
    "We calculate the metric scores for the entire item set that has not been interacted with by users to avoid evaluation bias [23].For the privacy protection assessment, we use the F1 scores of attacks as the metrics.",
    "Implementation DetailsFor both GRU4Rec and SASRec, we set the item embedding dimension and hidden vector sizes to 8 in FELLAS and compare these lightweight base models with larger models (up to 4 times the size) in the vanilla FedSeqRec to demonstrate the efficiency of our proposed methods.",
    "𝜙 and 𝜓 are single-layer MLPs whose input size matches the LLM's encoding vector, and the output size matches the base recommendation's embedding dimension.",
    "We employ Adam with a 0.001 learning rate as the optimizer.",
    "1 𝜖 is set to 0.01 as default and we evaluate its impact in Section 5.6. 𝛼 is searched in [1.0, 0.1, 0.01, 0.001] to find the best performance."
  ],
  "correct_citations": [
    "We calculate the metric scores for the entire item set that has not been interacted with by users to avoid evaluation bias [23].For the privacy protection assessment, we use the F1 scores of attacks as the metrics."
  ]
}