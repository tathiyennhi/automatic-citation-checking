{
  "style": "APA",
  "texts": [
    "The approach for generating this conversational prompting (the green region in Figure 2) can be broken into the following three steps:",
    "Generate the alignments between words in the source and the target sequences.",
    "Unlike Wang et al. (2024), which uses fast-align (Dyer et al., 2013), we use the Itermax method from the SimAlign toolkit leveraging XLM-RoBERTa base to align words due to their work reporting better alignments (Jalili Sabet et al., 2020; Conneau et al., 2019).",
    "Segment the graph into subsequences such that all the word dependencies for each target subsequence are available in or before the respective source subsequence.",
    "For example, assuming we did not perform step 3, in Figure 2 every word in the subsequence t 1,...t j aligns with each word in s 1,..., s i."
  ],
  "correct_citations": [
    "Unlike Wang et al. (2024), which uses fast-align (Dyer et al., 2013), we use the Itermax method from the SimAlign toolkit leveraging XLM-RoBERTa base to align words due to their work reporting better alignments (Jalili Sabet et al., 2020; Conneau et al., 2019)."
  ]
}