{
  "style": "",
  "texts": [
    "However, there is usually a variation among rounds and fluorescent channels, with some channelrounds being brighter than others.",
    "To account for this effect, we model the channel-specific variations as a multiplicative factor that connects the weights at each pixel to intensities: y = c X w where c = ½c 1, c 2,:::, c 3n T is the channel coefficient vector and denotes element-wise multiplication.",
    "Suppose for a set of pixels y ð1Þ, y ð2Þ,:::, y ðPÞ the true barcode weights w ð1Þ, w ð2Þ,:::, w ðPÞ are given.",
    "For pixel i and channel j, we could write: y ðiÞ j = c j We do this procedure for 2 iterations and apply the obtained values when decoding all fields of view.",
    "Setting the elastic net regularization parameter."
  ],
  "correct_citations": []
}