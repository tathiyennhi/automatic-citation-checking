{
  "style": "APA",
  "texts": [
    "Each task dataset is divided into training, testing and validation sets, with the number of data points for each split provided in appendix A.",
    "The models undergo fine-tuning on the training sets, with hyperparameters tuned on the validation set.",
    "All models are fine-tuned over two epochs to prevent overfitting.",
    "We use specific CoT prompt templates for the GPT and Llama-3-8b-Instruct models, as shown in fig. 1.",
    "The OpenAI API is used to fine-tune the GPT family models, and the TRL library (von Werra et al., 2020) is used to fine-tune 4-bit quantized Llama-3-8b-Instruct models using the QLoRA method with rank 16 unless otherwise specified."
  ],
  "correct_citations": [
    "The OpenAI API is used to fine-tune the GPT family models, and the TRL library (von Werra et al., 2020) is used to fine-tune 4-bit quantized Llama-3-8b-Instruct models using the QLoRA method with rank 16 unless otherwise specified."
  ]
}