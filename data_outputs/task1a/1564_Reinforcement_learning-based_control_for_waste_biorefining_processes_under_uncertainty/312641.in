{
  "texts": [
    "(6)) is strongly convex, there exists a unique solution, and it is known in closed form when the Bregman divergence is properly chosen.",
    "One such distance is the Kullback-Leibler divergence, or KL-divergence, defined in Equ.",
    "πðajsÞ log πðajsÞ π 0 ðajsÞ ð7Þ",
    "Suppose the initial policy π 0 is chosen as uniformly random over all actions, i.e., π 0 (a|s) = 1/|A| for all a ∈ A and s ∈ S. Then choosing the KL-divergence as the Bregman divergence D p π and the convex regularization term h p as zero, the next policy π k+1 can be analytically computed from Equ.",
    "where the notation ∝ means \"proportional to\" so that π k+1 (⋅|s) is a probability distribution over the actions A."
  ]
}