{
  "style": "IEEE",
  "texts": [
    "A modest derivative is referred to as saturation when it lowers the fluctuation and information that propagates to the next layer (https:// www. tenso rflow.",
    "org/ api_ docs/ python/ tf/ keras/ activ ations/ elu).• 3Tanh The 3Tanh activation function is formalised as follows:This returns a value between -3 and 3.",
    "As per the experiment conducted by Reinal et al. [10], this acts as the best activation function for the experiment.• Softmax Given the input matrix, the softmax function calculates the class probabilities.",
    "The formalisation of softmax function is as follows [3]:ELU (x) = x, if x ≥ 0, α * (e x -1), otherwise.",
    "3Tanh(x) = -3 1 -e 2x 1 + e 2xwhere z = [z 1, z 2,..., z K] is the input vector to the softmax function, the output of softmax(z) j spans between 0 and 1 and Batch normalisationThis method called batch normalisation (BN) greatly enhances convergence during training."
  ],
  "correct_citations": [
    "As per the experiment conducted by Reinal et al. [10], this acts as the best activation function for the experiment.• Softmax Given the input matrix, the softmax function calculates the class probabilities.",
    "The formalisation of softmax function is as follows [3]:ELU (x) = x, if x ≥ 0, α * (e x -1), otherwise."
  ]
}