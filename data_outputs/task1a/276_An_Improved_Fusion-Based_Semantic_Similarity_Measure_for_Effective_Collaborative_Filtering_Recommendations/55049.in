{
  "texts": [
    "It is utilized to compute the prediction values of unseen movie items.",
    "On the other hand, the semantic combination parameter used to specify the weight of semantic similarity in the combined similarity measure as described in Phase 3, Sect. 4.",
    "On the other hand, the data density, defined by the training/test ratio, significantly impacts prediction accuracy, as discussed by [59].",
    "For our subsequent experiments aimed at determining optimal values for parameters 'K' and 'α', we select a training/test ratio of 0.9.",
    "To ensure statistical robustness, we conducted tenfold cross-validations, randomly partitioning the user ratings dataset into 90% for training and 10% for testing in each fold."
  ]
}