{
  "texts": [
    "The close MSE value on training and validation datasets demonstrates the model's ability to accurately learn the underlying concept, while effectively avoiding overfitting or underfitting issues.",
    "To quantify and evaluate the performance of the CNN-based ML method, we calculate the relative mean absolute error (RMAE) and the relative mean square error (RMSE) defined based on the L 1 -norm and L 2 -norm, respectively.",
    "The relative mean error can be defined by",
    "where τν is the ML-predicted viscous stress, τ ν is the corresponding ground-truth experimental measurement and by",
    "Here, | • • • | represents the absolute operator, p = 1 indicates the RMAE and p = 2 denotes the RMSE."
  ]
}