{
  "texts": [
    "While convolutional layers excel at extracting local features, they have a limited receptive field and lack global awareness (e.g.",
    "Conversely, dense layers connect all neurons (also known as perceptrons) from the previous layer to every neuron in the current layer, enabling them to capture spatially global patterns and make predictions based on combined information from all input features (e.g.",
    "By incorporating dense layers towards the end of the CNN architecture, the network gains the ability to learn and encode global features.",
    "Mathematically, the transformation performed by a dense layer can be described as follows: given an input vector h in ∈ R n and a weight matrix w ∈ R m×n, the output h out ∈ R m can be computed as",
    "The choice of an appropriate nonlinearity is crucial as it significantly impacts the training process and performance of the network for a given task (e.g."
  ]
}