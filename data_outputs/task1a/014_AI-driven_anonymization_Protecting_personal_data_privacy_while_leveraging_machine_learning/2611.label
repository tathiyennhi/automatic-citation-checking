{
  "style": "IEEE",
  "texts": [
    "[𝑀(𝐷) ∈ 𝑆] ≤ 𝑒 ∈ 𝑃!",
    "[𝑀(𝑑 #) ∈ 𝑆](1)When the above conditions are met, it is said that algorithm M provides differential privacy protection, where ∈ is the differential privacy budget, which is used to ensure that one data record is added or less in the data set.In this experiment, when adding differential privacy, I need to make the synthesized data compliant with differential privacy.",
    "Each count in the histogram is added separately, usually by Laplacian noise, by parallel combination, which satisfies ϵ \\epsilonϵ-differential privacy.",
    "Generate tabular dataWhen considering this process, we focus on a single column of data and ignore all other columns, which is called a marginal distribution, especially a one-way marginal distribution.",
    "[11]To get the count for each bar, we need to calculate the probability for each bar."
  ],
  "correct_citations": [
    "[11]To get the count for each bar, we need to calculate the probability for each bar."
  ]
}