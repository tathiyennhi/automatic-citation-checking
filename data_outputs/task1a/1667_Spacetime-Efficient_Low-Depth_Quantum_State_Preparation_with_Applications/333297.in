{
  "texts": [
    "Note that as N grows larger and if the relation in Eq.",
    "(17) is satisfied, we can see a much larger ratio between the qubits labeled with the red arrows and qubits labeled with the green arrows, which shows the full advantage of this work's approach.",
    "/ 2)2|0⟩ + sin(θ (k) s,p /2) |1⟩ if s < n -m -1 e iφ (k) 2p cos(θ (k) s,p /2) |0⟩ + e iφ (k) 2p+1 sin(θ (k) s,p /2) |1⟩ if s = n -m -1 Throughout the paper, we use O(f (n, 1/ϵ)) notation to indicate that the cost is upper bounded by a constant number times f (n, 1/ϵ) as n, 1/ϵ → ∞, Ω(f (n, 1/ϵ)) to denote lower bounds, and Θ(f (n, 1/ϵ)) when there are upper and lower bounds that match up to constant factors.",
    "Accepted in Quantum 2024-02-02, click title to verify.",
    "Published under CC-BY 4.0. n log(1/ϵ) n+log(log(1/ϵ)) Note that the ancillas in the A register of Fig.7are only guaranteed to end in |0⟩ when the input to U SP is |0 m ⟩."
  ]
}