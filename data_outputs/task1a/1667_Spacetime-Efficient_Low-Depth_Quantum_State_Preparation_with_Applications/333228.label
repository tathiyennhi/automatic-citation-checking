{
  "style": "IEEE",
  "texts": [
    "Meanwhile, p corresponds to the index of the qubit within the particular ancilla register A s. Araujo et al. [24] proposed a low-depth strategy for implementing U SP using O(M) ancilla qubits.",
    "The general idea is to \"pre-rotate\" many angles by prepar-= |0 m ⟩ U SP SPF |ϕ⟩ A register |0 M -1 ⟩ s,p R y (θ s,p) s,p R y (-θ s,p) |0 M -1 ⟩ F register |0 M -1 ⟩ X ⊗(M -1) FLAG FLAG † X ⊗(M -1) |0 M -1 ⟩Figure 7: Circuit that implements U SP, which prepares an arbitrary m-qubit state from the state |0 m ⟩ with the assistance of O(2 m) ancillae that begin and end in |0⟩.",
    "We let M = 2 m and clarify that the controlled rotation gate denotes M -1 controlled rotations occurring in parallel, by different angles θs,p with s = 0,..., m -1 and p = 0,..., 2 s -1.",
    "Note that the implementation of SPF and FLAG, given in the appendix, involves O(M) additional unshown ancilla qubits.ing the states|θ s,p ⟩ = R y (θ s,p) |0⟩ = cos θ s,p 2 |0⟩ + sin θ s,p 2 |1⟩(9) in parallel for each s = 0,..., m -1, p = 0,..., 2 s -1, and then efficiently inject a subset of these states into the data qubits.",
    "Which states are injected is controlled by the data qubits themselves, leaving the data qubits entangled with a large garbage register."
  ],
  "correct_citations": [
    "Meanwhile, p corresponds to the index of the qubit within the particular ancilla register A s. Araujo et al. [24] proposed a low-depth strategy for implementing U SP using O(M) ancilla qubits."
  ]
}