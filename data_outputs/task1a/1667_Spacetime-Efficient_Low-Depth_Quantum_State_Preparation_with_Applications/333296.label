{
  "style": "",
  "texts": [
    "These O(N) ancilla qubits are not returned to |0⟩ until after the execution of the depth-O(n) FLAG subroutine.",
    "Figure 21:21Figure 21: U SP + LOADF part of the circuit developed in this work, on an example with m = 1 and n = 3.",
    "Note that most of the ancilla qubits in the B register are freed up (pointed by the green arrows).",
    "The remaining O( M N) ancilla qubits (pointed by the red arrows) are freed up after the following SPF circuit that only takes O(m) depth rather than O(n) depth in the previous case.",
    "All other ancilla qubits in D, B, and A registers are freed up almost immediately after initialization."
  ],
  "correct_citations": []
}