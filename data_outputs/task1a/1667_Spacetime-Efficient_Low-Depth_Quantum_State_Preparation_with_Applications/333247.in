{
  "texts": [
    "We illustrate the above operation as a quantum circuit in Fig. 9.",
    "If we choose m = n -⌈log 2 (n)⌉, the first operation described in Eq.",
    "( 20) can be achieved in O(log(N)) depth and O(N) spacetime allocation.",
    "Since each of the U d(SP) is using N log(N) qubits, up to O(log(N)) joint states can be parallelized in this way without introducing additional ancilla qubits.",
    "Now let's walk through the second operation described in Eq."
  ]
}