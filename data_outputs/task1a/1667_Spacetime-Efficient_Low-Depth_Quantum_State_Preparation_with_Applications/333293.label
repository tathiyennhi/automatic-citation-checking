{
  "style": "",
  "texts": [
    "Figure 18:Algorithm 7 2: 3:18723 Figure 18: Left: action of CopySwap operation, which simultaneously copies m control bits set to |k⟩ and moves a target register into the k th position of a target register.",
    "The input is m + 1 qubits and the output is 2M -1 qubits, as several fresh ancillae are introduced during the protocol.",
    "Right: implementation of CopySwap operation using copying layers and swap operations for m = 5.",
    "The total depth is m. At layer t = 0,..., m -1, there are (m + (2 t -1)(m -t) + 2 t+1) active qubits so the total spacetime allocation is m-1 t=0 (m + (2 t -1)(m -t) + 2 t+1) = O(2 m).",
    "end procedure ▷ Total D exact: O(m) ▷ Total D approx: O(m) ▷ Total SA exact: O(m2 m) ▷ Total SA approx: O(m2 m) Figure 19:19Figure 19: Top: Modular abstraction of LOADF."
  ],
  "correct_citations": []
}