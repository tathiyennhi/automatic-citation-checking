{
  "texts": [
    "Concerning the mass fractions of active and inactive agents, we instead now getd dt ρ i (t) = - C i ((1 -θ)λ A (ε -a p) + θγ λ c 2) ∫ I f (w, -γ, t)dw, d dt ρ a (t) = Ca ((1 -θ)λ A (ω p + ε -a p) -θγ λ c 2) ∫ I f (w, γ, t)dw.Therefore, in order to increase the number of active individuals while reducing the number of inactive ones, we need to impose that the coefficients C i and C a are positive, thus the parameter λ c has to satisfy 2 γ1 -θ θ λ A (a p -ε) < λ c < 2 γ 1 -θ θ λ A (ω p + ε -a p),(29)and the set of the solutions is not empty if an only ifa p ∈ (ε, ω p 2 + ε) ⊂ (ε, ω p + ε).",
    "(30)If we fix λ c = λ A to deal with a lower number of free parameters, θ must then satisfy0 < a p -ε γ 2 + a p -ε < θ < ω p + ε -a p γ 2 + ω p + ε -a p < 1,(31)and, in this case, the set of the solutions is not empty if an only ifa p ∈ (ε, min{2ε, ω p + ε}) ⊂ (ε, ω p + ε).(32)Eq.",
    "( 31) tells us that a reduction in the number of inactive agents can be achieved when a sufficiently high number of individuals follow the control strategy, and that, at the same time, if too many agents follow this strategy then also the number of active individuals would have to decrease.",
    "Moreover, it also follows that the mass fraction ρ u of undecided agents does not show a monotone behavior, since inactive agents close to A = -γ tend to become undecided (C i > 0) while undecided agents close to A = γ tend to become active (C a > 0).",
    "Special cases.We conclude the discussion of the control strategy by analyzing several special cases that we encountered."
  ]
}