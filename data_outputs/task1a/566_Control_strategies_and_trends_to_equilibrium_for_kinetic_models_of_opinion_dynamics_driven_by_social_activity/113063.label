{
  "style": "IEEE",
  "texts": [
    "Indeed, assuming for example µ ℓ = 1, for any ϵ > 0∫ 1-ϵ -1 h(t, w)dw ≤ 1 ϵ ∫ 1-ϵ -1 (1 -w)h(t, w)dw ≤ 1 -mw(t) ϵ → t→+∞ 0 since mw(t) → t→+∞ 1.In particular, it is easy to check that the mean opinion m w (t) of h(t, w) evolves according tod dt (m w (t) -µ ℓ) = -λ ℓ (ω ℓ + ε)(m w (t) -µ ℓ),(46)and thus converges exponentially to the average opinion of the leaders µ ℓ, when t → +∞.",
    "Therefore, we expect h(t, w) to relax toward the global equilibrium state (see Eq.",
    "( 24))h ∞ (w) = C ∞ (1 -w) -1+ λp(ωp+ε) 2 +λ ℓ (ω ℓ +ε) σ 2 p +σ 2 ℓ (1-µ ℓ) (1 + w) -1+ λp(ωp+ε) 2 +λ ℓ (ω ℓ +ε) σ 2 p +σ 2 ℓ (1+µ ℓ),(47)whereC ∞ > 0 is a normalization constant explicitly given by C ∞ = 1/B(b ∞ 1, b ∞ 2), where B denotes the Beta functionB(b ∞ 1, b ∞ 2) = ∫ I (1 -w) -1+b ∞ 1 (1 + w) -1+b ∞ 2 dw, of parameters b ∞ 1 and b ∞ 2 given by b ∞ 1 = λ p (ω p + ε) 2 + λ ℓ (ω ℓ + ε) σ 2 p + σ 2 ℓ (1 -µ ℓ), b ∞ 2 = λ p (ω p + ε) 2 + λ ℓ (ω ℓ + ε) σ 2 p + σ 2 ℓ (1 + µ ℓ).Notice that when opinion leaders are taken into account, the influence of the initial distribution f in (A, w) is not seen in the equilibrium h ∞ (w).",
    "Moreover, like before, similar considerations obviously hold if all agents become (in)active under the evolution of the original model.",
    "Proving the above convergence turns out to be rather problematic, due to the appearance of the time-dependent quantity m w (t) inside the Fokker-Planck equation [2]."
  ],
  "correct_citations": [
    "Proving the above convergence turns out to be rather problematic, due to the appearance of the time-dependent quantity m w (t) inside the Fokker-Planck equation [2]."
  ]
}