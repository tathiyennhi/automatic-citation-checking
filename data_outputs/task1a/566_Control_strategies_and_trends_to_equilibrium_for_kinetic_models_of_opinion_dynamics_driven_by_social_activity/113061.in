{
  "texts": [
    "If we can show that each m • (t) → t→+∞ µ ℓ, then the thesis would follow from the fact that ρ i + ρ a ≡ 1.",
    "For m i (t), since ρ i (t) is definitely constant, we find thatd dt m i (t) = 1 ρ i (t) d dt ∫ (-∞,-γ)×I wf (t, A, w)dAdw = - 1 ρ i (t) λ ℓ ε ∫ (-∞,-γ)×I (w -µ ℓ)f (t, A, w)dAdw - 1 ρ i (t) λ A (2ε -a p -a ℓ) ∫ I wf (t, A, w)| A=-γ A→-∞ dw = -λ ℓ ε(m i (t) -µ ℓ),since there are no agents with an activity level equal to -γ and due to the boundary conditions (16).",
    "Hence m i (t) → t→+∞ µ ℓ.",
    "The proof for m a (t) is similar and the thesis follows.",
    "□Corollary 2 implies that Proposition 5 holds even if we are considering the controlled model coupled with opinion leaders, since all agents tend to become active, and in this case A * c (37) would be defined asA * c = 2 1-θ θ λ A λc (ω p + ω ℓ + 2ε -a p -a ℓ).Concerning the convergence to equilibrium for the marginal distribution h(t, w) evolving under the controlled model, two situations must be distinguished."
  ]
}