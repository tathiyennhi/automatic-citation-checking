{
  "texts": [
    "Assume that unwatermarked tabular data X ∈ R N ×p has i.i.d.",
    "rows x i ∈ R p with zero mean and covariance Σ ∈ R p×p, and are Σ-sub-Gaussian, meaning there exists κ ≥ 1 such that for every u ∈ R p,∥⟨u, [29].",
    "Denote the smallest eigenvalue of Σ by λ min.",
    "Define Z(γ, δ, σ) as the standard Z-score (as in (3)) computed on the Gaussian noise-corrupted table X wm + ε, where X wm ∈ R N ×p denote the table watermarked under soft hyperparameters (γ, δ) and ε i,j i.i.d.x i ⟩∥ ψ2 ≤ κ √ u ⊤ Σu∼ N (0, σ 2).",
    "Fix any θ ∈ (0, 1) and let C 4 > 0 denote a constant such that for every real sub-Gaussian U with variance v we haveEU 4 ≤ C 4 κ 4 v 2."
  ]
}