{
  "style": "",
  "texts": [
    "The fine-tuning process in training with new tasks is repeated here and new parameters (h test) are obtained.",
    "Predictions are made using the new parameters.In our context, MAML was applied to fine-tune the CNNs used for image classification.",
    "By initializing the CNNs with parameters optimized for rapid adaptation to the task of breast cancer classification, MAML enabled our models to perform effectively even with limited training data.Fig. 2 The instances of the support set (S) and the instance x (query) are mapped into the embedding space by passing through the backbone model f h. The prototype of each class (v c) is calculated and the instance x is assigned to one of the support set prototypes according to the distance metric d Fig.",
    "3 MAML algorithm: The initial parameters of the model (h) are fine-tuned for three different tasks(h Ã 1; h Ã 2; h Ã 3) and adapted to the new tasks.",
    "Thus, the few-shot process (meta-testing) is actually imitated in the meta-training phase By incorporating these meta-learning techniques into our methodology, we effectively addressed the challenges posed by small medical datasets, ultimately leading to improved classification accuracy."
  ],
  "correct_citations": []
}