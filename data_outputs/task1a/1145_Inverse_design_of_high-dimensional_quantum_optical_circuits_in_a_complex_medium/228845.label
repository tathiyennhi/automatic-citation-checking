{
  "style": "",
  "texts": [
    "The implemented d-dimensional circuit T𝕋 is embedded in T, which lives in the n-dimensional space of the apparatus such that T𝕋 = P o TP i,",
    "where t ab is a transmission coefficient given that T𝕋 = ∑ Noting that the optical flux ℐ ab can be moved outside the bracket because the target outputs are foci that are spatially separated and conveniently detected using a coherent light source.",
    "For a twophoton entangled state, the success probability is then calculated in a similar way using the outcomes of joint measurements between Alice and Bob, the latter of which performs the measurements Π(x) ∶= η(x) |x⟩ ⟨x| across the output spatial x space:",
    "where the coincidence counts (𝒞𝒞 μ,ν=0 ab and 𝒞𝒞 μ a (x)) are calibrated by the detection efficiencies for both parties and 𝒞𝒞 μ a (x) is defined as",
    "Note that since we perform the experiment in all the input spatial-mode bases, the success probability is averaged over all the input bases as 𝒮𝒮 = ∑ d μ=0 𝒮𝒮 μ /(d + 1)."
  ],
  "correct_citations": []
}