{
  "style": "IEEE",
  "texts": [
    "In the first stage, the discrete targets are obtained by clustering on the MFCC features.",
    "In the second stage, a new generation of training targets is derived by clustering on the learned representations from the first stage model.",
    "For the pre-training of T-HuBERT, we adopt a similar two-iteration strategy.",
    "Specifically, we apply these k-means codes, obtained using the HuBERT model from the first iteration, as the targets of the top layer of the T-HuBERT model.",
    "B. GAN-based TokenizerTo leverage additional text data and improve the phonetic information of the pre-training, we utilize the GANs [17] as a bridge between unpaired speech and text, to discretize the speech waveform into phoneme-like pseudo-label sequences in an unsupervised manner."
  ],
  "correct_citations": [
    "B. GAN-based TokenizerTo leverage additional text data and improve the phonetic information of the pre-training, we utilize the GANs [17] as a bridge between unpaired speech and text, to discretize the speech waveform into phoneme-like pseudo-label sequences in an unsupervised manner."
  ]
}