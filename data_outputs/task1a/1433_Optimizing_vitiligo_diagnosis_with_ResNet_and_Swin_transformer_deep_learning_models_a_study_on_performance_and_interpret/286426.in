{
  "texts": [
    "Hence, given the category c, the result of classifier S c is: where w c k represents the weight of the model for channel k in the final dense layer corresponding to category c.",
    "It follows that w c k is important for the final class judgment, and each position element in CAM is defined in category c is:",
    "In the case where the input image is classified as target class c, the CAM identifies the significance of each location pixel (x, y) on feature map's spatial grid.",
    "Up-sampling the CAM to match the size of the original input image, the most relevant region to the target category c can be identified.",
    "the specified instructions."
  ]
}