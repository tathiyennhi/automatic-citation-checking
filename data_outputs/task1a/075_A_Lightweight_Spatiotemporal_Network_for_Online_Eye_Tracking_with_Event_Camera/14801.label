{
  "style": "IEEE",
  "texts": [
    "Furthermore, the process of binning into frames introduces an inevitable compromise in the temporal domain: too short, and the frames may not contain adequate event information; too long, and the low-latency advantage of the event-based data is lost 1.In short, spatial CNNs are not efficient for either capturing temporal correlations of events or performing online inference efficiently on streaming data, as they inherently lack temporal continuity in the network.",
    "One way to compensate for this is to add a recurrent head (e.g.",
    "ConvLSTM) to the spatial CNN backbone [11], in order to model temporal correlations by maintaining internal memories of high-level features.",
    "However, this temporal modeling only happens deep into the network, and far from the event inputs where low-level temporal details are prominent.",
    "Furthermore, this suffers from the typical difficulty of training recurrent networks in general [17].Here, we propose a spatiotemporal convolutional network that efficiently performs online inference on streaming data, applying it on the challenge of event-based eye tracking."
  ],
  "correct_citations": [
    "ConvLSTM) to the spatial CNN backbone [11], in order to model temporal correlations by maintaining internal memories of high-level features.",
    "Furthermore, this suffers from the typical difficulty of training recurrent networks in general [17].Here, we propose a spatiotemporal convolutional network that efficiently performs online inference on streaming data, applying it on the challenge of event-based eye tracking."
  ]
}