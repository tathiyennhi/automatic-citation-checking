{
  "style": "APA",
  "texts": [
    "For each input example (xi, yi), the prompt text is processed through the frozen LLM to obtain h p, which represents the hidden representation from each layer.",
    "This representation is then transformed using a trainable adapter F Φ to yield z.",
    "Subsequently, the input text xi is processed, incorporating the generated z for improved task-specific adaptation.",
    "Finally, the classification head C ζ computes the output oi for the downstream task, and the loss L (oi, yi) is computed.",
    "The trainable parameters Φ and ζ are updated iteratively to minimize the loss."
  ],
  "correct_citations": [
    "For each input example (xi, yi), the prompt text is processed through the frozen LLM to obtain h p, which represents the hidden representation from each layer."
  ]
}