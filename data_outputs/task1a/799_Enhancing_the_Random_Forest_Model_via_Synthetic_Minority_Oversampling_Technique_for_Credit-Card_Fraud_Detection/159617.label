{
  "style": "",
  "texts": [
    "We use Chi-square to test if the occurrence of a specific feat relates to the target (fraud) class using their frequency distribution.",
    "FS extracts only feats (as parameters) that highly correlate with the output class.",
    "Here, we use Python sklearn (which sets a 0 if there is no mutual information and a 1 if its perfectly correlates) a chosen feat with a target feature/class.",
    "All features are ranked by chi-squared using the threshold value as in Equation (1).𝑋 = ∑ 𝑥 𝑖 𝑛(1)A total of 22 features were extracted from the original dataset.",
    "Using the chi-square approach, we compute the threshold value using Equation (1) for each attribute to yield the scores instead of each attribute's correlation with the target class 1 (i.e., fraud) as in Table 3."
  ],
  "correct_citations": []
}