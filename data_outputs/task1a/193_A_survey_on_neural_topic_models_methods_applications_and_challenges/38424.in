{
  "texts": [
    "The variational distribution is modeled as",
    "We compute and with encoder networks parameterized by Θ: where Θ = {Θ 1, Θ 2 } and diag(⋅) denotes transforming a vector to a diagonal matrix.",
    "In practice, we transform document x into a Bag-of-Words (BoW) vector as the input and employ MLPs as encoder networks.",
    "Then to avoid gradient variance (Kingma and Welling 2014; Rezende et al 2014), we sample r through the reparameterization trick by sampling a random variable:",
    "We model the doc-topic distribution with a softmax function to restrict it on a simplex:"
  ]
}