{
  "texts": [
    "We therefore apply a logarithmic transformation to these variables prior to running the regressions (see Web Appendix Fig.",
    "A4 for histograms).",
    "Specifically, we estimate the following regression (1):where P(Correctness Model = 1) denotes the probability of a correct prediction of the model, α i (i ∈ {1, … 10}) represents the coefficients of the independent variables.",
    "To account for dataset-specific fixed effects, we categorize each dataset into one of two primary groups: either as a 'product review' (13 datasets) or as a 'Tweet' (seven datasets).",
    "We select text documents from the 'product review' category as the reference group for regression analysis and cluster standard errors on the dataset level."
  ]
}