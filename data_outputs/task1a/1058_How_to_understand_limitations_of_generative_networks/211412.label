{
  "style": "IEEE",
  "texts": [
    "For a well-motivated statistical test we can use the fact that many networks are trained on likelihood losses.",
    "Those losses include an uncertainty estimate σ i, for instance from a Bayesian regression network, so we can supplement the weight distributions by a pull and analyse both [75],w i = A i,dataA i,model, andt i = A i,model -A i,data σ i = A i,model σ i (1 -w i).",
    "(5)The pull should follow a standard Gaussian for uncorrelated stochastic deviations.",
    "The combination of weights and pulls it is extremely useful for testing regression networks, so we will try to generalize it to generative networks.",
    "Finally, we make the (obvious) observation that the AUC of a classifier can be extracted from weight samples w(x i) evaluated on training and generated configurations."
  ],
  "correct_citations": [
    "Those losses include an uncertainty estimate σ i, for instance from a Bayesian regression network, so we can supplement the weight distributions by a pull and analyse both [75],w i = A i,dataA i,model, andt i = A i,model -A i,data σ i = A i,model σ i (1 -w i)."
  ]
}