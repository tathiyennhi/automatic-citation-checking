{
  "texts": [
    "*] SQL: SELECT YEAR FROM SECTION GROUP BY YEAR ORDER BY count (*) DESC FETCH FIRST 1 ROWS ONLY Q: \" What are the names and average salaries for departments with average salary higher than 42000?\"",
    "Schema_links: [ instructor.",
    "dept_name, instructor.",
    "salary,42000] SQL: SELECT dept_name, AVG (salary) FROM instructor GROUP BY dept_name HAVING AVG (salary) > 42000 Q: \" How many rooms in each building have a capacity of over 50?\"",
    "Schema_links: [ classroom."
  ]
}