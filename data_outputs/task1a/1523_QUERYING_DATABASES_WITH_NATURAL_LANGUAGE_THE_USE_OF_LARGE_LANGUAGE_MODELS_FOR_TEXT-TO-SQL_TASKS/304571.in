{
  "texts": [
    "*] SQL: SELECT dept_name FROM course GROUP BY dept_name ORDER BY count (*) DESC FETCH FIRST 3 ROWS ONLY Q: \" Find the maximum and average capacity among rooms in each building.\"",
    "Schema_links: [ classroom.",
    "building, classroom.",
    "capacity ] SQL: SELECT max (capacity), avg (capacity), building FROM classroom GROUP BY building Q: \" Find the title of the course that is offered by more than one department.\"",
    "Schema_links: [ course."
  ]
}