{
  "texts": [
    "1) Use the database values that are explicitly mentioned in the question.",
    "2) Pay attention to the columns that are used for the JOIN by using the Foreign_keys.",
    "3) Use DESC and DISTINCT when needed.",
    "Table city, columns = [*, name, country, province, population, latitude, longitude, elevation ] Table geo_desert, columns = [*, desert, country, province ] Table geo_estuary, columns = [*, river, country, province ] Table geo_island, columns = [*, island, country, province ] Table geo_lake, columns = [*, lake, country, province ] Table geo_mountain, columns = [*, mountain, country, province ] Table geo_river, columns = [*, river, country, province ] Table geo_sea, columns = [*, sea, country, province ] Table geo_source, columns = [*, river, country, province ] Table provincelocalname, columns = [*, province, country, localname ] Table provinceothername, columns = [*, province, country, othername ] Table provpops, columns = [*, province, country, year, population ] Table airport, columns = [*, iatacode, name, country, city, province, island, latitude, longitude, elevation, gmtoffset ] Table citylocalname, columns = [*, city, country, province, localname ] Table cityothername, columns = [*, city, country, province, othername ] Table citypops, columns = [*, city, country, province, year, population ] Table located, columns = [*, city, province, country, river, lake, sea ] Table locatedon, columns = [*, city, province, country, island ] Table organization, columns = [*, abbreviation, name, city, country, province, established ] Table ismember, columns = [*, country, organization, type ]",
    "https://openai.com/research/better-language-models"
  ]
}