{
  "texts": [
    "@ > avg (department.",
    "budget) SQL: SELECT dept_name, building FROM department WHERE budget > (SELECT avg (budget) FROM department) Q: \" Find the total number of students and total number of instructors for each department.\"",
    "Schema_links: [ department.",
    "dept_name = student.",
    "dept_name, student. id, department."
  ]
}