{
  "texts": [
    "The intention is to provide the reader with a high-level understanding of the main approaches (1) to SSL that may be useful when describing specific studies in the subsequent sections.",
    "These approaches are often trialled on natural images first, likely due to the high availability of benchmark datasets and broad applicability.",
    "(θ *, φ *) = arg min θ,φ L pre (g φ (f θ (x))) Generative methodsSeveral SSL pretext tasks are built around generating samples.",
    "The output of g φ is an entire image or a fragment of an image.",
    "Note that generative SSL methods are not to be confused with general generative methods in machine learning (e.g."
  ]
}