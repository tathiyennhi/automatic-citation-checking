{
  "style": "APA",
  "texts": [
    "While training a reward model from pairwise data (Ouyang Human expert written multi-turn prompts Figure 1 Example of AdvancedIF benchmark.",
    "The example is from the capability of multi-turn carried context where prompts and rubrics written by human experts.",
    "et al., 2022) can provide a proxy for the ground truth reward, such an approach relies on a vast amount of preference data and the resulting reward signal is usually uninterpretable and prone to reward hacking (Bai et al., 2022a).",
    "To further unleash the power of RL, we propose to use rubric-based RL to improve LLMs' advanced IF capabilities during post-training.",
    "IF tasks are naturally suitable for rubric-based RL, as complex instructions usually can be decomposed into simpler ones, which can be verified relatively easily."
  ],
  "correct_citations": [
    "et al., 2022) can provide a proxy for the ground truth reward, such an approach relies on a vast amount of preference data and the resulting reward signal is usually uninterpretable and prone to reward hacking (Bai et al., 2022a)."
  ]
}