{
  "texts": [
    "The pool is decremented by the number of whale transactions in the public chain (pool ← pool -#𝑇 ì ℎ, where #𝑇 (•) is the number of whale transactions in the given chain).",
    "In addition, a difficulty contribution of ℓ is yielded.Reveal ℓ.",
    "This is only allowed when ℓ ≥ | ì 𝑎| (where |•| is the length of a vector) and either ℓ > ì ℎ (overriding the public chain)or ℓ = ì ℎ and fork = relevant (attempting rushing).",
    "In the first case, the selfish miner publishes exactly ℓ blocks, causing all honest miners to discard the public chain and prepare to mine on top of the selfish miner's revealed chain.5 Figure 1:51Figure 1: Colordag's reward mechanism and MAD-DAG's ledger function.",
    "Red blocks are destructed."
  ]
}