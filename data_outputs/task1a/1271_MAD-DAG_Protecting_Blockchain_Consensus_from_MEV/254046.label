{
  "style": "",
  "texts": [
    "The selfish miner reveals the first ℓ blocks in their secret chain.",
    "This is only allowed if ℓ ≥ ì ℎ 𝑐.If the selfish miner reveals a strictly longer chain (ℓ > ì ℎ 𝑐), those blocks are shifted to be pre-fork (𝑎 𝑑 ← 𝑎 𝑑 + ℓ, 𝑎 𝑐 ← 𝑎 𝑐ℓ), and the public chain is also shifted to be pre-fork (ℎ 𝑑 ← ℎ 𝑑 + ì ℎ 𝑐, ì ℎ 𝑐 ← []).",
    "If p = false (honest miners' blocks that are not part of the canonical chain cannot become acceptable) or if the number of pre-fork blocks (after the shift) is more than 𝑁 ℓ, then the selfish miner's pre-fork blocks are uncontested, causing p ← false and yielding a reward and difficulty contribution of 𝑎 𝑑.",
    "An additional reward based on the transactions in the revealed ℓ blocks is also yielded, and whale transactions that were in the revealed blocks are decremented from pool (pool ← pool -#𝑇 ì ℎ 𝑐 [: ℓ]).",
    "The values of 𝑐 and fork are reset to 0 and irrelevant, respectively.If the selfish miner reveals a chain equal in length to the public chain (ℓ = ì ℎ 𝑐), one of the following cases occurs."
  ],
  "correct_citations": []
}