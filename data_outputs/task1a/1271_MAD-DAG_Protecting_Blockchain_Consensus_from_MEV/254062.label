{
  "style": "",
  "texts": [
    "This is because in these cases, when an honest miner creates a block with a whale transaction, it is more profitable for the selfish miner to fight for the whale transaction by attempting to create and to publish an alternative chain with a single block holding that whale transaction.",
    "MAD-DAG's security threshold is largely unaffected by the choice of the tie-breaking rule and while it decreases for high values of 𝐹, it remains significantly higher than the alternatives: 19.9% when 𝐹 = 4 and 11% when 𝐹 = 8.",
    "This is because the previously described strategy does not work when the MAD ledger function is employed.",
    "State Space Restriction.We next analyze how the maximum fork length 𝐿 affects the security threshold (Figure 11).",
    "As before, increasing 𝐿 reduces the security threshold of all protocols, but the threshold remains virtually the same when 𝐿 ≥ 10.We also analyze how the maximum pool size max_pool affects the security threshold (Figure 12)."
  ],
  "correct_citations": []
}