{
  "style": "IEEE",
  "texts": [
    "The utility function of the agent would then be expected ratio between the total reward and the total difficulty contribution:𝑢 (𝜋) = E ∞ 𝑡 =0 𝑅 𝑡 ∞ 𝑡 =0 𝐷 𝑡.",
    "(2)While this is a non-standard utility function, Bar-Zur et al. [64] show that an MDP with such a utility function can be transformed to a stochastic shortest path MDP whose optimal policy is also with a bounded error to the optimal policy in the original MDP with the non-standard utility function.",
    "The stochastic shortest path MDP can then be solved using standard techniques.",
    "Colordag and MAD-DAG ModelWe now present models of selfish mining in Colordag and MAD-DAG.",
    "For brevity, we present the models in a unified manner, where different variants can be selected to represent the different protocols.As in previous models [8,46], we consider a single rational selfish miner and all other miners are honest."
  ],
  "correct_citations": [
    "(2)While this is a non-standard utility function, Bar-Zur et al. [64] show that an MDP with such a utility function can be transformed to a stochastic shortest path MDP whose optimal policy is also with a bounded error to the optimal policy in the original MDP with the non-standard utility function.",
    "For brevity, we present the models in a unified manner, where different variants can be selected to represent the different protocols.As in previous models [8,46], we consider a single rational selfish miner and all other miners are honest."
  ]
}