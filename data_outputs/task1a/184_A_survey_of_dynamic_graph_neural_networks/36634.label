{
  "style": "IEEE",
  "texts": [
    "It further employs self-attention mechanisms to construct a temporal point process, which enables the model to prioritize relevant information for the current task, filtering out irrelevant details.",
    "Therefore, the structural embedding for node is defined as:, where, is the embedding of the most recent node, represents the most recent time node was active, and denotes the aggregation weight corresponding to the neighbor node.",
    "Specifically, the model computes the relationship between node and its neighbor node byq t (u, i) = exp ( S t (u, i)) ∑ i ′ ∈N t (u) exp ( S t (u, i ′)),(6)S t ∈ R n×n t S A u vwhere is the attention matrix, which represents the fraction of attention between pairs of nodes at time.",
    "is computed and maintained by the adjacency matrix and the conditional intensity function, and the conditional intensity function between node and node is defined asλ t (u, v) = f ( g t(u, v)),(7)g t(u,v) g t(u, v) = w T • [ z t(u): z t(v)]: w T f (•) f (x) = ψ log ( 1 + exp ( x ψ)) x g t (•) ψ > 0where is an internal function that computes the compatibility between two recently updated nodes:, and denotes the concatenation, is the learnable scale-specific compatibility model parameter.uses the modified softplus function, where represents, is the learnable scalar time parameter corresponding to the proportion of relevant events occurring during the process.Latent Dynamic Graph (LDG) [49] is an enhanced version of DyREP [48] that integrates Neural Relational Inference (NRI) [66] to encode temporal interactions, thereby optimizing the attention mechanism and resulting in improved performance.",
    "Graph Hawkes Neural Network (GHNN) [50] and Graph Hawkes Transformer (GHT) [51] model temporal dependencies between nodes using Hawkes processes, and estimate the intensity functions using continuous-time LSTM and Transformer respectively to learn time-evolving node representations."
  ],
  "correct_citations": [
    "is computed and maintained by the adjacency matrix and the conditional intensity function, and the conditional intensity function between node and node is defined asλ t (u, v) = f ( g t(u, v)),(7)g t(u,v) g t(u, v) = w T • [ z t(u): z t(v)]: w T f (•) f (x) = ψ log ( 1 + exp ( x ψ)) x g t (•) ψ > 0where is an internal function that computes the compatibility between two recently updated nodes:, and denotes the concatenation, is the learnable scale-specific compatibility model parameter.uses the modified softplus function, where represents, is the learnable scalar time parameter corresponding to the proportion of relevant events occurring during the process.Latent Dynamic Graph (LDG) [49] is an enhanced version of DyREP [48] that integrates Neural Relational Inference (NRI) [66] to encode temporal interactions, thereby optimizing the attention mechanism and resulting in improved performance.",
    "Graph Hawkes Neural Network (GHNN) [50] and Graph Hawkes Transformer (GHT) [51] model temporal dependencies between nodes using Hawkes processes, and estimate the intensity functions using continuous-time LSTM and Transformer respectively to learn time-evolving node representations."
  ]
}