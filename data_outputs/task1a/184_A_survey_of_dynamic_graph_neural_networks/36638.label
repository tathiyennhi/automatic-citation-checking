{
  "style": "IEEE",
  "texts": [
    "By projecting, the representation of node over a certain time interval can be learned as:ẑt+∆ (u) = ( 1 + W p ∆) * z t (u),(9)W p ∆ j u t + ∆ j t + ∆where is the learned matrix.",
    "As increases, the offset of the projection embedding becomes larger.",
    "Assume that item interacts with user at time, the embedding of node at time is predicted based on a fully connected linear layer:z t+∆ ( j) = W 1 ẑt+∆ (u) + W 2 z(u) + W 3 z t+∆ (i) + W 4 z(i) + B, (10) z(u) z(i) u i W 1, W 2, W 3, W 4 Bwhere and represent the static embeddings of nodes and, respectively.",
    "and are the parameters of this linear layer.Inspired by Time2Vec [67], TGAT [2] introduces a time encoding function based on the Bochner theorem [68], which can map continuous time to a vector space.",
    "This method presents a viable substitute for position encoding within selfattention mechanisms, effectively leveraging temporal information."
  ],
  "correct_citations": [
    "and are the parameters of this linear layer.Inspired by Time2Vec [67], TGAT [2] introduces a time encoding function based on the Bochner theorem [68], which can map continuous time to a vector space."
  ]
}