{
  "style": "IEEE",
  "texts": [
    "Similarly, models like TNDCN [38] and TEDIC [39] combine GNNs with one-dimensional spatio-temporal convolutions.",
    "Integrated dynamic GNNs merge GNNs and RNNs within a√ √ √ × × Know-Evolve [47] RNN-parameterized TPP CTDG × √ × × × × DyREP [48] RNN-parameterized TPP CTDG √ √ × × × × LDG [49] RNN-parameterized TPP and self attention CTDG √ √ × × × × GHNN [50] LSTM-parameterized TPP CTDG × √ × × × × GHT [51] LSTM-parameterized TPP CTDG × √ × × × × TREND [52] Hawkes process-based GNN CTDG √ √ × × □ * × DGNN [53] GNNs and LSTM CTDG √ √ × × × f i f f f u f o f c f i f f f u f o H t-1 f c C t-1single network layer, thus amalgamating spatial and temporal modeling within the same architectural level, as shown in Fig. 3(b).",
    "GC-LSTM [40] uses the adjacency matrix of a given time as input to the LSTM and performs spectral graph convolution [28,61] on the hidden layer.",
    "The model takes a sequence of adjacency matrices as input and returns node representations that encode both temporal and spatial information.",
    "Within this framework, the GNN learns the topological features of the LSTM's cell and hidden states, which are used to maintain long-term relationships and extract input information, respectively."
  ],
  "correct_citations": [
    "Similarly, models like TNDCN [38] and TEDIC [39] combine GNNs with one-dimensional spatio-temporal convolutions.",
    "Integrated dynamic GNNs merge GNNs and RNNs within a√ √ √ × × Know-Evolve [47] RNN-parameterized TPP CTDG × √ × × × × DyREP [48] RNN-parameterized TPP CTDG √ √ × × × × LDG [49] RNN-parameterized TPP and self attention CTDG √ √ × × × × GHNN [50] LSTM-parameterized TPP CTDG × √ × × × × GHT [51] LSTM-parameterized TPP CTDG × √ × × × × TREND [52] Hawkes process-based GNN CTDG √ √ × × □ * × DGNN [53] GNNs and LSTM CTDG √ √ × × × f i f f f u f o f c f i f f f u f o H t-1 f c C t-1single network layer, thus amalgamating spatial and temporal modeling within the same architectural level, as shown in Fig. 3(b).",
    "GC-LSTM [40] uses the adjacency matrix of a given time as input to the LSTM and performs spectral graph convolution [28,61] on the hidden layer."
  ]
}