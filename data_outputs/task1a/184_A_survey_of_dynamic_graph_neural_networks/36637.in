{
  "texts": [
    "In the update operation, the embeddings of the user node and the item node are updated by two RNNs that have the same structure but different parameters:z t (u)= σ ( W u,1 z t(u) + W u,2 z t(i) + W u,3 e u,i + W u,4 ∆ u), z t (i)= σ ( W i,5 z t(i) + W i,6 z t(u) + W i,7 e u,i + W i,8 ∆ i),(8)z t(u) z t(i) u i ∆ u ∆ i e u,i u i {W u,1,..., W u,4 } {W i,5,..., W i,8 } RNN u RNN i σwhere and represent the previous embeddings of node and node respectively.",
    "and represent the time difference from the last interaction.is the feature vector corresponding to the current interaction between node and node.",
    "The sets and represent the parameters for the two RNN, and, respectively.",
    "is the sigmoid function.u tz t (u) ∆ z t (u) uThe embedding projection operation is designed to generate the future embedding trajectory of a user, and this embedding can be applied to downstream tasks.",
    "The projection operation takes two inputs: (1) the embedding of node at time, denoted as, and (2) the time difference,."
  ]
}