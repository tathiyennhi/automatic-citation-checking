{
  "style": "IEEE",
  "texts": [
    "The ker- nel of x is x (Z �) = (M-1) ⎛ ⎜ ⎜ ⎝ M �Z � � ⎞ ⎟ ⎟ ⎠ �Z � �(M-�Z � �)where the |z ′ | is the number of nonzero entries in z ′ and z ′ ⊆ x represents all z ′ vectors where the nonzero entries are a subset of the entries in x.In kernel SHAP 0 = f (h x (0)) is set as 0, and the loss func- tion of kernel SHAP becomes Kernel SHAP estimated the contribution of instance x by appropriate the x using a linear model and treating the weight of linear models as the local factor contribution i.",
    "Although the kernel SHAP can help us understand the factor contribution in each model, the value of the factor ranking in each model is different.",
    "Because the kernel SHAP is based on the (1)𝜙 i (f, x) = ∑ z � ⊆x � 𝜋 x (z �) f x (z �) -f x (z � ⧵ i) (2) L f, g, x = ∑ z � ∈Z f h x z � -g z �2x z � Fig. 1 The proposed methodology flowchart appropriate calculation theory, the kernel SHAP values of different methods will differ [73].",
    "When the SHAP method approximates a linear model, it just uses the predicted output of one model, which will affect the predicted outcome by how good the prediction model is.",
    "Meanwhile, because the model-agnostic explanation method only approximates the predicted outputs of models, the kernel SHAP values for all models have the same metric when we analyze one dataset."
  ],
  "correct_citations": [
    "Because the kernel SHAP is based on the (1)𝜙 i (f, x) = ∑ z � ⊆x � 𝜋 x (z �) f x (z �) -f x (z � ⧵ i) (2) L f, g, x = ∑ z � ∈Z f h x z � -g z �2x z � Fig. 1 The proposed methodology flowchart appropriate calculation theory, the kernel SHAP values of different methods will differ [73]."
  ]
}