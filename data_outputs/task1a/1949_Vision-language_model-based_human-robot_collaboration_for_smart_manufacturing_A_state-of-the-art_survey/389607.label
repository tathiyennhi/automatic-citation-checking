{
  "style": "APA",
  "texts": [
    "This section provides a brief introduction to the fundamental concepts and development status of LLMs and VLMs, respectively.",
    "LLMs are normally constructed on the transformer architecture (Vaswani et al., 2017), which uses a specifically designed neural network and the multi-head attention mechanism to understand context and meaning in text.",
    "The core mathematical operation in the self-attention mechanism is the computation of attention scores, which are derived from the query (Q), key (K), and value (V) vectors, as formulated in the following equation:",
    "d k where represents the dimensionality of the key vectors.",
    "The softmax function normalizes the attention scores."
  ],
  "correct_citations": [
    "LLMs are normally constructed on the transformer architecture (Vaswani et al., 2017), which uses a specifically designed neural network and the multi-head attention mechanism to understand context and meaning in text."
  ]
}