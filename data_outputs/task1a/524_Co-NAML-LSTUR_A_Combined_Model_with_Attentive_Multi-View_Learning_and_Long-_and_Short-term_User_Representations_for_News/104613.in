{
  "texts": [
    "For the i-th word in the title, the contextual representation c t i is computed as:",
    "where e t (i-k):(i+k) denotes the concatenated embeddings within a fixed-size window, and F t, b t are CNN kernel weights and biases respectively.",
    "To identify the most informative words, we apply a wordlevel attention mechanism (Wu et al. 2019d).",
    "The attention weight α t i for word i is computed as:",
    "where q t, V t, and v t are trainable parameters."
  ]
}