{
  "texts": [
    "Here P t ∈ R n×r and Q t ∈ R m×r are projection matrices, and ρ t is the optimizer such as Adam.",
    "In practice, to save memory, the projection is typically performed using only one of the two matrices, based on the smaller dimension between m and n. This approach allows for efficient gradient compression and memory savings.",
    "GaLore's theoretical foundation, including its convergence properties, is captured in the following theorem:",
    "(Convergence of GaLore with fixed projections).",
    "Suppose the gradient follows the parametric form:"
  ]
}