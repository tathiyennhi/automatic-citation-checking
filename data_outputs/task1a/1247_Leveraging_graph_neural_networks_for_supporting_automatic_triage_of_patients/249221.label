{
  "style": "APA",
  "texts": [
    "The node classification module is responsible for assigning a triage level to each new patient requiring the assignment.",
    "Both the graph representation learning module and node classification module leverage the computational intelligence of Graph Neural Networks methods.",
    "From the existing techniques for GRL, we select those that can deal with node labels based on inductive strategies.",
    "For instance, Graph Neural Networks are used for node embedding 50 because they present two main advantages: (i) they take into account data related to nodes, (i.e.",
    "node features); (ii) they are inherently inductive, so they do not need to recalculate the whole embedding in case of any graph modification (e.g."
  ],
  "correct_citations": [
    "For instance, Graph Neural Networks are used for node embedding 50 because they present two main advantages: (i) they take into account data related to nodes, (i.e."
  ]
}