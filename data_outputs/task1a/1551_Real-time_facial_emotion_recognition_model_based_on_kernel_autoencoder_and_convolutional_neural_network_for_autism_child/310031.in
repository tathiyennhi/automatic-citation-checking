{
  "texts": [
    "Applying regulations",
    "The rules in this stage explain the connection between the output and the specified input variables (AT, TTL, and P) (R).",
    "The fuzzy language rules are founded on IF-THEN statements like these:",
    "If TTL is small and P is low and AT is early THEN R is R2 If P is high THEN R is R1",
    "Crips values It will be chosen whether to delete the image or keep it for longer than was originally determined based on the fuzzy output value (R)."
  ]
}