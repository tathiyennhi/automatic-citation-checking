{
  "style": "IEEE",
  "texts": [
    "These samples are then used to improve the model [15].",
    "Other well-known semi-supervised approaches are self-learning [16], label propagation [17] and co-training [18].",
    "However, Semi-supervised learning methods still require labeled data to initialize the recognition model.",
    "Moreover, methods like active learning require bothering the users, while methods like self-learning are prone to error propagation.Another method for mitigating data scarcity is fabricating synthetic data with generative models to enhance the limited labeled data available [19].",
    "However, synthetic data are often unrealistic and do not help the model generalize.Recently, Self-Supervised Learning (SSL) has become one of the most popular methods to mitigate labeled data scarcity [6]."
  ],
  "correct_citations": [
    "These samples are then used to improve the model [15].",
    "Other well-known semi-supervised approaches are self-learning [16], label propagation [17] and co-training [18].",
    "Moreover, methods like active learning require bothering the users, while methods like self-learning are prone to error propagation.Another method for mitigating data scarcity is fabricating synthetic data with generative models to enhance the limited labeled data available [19].",
    "However, synthetic data are often unrealistic and do not help the model generalize.Recently, Self-Supervised Learning (SSL) has become one of the most popular methods to mitigate labeled data scarcity [6]."
  ]
}