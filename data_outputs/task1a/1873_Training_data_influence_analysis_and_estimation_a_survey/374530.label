{
  "style": "APA",
  "texts": [
    "Training on a single instance at a time would be far too slow (You et al., 2017; Goyal et al., 2017; Brown et al., 2020).",
    "A naive fix to Eq.",
    "(51) to support non-singleton batches assigns the same influence to all instances in the minibatch, or more formally, divide the change in loss L(z te; (t-1)) -L(z te; (t)) by batch size |B (t) | for each z i ∈ B (t).",
    "This naive approach does not differentiate those instances in batch B (t) that had positive influence on the prediction from those that made the prediction worse.",
    "Instead, Pruthi et al. (2020) estimate the contribution of each training instance within a minibatch via a first-order Taylor approximation."
  ],
  "correct_citations": [
    "Training on a single instance at a time would be far too slow (You et al., 2017; Goyal et al., 2017; Brown et al., 2020).",
    "Instead, Pruthi et al. (2020) estimate the contribution of each training instance within a minibatch via a first-order Taylor approximation."
  ]
}