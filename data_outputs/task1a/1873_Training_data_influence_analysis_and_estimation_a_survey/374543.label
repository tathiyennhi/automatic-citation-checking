{
  "style": "APA",
  "texts": [
    "For example, TracIn-Last is identical to vanilla TracIn except gradient vectors ∇ L(z i; (t-1)) and ∇ L(z te; (t-1)) only consider the model's final linear layer (Pruthi et al., 2020).",
    "This can make TracIn significantly faster at the risk of (significantly) worse accuracy (Yeh et al., 2022).",
    "TracIn for language models As a counter to the disadvantages of solely considering a model's last layer, 24 TracIn's authors subsequently proposed TracIn word embeddings (TracInWE), which targets large language models and considers only the gradients in those models' word embedding layer (Yeh et al., 2022).",
    "Since language-model word embeddings can still be very large [e.g., BERT-Base's word embedding layer has 23 M parameters (Devlin et al., 2019) ], the authors specifically use the gradients of only those tokens that appear in both training instance z i and test instance z te.",
    "Low dimensional TracIn Pruthi et al. (2020) also propose TracIn Random Projection (TracInRP)-a low-memory version of TracIn that provides unbiased estimates of ÎTracIn (i.e., an estimate of an estimate)."
  ],
  "correct_citations": [
    "For example, TracIn-Last is identical to vanilla TracIn except gradient vectors ∇ L(z i; (t-1)) and ∇ L(z te; (t-1)) only consider the model's final linear layer (Pruthi et al., 2020).",
    "This can make TracIn significantly faster at the risk of (significantly) worse accuracy (Yeh et al., 2022).",
    "TracIn for language models As a counter to the disadvantages of solely considering a model's last layer, 24 TracIn's authors subsequently proposed TracIn word embeddings (TracInWE), which targets large language models and considers only the gradients in those models' word embedding layer (Yeh et al., 2022).",
    "Since language-model word embeddings can still be very large [e.g., BERT-Base's word embedding layer has 23 M parameters (Devlin et al., 2019) ], the authors specifically use the gradients of only those tokens that appear in both training instance z i and test instance z te.",
    "Low dimensional TracIn Pruthi et al. (2020) also propose TracIn Random Projection (TracInRP)-a low-memory version of TracIn that provides unbiased estimates of ÎTracIn (i.e., an estimate of an estimate)."
  ]
}