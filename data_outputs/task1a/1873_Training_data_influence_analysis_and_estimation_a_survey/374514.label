{
  "style": "APA",
  "texts": [
    "To workaround this limitation, Yeh et al. (2018) rely on what they (later) term last layer similarity (Yeh et al., 2022).",
    "21 For classification,",
    "is |Y| dimensional, i.e., its dimension equals the number of classes.",
    "Formally, Yeh et al. (2018) partition the model parameters 𝜃 (T) = [ θ(T) θ(T) ] into two subsets, where θ(T) is the last linear (i.e., classification) layer's parameters and θ(T) ∶= 𝜃 (T) ⧵ θ(T) is all other model parameters.",
    "Since θ(T) is simply a linear function, the representer theorem analysis above still applies to it."
  ],
  "correct_citations": [
    "To workaround this limitation, Yeh et al. (2018) rely on what they (later) term last layer similarity (Yeh et al., 2022).",
    "Formally, Yeh et al. (2018) partition the model parameters 𝜃 (T) = [ θ(T) θ(T) ] into two subsets, where θ(T) is the last linear (i.e., classification) layer's parameters and θ(T) ∶= 𝜃 (T) ⧵ θ(T) is all other model parameters."
  ]
}