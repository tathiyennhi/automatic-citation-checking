{
  "style": "APA",
  "texts": [
    "Downsampling has two primary related methods.",
    "Training instance memorization also occurs in generative models where the generated outputs are (nearly) identical copies of training instances (Kandpal et al., 2022).",
    "van den Burg & Williams (2021) extend Downsampling to deep generative models-specifically, density models [e.g., variational autoencoders (Kingma & Welling, 2014; Rezende et al., 2014) ] that estimate posterior probability p(x|P,), where P denotes the training data distribution.",
    "Like Downsampling, van den Burg and Williams (2021) approach relies on training multiple submodels.",
    "12 The primary difference is that van den Burg and Williams (2021) consider generative risk Beyond that, van den Burg and Williams (2021) method is the same as Downsampling as both methods consider the LOO influence (9)."
  ],
  "correct_citations": [
    "Training instance memorization also occurs in generative models where the generated outputs are (nearly) identical copies of training instances (Kandpal et al., 2022).",
    "van den Burg & Williams (2021) extend Downsampling to deep generative models-specifically, density models [e.g., variational autoencoders (Kingma & Welling, 2014; Rezende et al., 2014) ] that estimate posterior probability p(x|P,), where P denotes the training data distribution."
  ]
}