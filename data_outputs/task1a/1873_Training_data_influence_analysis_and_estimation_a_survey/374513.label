{
  "style": "APA",
  "texts": [
    "Yeh et al. (2018) provide the intuition that a larger magnitude i indicates that training instance z i has larger influence on the final model parameters *.",
    "Following Schölkopf et al.'s (2001) representer theorem kernelized notation, the training set's group influence on test instance z te for any linear model is where kernel function K(x i, x te, i) ∶= i x ⊺ i x te returns a vector.",
    "K(x i, x te, i) | y te denotes the ker- nel value's y te -th dimension.",
    "Then, z i 's pointwise linear representer point influence on z te is Extending representer point to multilayer models Often, linear models are insufficiently expressive, with multilayer models used instead.",
    "In such cases, the representer theorem above does not directly apply."
  ],
  "correct_citations": [
    "Yeh et al. (2018) provide the intuition that a larger magnitude i indicates that training instance z i has larger influence on the final model parameters *.",
    "Following Schölkopf et al.'s (2001) representer theorem kernelized notation, the training set's group influence on test instance z te for any linear model is where kernel function K(x i, x te, i) ∶= i x ⊺ i x te returns a vector."
  ]
}