{
  "style": "",
  "texts": [
    "(57) has a closed form.",
    "However, deep neural models are not convex.",
    "Under non-convex gradient descent without momentum and with L 2 regularization, (t) ∶= (t-1) -(t) g (t-1) where gradient",
    "The exact definition of gradient g (t-1) depends on the specific contents of batch B (t) so for simplicity, we encapsulate the batch's contribution to the gradient using catch-all term ∇ L(B (t); (t-1)).",
    "(58), hypergradient h(T) i can be defined recursively as"
  ],
  "correct_citations": []
}