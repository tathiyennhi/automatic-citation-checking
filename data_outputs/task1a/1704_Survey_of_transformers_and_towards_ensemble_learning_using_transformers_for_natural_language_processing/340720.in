{
  "texts": [
    "Table 13 shows the GPU usage of each model in sentiment analysis tasks.",
    "Using the ensemble learning methodsIn this section, we propose our ensemble learning models using the existing and wellknown ensemble learning [54,55] and apply these to the natural language processing tasks.",
    "Then, we compare the results of the ensemble learning model with the single model and analyze the reasons.",
    "Using the ensemble learning method for transformersIn real life, when we use machine learning methods, we often only get a classifier that is more accurate in a single category.",
    "People want to train to obtain a classifier that is more accurate in each category, which uses an ensemble learning method."
  ]
}