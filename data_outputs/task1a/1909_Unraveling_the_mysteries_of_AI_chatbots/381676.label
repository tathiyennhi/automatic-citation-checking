{
  "style": "APA",
  "texts": [
    "The first decoder layer performs the transformation:",
    "where the matrix X = (x -n, …, x -1) contains the token symbols x in the context window of size n. GPT-3 uses n = 2048 (Brown et al. 2020).",
    "The matrix W e is the token embedding matrix, and W p is the position encoding matrix.",
    "Figure 16 annotates the dimension of each matrix in the superscript indicated next to the output of a functional block.",
    "The dimension of each token vector is d. GPT-3 uses d = 12,288 (Brown et al. 2020)."
  ],
  "correct_citations": [
    "where the matrix X = (x -n, …, x -1) contains the token symbols x in the context window of size n. GPT-3 uses n = 2048 (Brown et al. 2020).",
    "The dimension of each token vector is d. GPT-3 uses d = 12,288 (Brown et al. 2020)."
  ]
}