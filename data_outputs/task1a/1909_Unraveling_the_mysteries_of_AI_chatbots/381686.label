{
  "style": "APA",
  "texts": [
    "That is, the mask ensures that at each prediction timestep, weights from the SoftMax that correspond to future unpredicted tokens will be zero without compromising the ability to parallelize computations.",
    "The GPT-3 architecture uses more than one decoder layer.",
    "GPT-3 uses N = 96 layers, which is the same as the number of attention heads per layer (Brown et al. 2020).",
    "As illustrated in Fig. 16, the structure of each decoder in the stack is identical.",
    "Hence, the GPT-3 architecture is effectively as wide as it is deep in computational elements."
  ],
  "correct_citations": [
    "GPT-3 uses N = 96 layers, which is the same as the number of attention heads per layer (Brown et al. 2020)."
  ]
}