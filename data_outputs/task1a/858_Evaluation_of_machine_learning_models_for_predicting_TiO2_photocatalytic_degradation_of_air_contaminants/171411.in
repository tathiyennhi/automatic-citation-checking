{
  "texts": [
    "(1), the objective is to minimize the following expression:",
    "Here, sample numbers in the dataset are indicated by i, while the number of input characteristics is shown by m. The shrinkage parameter is denoted by λ, and as λ grows, the coefficients (βj) tend to approach zero.",
    "Figure 4 Illustrates the shrinkage effect and sparsity induction in feature selection using the L1 constraint of (1)",
    "Support vector machines (SVMs) are commonly used for support vector regression (SVR), which was introduced by Vapnik et al.",
    "To reduce generalization error, the SVR technique builds a model based on the training data and uses a hyperplane that maximizes the distance between labeled classes to categorize new data points."
  ]
}