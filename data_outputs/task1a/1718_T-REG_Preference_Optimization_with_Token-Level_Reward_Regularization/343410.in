{
  "texts": [
    "Therefore, we mitigate the risks of solely relying on auto-labeled Algorithm 1: Token-Level Reward Regularization (T-REG) Input: Dataset (D) with prompts and responses, policy model π θ, total number of iterations T, learning rate α t, for t = 0 to T do Sample a mini-batch of tuples (x, y w, y l) from D, Generate token-level rewards via Eq.",
    "(3), Define the regularization at token-level rewards via Eq.",
    "(1), Compute L DPO-REG via Eq.",
    "(2), Update policy parameters θ using gradient descent: θ ← θα t ∇θ(x, y w, y l, θ).",
    "end for token-level rewards, achieving a more robust and effective optimization process."
  ]
}