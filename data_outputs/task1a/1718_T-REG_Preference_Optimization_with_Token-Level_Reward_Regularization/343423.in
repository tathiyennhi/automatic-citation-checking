{
  "texts": [
    "where for each sequence, the gradient norm with respect to each token is proportional to σ (r DPO (x, y l)r DPO (x, y w)).",
    "To ensure that the regularization loss does not dominate the preference optimization loss for each sequence, we introduce a sequence weight to modulate the regularization loss.",
    "The final regularized token-level preference optimization objective is:",
    "where w (x,yw,y l) = σ (r DPO (x, y l)r DPO (x, y w)) is the sequence weight and is detached from back propagation, α is a hyperparameter that controls the strength of regularization.",
    "As shown in our ablation study (see Section 4.2), sequence weighting achieves the best performance."
  ]
}