{
  "texts": [
    "its nn-neighbor(s) actuation.",
    "Later, in section 4 we will see that the baby and giant-step may be combined with an extended detection algorithm, to provide up to four different detection mechanisms.for index i = 1 to end // i ∈ NSD do sensorval:= FindSensorVal(NSD, i, s) diffSenVal:= sensorval[i]-sensorval[i-1] NSD[i]:= AppendToRow(NSD[i], di f f SenVal) end return NSD Def LinearizeStates(s, NSD, RG): for index i = 0 to end // i ∈ Giant-Step Baby-Step Anomaly DetectorThe Giant-step Baby-step (GiBy) anomaly detector is based on the switchboard implementation, linearization step, and determination of training bounds detailed in Section 3.3.",
    "The algorithm is run for each sensor with a nn-actuator(s) dependency.",
    "We note that the GiBy algorithm is able to take as input, nn-actuator(s) with different actuation sequences.",
    "For example, the first actuator a 1 might have|k 1 | = 2,a 2 with |k 2 | = 3 and a 3 actuator with |k 3 | = 2."
  ]
}