{
  "texts": [
    "Note that the attack dataset is a mix of normaland attack records.",
    "The attack records accounted for were lessthan 6% of total data TABLE 2)2.",
    "These trained [LB,UB] bounds are used to determine the anomaly status.An example attack (testing) dataset (see first four columns) is shown in TABLE3.",
    "As seen earlier, the remaining two columns are derived.",
    "The testing switchboard state sb = 11 is mapped to matching [LB,UB] btr sb, seen in training."
  ]
}