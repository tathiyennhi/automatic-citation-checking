{
  "texts": [
    "This is inherent to the domain, and further cleaning might negatively affect the realism of the benchmark.",
    "One type of noise that",
    "Table B.5 Filtering rules.",
    "Articles must contain at least ${VALUE} tokens 16 Articles must not contain more than ${VALUE} tokens 4096 The article must have a title -The article must be at least ${VALUE} times longer than its title 3 The article must have a mean token length greater than ${VALUE} 2 The article must have a mean token length less than ${VALUE} 10 The article must have at most ${VALUE}% copyright tokens 20 could interfere with learning is the presence of stylistically unique substrings identifying publishers.",
    "For example, articles from the same publisher tend to contain similar by-lines, attribution messages, or donation requests."
  ]
}