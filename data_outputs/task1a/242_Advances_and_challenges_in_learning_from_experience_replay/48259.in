{
  "texts": [
    "N i ST } and is the representing transition of similar transitions set ST i (e.g., the first one), and Q i(k) is the Q-value corresponding to some transition τ t j, j ∈ {1,.",
    "N i ST } such that τ i(1) ∈ ST i and τ i(1)",
    "Therefore, T M can seem as a set of ST s. A single representative transition for each ST can be generated together with the prediction of their next Q-value from an explicit model of ST using the LSTM.",
    "This way, from T M, one can produce a RT M in which τ i is the transition that represents all the similar transitions so far identified in ST i, so that RT M = {[τ i ] | i = 1, 2, 3,.",
    "Unlike T M, RT M does not take care of sets of similar transitions since each τ i is unique and represents all the transitions in a given ST i."
  ]
}