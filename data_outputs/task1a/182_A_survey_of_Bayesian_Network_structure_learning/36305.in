{
  "texts": [
    "Since each arc addition increases S LL (G, D), this score will favour denser graphs.",
    "In the AIC score, the complexity penalty is just the number of free parameters in the model, F, defined as: so that",
    "The AIC score represents a rather soft penalty in terms of the number of free parameters.",
    "As a result, the AIC score tends to favour networks with a higher number of free parameters compared to BIC which is represented by where N is the sample size.",
    "Note that in BIC, and even more so AIC, the relative influence of the complexity penalty decreases as N grows, implying that increasing sample size will eventually allow LL to dominate the score."
  ]
}