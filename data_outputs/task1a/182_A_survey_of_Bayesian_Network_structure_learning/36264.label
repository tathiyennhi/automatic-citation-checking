{
  "style": "",
  "texts": [
    "It checks if X ⟂ T|MB(T)�{X} for all X ∈ MB(T).",
    "If yes, X is removed from MB(T).",
    "Having constructed the Markov Blanket of all nodes in G, GS performs the following steps:",
    "Completes the adjacency determination by removing parents of children of T in each Markov Blanket MB(T).",
    "These are identified by having the condition X ⟂ T|S for some S ⊆ MB(T)�{X}."
  ],
  "correct_citations": []
}