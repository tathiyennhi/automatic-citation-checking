{
  "texts": [
    "Support Vector Machines refer to strong supervised learning models used in classification as well as regression classification.",
    "In the case of IDS, SVM is used for classification whereby it is able to differentiate between the normal and the intrusive behavior in a network in Figure 10.",
    "Suppose we have a training data set that contain feature vectors X and the labels y, where y is -1 for normal observations and 1 for anomalous observations, SVM's goal is to use this data to find the hyperplane with the maximum margin between these two classes.",
    "The decision function of the SVM can be expressed as:The decision function of the SVM can be expressed as:",
    "w the weight vector x the feature vector b the bias term"
  ]
}