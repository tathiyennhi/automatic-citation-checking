{
  "texts": [
    "The high accentuation and the clearly marked individual sections stress to which extent the Decision Tree separates the feature space associated with a feature according to specific threshold values.",
    "This segmentation can also reveal non-linear patterns in the data and requires much fewer instances to learn from and can also over-fit the model more easily, especially when the dataset contains noise or consists of classes that have a significant overlap.",
    "The visualization presented here shows that the model is capable of seemingly separating the two classes and yet suggests possible failure in distinguishing points on the boundary between the two classes which are usually hard to predict.",
    "Hidden Layers: There can be one or more hidden layers, each with a variable number of neurons.",
    "Let's denote the number of neurons in the i-th hidden layer as hi."
  ]
}