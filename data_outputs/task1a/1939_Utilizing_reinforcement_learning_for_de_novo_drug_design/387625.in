{
  "texts": [
    "This yields more stable learning in terms of the validity of generated SMILES strings and biases the learning towards",
    "what the pre-trained model knows.",
    "It could possibly be useful to use two critic networks if their updates utilize different replay experiences.",
    "For regularizing the actor loss, we include the Kullback-Leibler (KL) divergence term between the (current) actor and the average policy network.",
    "The average policy network ′ is initialized as the actor and whose parameters ′ is updated using moving average (as in Eq."
  ]
}