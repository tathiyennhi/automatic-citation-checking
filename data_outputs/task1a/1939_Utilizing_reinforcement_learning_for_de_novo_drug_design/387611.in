{
  "texts": [
    "is a hyperparameter (Blaschke et al., 2020) that determines the importance of the reward signal.",
    "Note that it uses a margin guard, which resets the agent to the prior and adjusts sigma if the margin between the augmented likelihood and the agent likelihood log prior (a 1∶T) + R(a 1∶T)log (a 1∶T) is below a pre-defined threshold.",
    "Table 1 in Appendix A displays a list of hyperparameters and the values employed in this paper.",
    "Fig. 2 Taxonomy of the reinforcement learning (RL) algorithms explored in this work 1 3",
    "Proximal Policy Optimization (PPO) (Schulman et al., 2017) uses a clipping loss function defined by where r = (a t |s t)"
  ]
}