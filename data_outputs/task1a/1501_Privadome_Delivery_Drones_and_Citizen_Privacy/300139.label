{
  "style": "IEEE",
  "texts": [
    "The square root appears as the final step in the calculation of the product of the L2-norms | -→ D |×| -→ C |.",
    "Each of these operations is expensive in MPC.",
    "We therefore modify the algorithm to omit the costly operations.",
    "The algorithm computes the value -→ D • -→ C. It omits the square-root step in the computation of the norms within the MPC algorithm, leaving us with the values | -→ D | 2 and | -→ C | 2 Figure 3:3Figure3: Per-query mobile data usage (MBs) on citizen's phone.",
    "The average power draw was ∼0.96mAh per MB of data consumed, as measured with the Battery Historian tool[48]."
  ],
  "correct_citations": [
    "The average power draw was ∼0.96mAh per MB of data consumed, as measured with the Battery Historian tool[48]."
  ]
}