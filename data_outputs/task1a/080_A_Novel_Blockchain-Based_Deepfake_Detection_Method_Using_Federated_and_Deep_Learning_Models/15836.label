{
  "style": "IEEE",
  "texts": [
    "In addition, Eq.",
    "( 2) can be used to calculate the prediction vector: If detected deepfake is true Save and continue Send to global model Else Save and send to the global model Break End for Sending data to the sources End for End while Update global model Getting back results from the edge and cloud platform Update system and network status Evaluate accuracy and AUC EndWith c i,j as a coupling coefficient, the next higher-level capsule, s u, processes the sum of predictions from all lower- level capsules.",
    "So capsules s u can indeed be written as wherein c i,j is a softmax routing function with the following formula: Also, the parameter c is an important one.",
    "The output probabilities are scaled between 0 and 1 using Z squashing function, which can be written as(3) s u = Σ c i,j v i|j (4) c i,j = q i,j ∕Σqi, jSimilar to softmax, C is an array that may be chosen by consensus via dynamic routing [51].",
    "The basic principle behind this method is that the distribution of the low-level capsule's output to the high-level capsule is progressively modified based on the high-level capsule's output over several rounds until an optimal distribution is reached [52]."
  ],
  "correct_citations": [
    "The output probabilities are scaled between 0 and 1 using Z squashing function, which can be written as(3) s u = Σ c i,j v i|j (4) c i,j = q i,j ∕Σqi, jSimilar to softmax, C is an array that may be chosen by consensus via dynamic routing [51].",
    "The basic principle behind this method is that the distribution of the low-level capsule's output to the high-level capsule is progressively modified based on the high-level capsule's output over several rounds until an optimal distribution is reached [52]."
  ]
}