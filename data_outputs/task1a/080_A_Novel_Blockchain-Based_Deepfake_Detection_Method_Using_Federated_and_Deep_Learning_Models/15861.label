{
  "style": "",
  "texts": [
    "The time cost function is defined as follows:With time slot t, the learned model accuracy loss is deter- mined asThe aggregated model, on the other hand, is t, and the loss function is L().",
    "The images for the training data from source i are d i = ( x j, y j).",
    "For each source in our system, the quality of the trained model is evaluated.",
    "The overall cost of FL with a time slot is also calculated as follows:(13) c(i) = f i, q i.",
    "S m i (t) (14) c c (i) = f i, i.t = i t (15) c time (t) = max (c(i) + c c (i)) (16) c time (t) = 1 |S p | Σ(c(i) + c c (i)) (17) c q (t) = Σσ t, d iSo we have carefully defined a set of cost metrics."
  ],
  "correct_citations": []
}