{
  "style": "IEEE",
  "texts": [
    "and 𝑦 2 represent the Has_Answer label and LLM_Prefer label respectively.",
    "The loss function is denoted by 𝑙 (•, •), and we use binary cross-entropy loss as the loss function, that is 𝑙 (ℎ(𝑥, 𝜃), 𝑦) = -2 𝑖=1 𝑦 𝑖 log(ℎ 𝑖 (𝑥, 𝜃)) + (1 -𝑦 𝑖) log(1 -ℎ 𝑖 (𝑥, 𝜃)).",
    "We learn the bi-label scorer by optimizing the following learning objective.",
    "min 𝜃 𝐿(𝜃, 𝐷) = 1 |𝐷 | ∑︁ (𝑥,𝑦) ∈𝐷 𝑙 (ℎ(𝑥, 𝜃), 𝑦)To learn the bi-label document scorer, we collect the training set 𝐷 and build the classification model ℎ as follows.The process of training data annotation and model fine-tuning is demonstrated in Figure 3.The training set consists of the Top-50 documents for each question that are retrieved from the corpus using Contriever [13].",
    "They are annotated with the following principles."
  ],
  "correct_citations": [
    "min 𝜃 𝐿(𝜃, 𝐷) = 1 |𝐷 | ∑︁ (𝑥,𝑦) ∈𝐷 𝑙 (ℎ(𝑥, 𝜃), 𝑦)To learn the bi-label document scorer, we collect the training set 𝐷 and build the classification model ℎ as follows.The process of training data annotation and model fine-tuning is demonstrated in Figure 3.The training set consists of the Top-50 documents for each question that are retrieved from the corpus using Contriever [13]."
  ]
}