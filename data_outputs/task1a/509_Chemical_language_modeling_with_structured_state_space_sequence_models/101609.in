{
  "texts": [
    "This convolutional representation reveals a key aspect of state space models: they The global convolution filter K is parameterized via the matrices A, B, and C (Eq. (2)).",
    "During the generation, S4 switches to the recurrent formulation (via the same parameters, (Eq.",
    "(1)) and produces SMILES strings element-by-element for more efficient and effective chemical space exploration.",
    "c Computational pipeline, where S4 was used to learn from known SMILES strings and generate new molecules de novo.",
    "learn explicitly from the entire sequence (via global convolution) while preserving recurrent generation capabilities (Fig. 1b)."
  ]
}