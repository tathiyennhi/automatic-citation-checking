{
  "style": "IEEE",
  "texts": [
    "The recording ensures that the message structure and order are valid while mutational fuzzing will slightly corrupt both [36].",
    "In fact, all recently proposed protocol fuzzers, such as AFLNET [36] and NSFUZZ [38] follow this approach.Challenges.",
    "However, as a state-of-the-art (SOTA) approach, mutation-based protocol fuzzing still faces several challenges:INIT PLAY Setup/ Announce Describe/ Options/ Teardown Teardown Setup/Options/ SetParameter/GetParameter Play Pause Play/Setup/Options/ GetParameter/ SetParameter Pause Record /Setup/Options/ SetParameter/GetParameter Record Teardown Teardown RECORD INIT READY Fig. 2.",
    "The state machine for the RTSP protocol from RFC 2326.• (C1) Dependence on initial seeds.",
    "The effectiveness of mutation-based protocol fuzzers is severely limited by the provided initial seed inputs."
  ],
  "correct_citations": [
    "The recording ensures that the message structure and order are valid while mutational fuzzing will slightly corrupt both [36].",
    "In fact, all recently proposed protocol fuzzers, such as AFLNET [36] and NSFUZZ [38] follow this approach.Challenges."
  ]
}