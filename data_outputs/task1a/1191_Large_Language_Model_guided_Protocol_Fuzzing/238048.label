{
  "style": "",
  "texts": [
    "We quantify this duration based on the number of uninteresting seeds continuously generated by the fuzzer.",
    "Specifically, throughout the fuzzing campaign, we maintain a global variable called PlateauLen to keep track of the number of uninteresting seeds continuously observed thus far.",
    "Before commencing the fuzzing campaign, PlateauLen is initialized to 0 (Line 4 of Algorithm 1).",
    "During each fuzzing iteration,In the [protocol-name] protocol, the communication history between the [protocol-name] client and the [protocol-name] server is as follows: Communication history: ``[ Put the communication history here] ``` The next client request that can induce the server's state transition to other states is: Desired format of one real client request: ``[ Put one real message example from the initial seed corpus here] ```P rompt Template Fig. 9.",
    "The prompt template for obtaining the next client request that can induce the server's state transition to other states.PlateauLen is reset to 0 if we encounter a seed that crashes the program (line 26) or when the coverage increases (line 30)."
  ],
  "correct_citations": []
}