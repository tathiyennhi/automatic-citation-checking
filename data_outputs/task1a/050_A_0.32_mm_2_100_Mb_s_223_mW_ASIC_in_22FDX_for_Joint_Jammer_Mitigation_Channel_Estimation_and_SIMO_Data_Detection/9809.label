{
  "style": "IEEE",
  "texts": [
    "The jth PE of the ith slice stores the jth row of Y i, which enables the application of Cannon's algorithm [8] (cf.",
    "Fig. 3 product Ev on line 6b is computed by following the same procedure, which takes 13 clock cycles.",
    "The matrix-vector products involving E H on lines 6a and 8a are each computed after 10 clock cycles by using a variant of Cannon's algorithm (cf.",
    "Fig. 3(b)), which avoids rewriting memory to transpose E.To complete line 4a, we need the reciprocal value of the 32-dimensional inner product ∥s∥ 2 2 = sH s. The multipliers of the 32 PEs compute the partial products s * i si, which are then summed by reconfiguring the PEs' adders into an adder tree with five pipelined stages.",
    "The resulting ∥s∥ 2 2 is confined to the interval [4,32], and hence its inverse is simply computed via a look-up table (LUT)."
  ],
  "correct_citations": [
    "The jth PE of the ith slice stores the jth row of Y i, which enables the application of Cannon's algorithm [8] (cf.",
    "The resulting ∥s∥ 2 2 is confined to the interval [4,32], and hence its inverse is simply computed via a look-up table (LUT)."
  ]
}