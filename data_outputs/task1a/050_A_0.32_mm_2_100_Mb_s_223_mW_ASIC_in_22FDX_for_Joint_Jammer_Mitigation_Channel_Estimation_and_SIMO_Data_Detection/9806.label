{
  "style": "IEEE",
  "texts": [
    "Furthermore, while the original MAED algorithm [3] applies the Barzilai-Borwein method to determine the step sizes τ (t), we empirically tune these step sizes to powers of two.",
    "Doing so saves the computations of the Barzilai-Borwein method and simplifies multiplication by τ (t) into a simple arithmetic shift.",
    "+ × + × + × + × + × + × + × + × + × + × + × + × + × + × + × nextclockcycle nextclockcycle (a) Ax × × × × × × × × × + + + + + + + + + + + + + + + nextclockcycle nextclockcycle (b) A H z Fig. 3.",
    "Cannon's algorithm and its Hermitian variant illustrated for a 3 × 3 matrix-vector product: (a) To compute Ax, the jth PE stores a j, the jth row of matrix A.",
    "Each PE starts with x k and multiplies it by a j,k."
  ],
  "correct_citations": [
    "Furthermore, while the original MAED algorithm [3] applies the Barzilai-Borwein method to determine the step sizes τ (t), we empirically tune these step sizes to powers of two."
  ]
}