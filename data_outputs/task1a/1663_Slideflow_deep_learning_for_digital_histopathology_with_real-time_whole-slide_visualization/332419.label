{
  "style": "IEEE",
  "texts": [
    "Multi-modal models can also be trained with additional arbitrary input, such as clinical variables, using late multimodal fusion via concatenation at the post-convolutional layer.",
    "Hyperparameters can be optionally tuned using either grid-search sweeps or Bayesian hyperparameter optimization, which uses the SMAC3 [41] package.",
    "Hyperparameter search spaces can be easily customized, and several preconfigured search spaces are provided.",
    "In this weakly-supervised approach, models are trained using ground-truth labels for each tile determined from the label of the corresponding slide.",
    "b When evaluating a tile-based model, image tiles do not need to be buffered into TFRecords."
  ],
  "correct_citations": [
    "Hyperparameters can be optionally tuned using either grid-search sweeps or Bayesian hyperparameter optimization, which uses the SMAC3 [41] package."
  ]
}