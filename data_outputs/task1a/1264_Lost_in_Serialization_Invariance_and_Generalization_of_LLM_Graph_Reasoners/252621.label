{
  "style": "",
  "texts": [
    "We hypothesize this facilitates retrieval to the model.",
    "Shuffling edge-list break this locality: retrieving a node's neighborhood requires a scan of the entire list, or a first internal reordering to allow linear scans-both can introduce errors.",
    "As for adjacency-lists, locality remains preserved under all of its symmetries, indeed, shuffles hit comparatively mildly.Last, we replicate undirected edges in edge-lists enforcing full symmetric locality: when edges are not replicated in a sorted listing, many neighborhoods are contiguous, but some (e.g., node n) may be not.",
    "gaps are reported in fig. 3c; again G1's are most brittle and edge-counting tasks are hit hardest.",
    "This suggests, while replication resolves endpoint symmetries and aligns with edge extraction, it also inflates the sequence and can dilute signal.Sensitivity to syntax."
  ],
  "correct_citations": []
}