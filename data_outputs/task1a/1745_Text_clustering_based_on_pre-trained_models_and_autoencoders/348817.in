{
  "texts": [
    "The decoder maps the encoding representation from the latent space back to the reconstructed data, aiming to restore the original input as closely as possible.",
    "The structure is depicted above.",
    "DEC (Deep Embedded Clustering) is a clustering algorithm based on autoencoders (Xie et al., 2016).",
    "It iteratively learns data vector representations and performs clustering tasks.",
    "Assuming we have a dataset with n data points {x 1, x 2,."
  ]
}