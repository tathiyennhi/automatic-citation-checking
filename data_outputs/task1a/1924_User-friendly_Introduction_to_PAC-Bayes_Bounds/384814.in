{
  "texts": [
    "Let X = (X t) t∈Z be a real-valued stationary time series.",
    "Define, for θ = (θ 1, θ 2) ∈ R 2, ℓ t (θ) = (X t -θ 1 -θ 2 X t-1) 2 and R(θ) = E X [ℓ t (θ)] (it does not depend on t as the series is stationary).",
    "the empirical risk based on the observation of (X 0,.",
    "Assume the prior π is chosen such that ∥θ∥ 6 π(dθ) ≤ M 6 < +∞ (for example a Gaussian prior).",
    "Assume that X the α-mixing coefficients of X satisfy:"
  ]
}