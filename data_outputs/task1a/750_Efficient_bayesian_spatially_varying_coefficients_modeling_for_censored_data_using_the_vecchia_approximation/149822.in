{
  "texts": [
    "where v(i) denotes the conditioning set for observation i, and the censored indices I c correspond to values below the detection limit L.",
    "We apply the Vecchia approximation directly to the observed vector Z, by replacing each conditioning set v(i) with a reduced neighbor set ṽ(i), constrained to include only previously ordered non-censored observations.",
    "That is, for each i, we require:",
    "This yields the approximate likelihood:",
    "Each term in this expression corresponds to a Gaussian or truncated Gaussian:"
  ]
}