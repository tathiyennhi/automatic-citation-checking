{
  "texts": [
    "These problems include insufficient integrity of data set, high dimension of data, noise and poor reliability of data.",
    "The running speed is Fig. 6 | A basic variogram auto-encoder (VGAE).",
    "The input graph's adjacency matrix A and the node's identity matrix X are used to learn the mean μ and variance σ of the node's low-dimensional vector representation through an encoder (graph convolutional network), which is then used to generate the graph with a decoder (link prediction).",
    "slow, and the hardware equipment needed to improve the computing power is expensive; The model itself also has limitations, such as poor parallel ability of the algorithm, poor interpretability, long parameter optimization time, etc., and poor generalization ability of the model.",
    "A large amount of data is needed to achieve the best effect of the model."
  ]
}