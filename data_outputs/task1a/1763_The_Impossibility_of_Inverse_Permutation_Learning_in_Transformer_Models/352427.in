{
  "texts": [
    "The model is given a permutation and a permuted sequence as input, and is tasked with inverting the permutation to recover the canonical ordering.",
    "( a)aWeights for first attention layer A(1) (b) Weights for second attention layer A(2) (c) Weights for output layer W Figure 2:2Figure 2: Weights for decoder-only disentangled transformer trained to inverse permutations.",
    "1)1X ⊤)X = P I d 0 1/(2d)11 ⊤ (P + Y P)...... Y P 0 I d P...... 1)1[0 1×d; P] P 0 d ( 1)1A (2) h (1)⊤)) = Our code is available here: https://github.com/johnchrishays/icl.",
    "Perhaps the closest related work is that of Abbe et al. [1], who propose the notion of an \"agnostic\" (i.e., unsupervised) scratchpad.",
    "In this setting, the model may first produce a fixed number of output tokens which do not appear in the training loss for an otherwise standard supervised learning problem."
  ]
}