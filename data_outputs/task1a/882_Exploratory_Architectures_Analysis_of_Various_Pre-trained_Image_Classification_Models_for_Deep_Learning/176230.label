{
  "style": "IEEE",
  "texts": [
    "The proposed work shows that by increasing the width of the residual blocks in a deep network, they could achieve better, on the ImageNet dataset while using fewer parameters than previous methods.",
    "Experimental results shows that the WRN architecture is robust to solve overfitting problem and can simplify better for new datasets.The ImageNet Large Scale Visual Recognition is a competition for object detection and classification tasks using deep neural networks [32].",
    "The dataset consists of over twelve lakh high-resolution images and covers thousand object categories are used to analysis and solve the challenges.",
    "It also evaluated and compared the performance of different deep neural network architectures on the dataset.Aggregated Residual Transformations (ART) is new deep neural network architecture, which aims to improve the performance of deep neural networks with reduced computational cost [33].",
    "It is achieved by using connections and aggregating the predictions of multiple ART modules within the same network."
  ],
  "correct_citations": [
    "Experimental results shows that the WRN architecture is robust to solve overfitting problem and can simplify better for new datasets.The ImageNet Large Scale Visual Recognition is a competition for object detection and classification tasks using deep neural networks [32].",
    "It also evaluated and compared the performance of different deep neural network architectures on the dataset.Aggregated Residual Transformations (ART) is new deep neural network architecture, which aims to improve the performance of deep neural networks with reduced computational cost [33]."
  ]
}