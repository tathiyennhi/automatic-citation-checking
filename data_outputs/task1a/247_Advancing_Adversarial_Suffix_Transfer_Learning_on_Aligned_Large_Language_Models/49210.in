{
  "texts": [
    "To optimize the adversarial suffixes using multiple malicious prompts {X (j) }, the aggregated gradientj ∇ es i L(X (j), S) and the aggregated Algorithm 1 i-DeGCG Algorithm Input: Initial suffix S 0, behavior set {X (j) }, iterations T, batch size B, FTS threshold ϵ 1, CAS threshold ϵ 2, stage flag f ∈ {0, 1}, maximum steps T f for one stage 1: ▷ Initialize behavior set and accumulated step 2: m j ← 1, t ac ← 0 3: for t = 1, 2,..., T do 4:",
    "▷ Construct suffix batch under specific loss 5:",
    "▷ Update stage flag 13:",
    "ac ← 0 15: else 16: t ac ← t ac + 1 17: end if 18: ▷ Update behavior set 19:",
    "end if 22: end for Output: adversarial suffix S T loss j L(X (j), S) are used instead to construct candidate batches and select candidate suffixes."
  ]
}