{
  "texts": [
    "(6), the weights A ij + B ij t capture the network structure, while the function g represents the underlying exposure mechanism.",
    "Considering Eq.",
    "(6), CMP explicitly models how treatments trigger links.",
    "Note that the term",
    "describes the contribution of unit j to the outcome of unit i in round t. When unit j is treated in round t, the functionality of the link is shifted by replacing the baseline contribution ("
  ]
}