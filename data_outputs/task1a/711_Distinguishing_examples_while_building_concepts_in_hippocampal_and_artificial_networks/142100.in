{
  "texts": [
    "For example, we use 40, 100, and 200 epochs respectively for digit classification and set identification with 10 and 50 sets.",
    "In contrast to DeCorr, the DeCov loss function formulated to reduce overfitting is",
    "where mean activations are now taken over batch items:",
    "Multitask learning.",
    "We train a fully-connected two-layer perceptron with a hyperbolic tangent (tanh) activation function applied to each hidden layer."
  ]
}