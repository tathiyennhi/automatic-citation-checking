{
  "style": "",
  "texts": [
    "For the adjacency matrix A, where A uv = 1 if node u is connected to node v, the eigenvector centrality for node u is:",
    "where, λ is a unique positive eigenvalue.",
    "According to closeness centrality, a node is crucial if it has small, shortest-path lengths to all other nodes.",
    "The centrality closeness of the node u, C c (u), is defined as:",
    "where, N is the set of nodes in the network and d (u, v) is the shortest-path length between u and v."
  ],
  "correct_citations": []
}