{
  "texts": [
    "Definition 2 (Consistency).",
    "Given a categorical distribution p τ (y|x) defined as in Equation 1 and two samples x, x ′ ∈ D y, the pair-wise consistency of a classifier is measured as",
    "whereas the expected consistency is",
    "Intuitively, a consistent LLM produces similar distributions p τ (•|x) regardless of the sample x of class y.",
    "When sensitivity is not 0, being consistent suggests that prompt rephrasings cause similar mistakes across all samples of class y, hence a careful tuning of the prompt is required."
  ]
}