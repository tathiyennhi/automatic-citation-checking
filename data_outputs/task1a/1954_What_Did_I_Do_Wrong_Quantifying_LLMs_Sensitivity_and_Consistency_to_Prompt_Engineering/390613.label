{
  "style": "",
  "texts": [
    "samples D = (x i, y i) i∈{1,...,N }, with x being the input text to classify and y ∈ {1,.",
    ", C} the ground truth label.",
    "Moreover, we refer to the subset of samples of class y as D y.",
    "We make the typical conditional independence assumption about the data, that is, p(y, x) = p(y|x)p(x).",
    "Without loss of generality, we introduce the distribution p ρ 0 (ρ) defined over all prompts that are semantically equivalent to a reference prompt ρ 0, which is typically prepared by the user based on task τ; when clear from the context, we will omit the subscript from the notation."
  ],
  "correct_citations": []
}