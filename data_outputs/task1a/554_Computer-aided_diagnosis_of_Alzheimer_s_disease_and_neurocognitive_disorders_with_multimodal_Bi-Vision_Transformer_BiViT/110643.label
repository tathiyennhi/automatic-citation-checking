{
  "style": "",
  "texts": [
    "After adding, we apply the floor function to the resulting vector values to convert them into integers within the domain Z.",
    "The resulting output, obtained after applying stochastic depth, is illustrated in Eq. 13.The dense transformation and concatenation operations, represented by, are applied on the output of self-attention (in case of patching), as described in Eq.",
    "14.For tokenization, the same transformations are applied to the output of self-attention, as depicted in Eq. 15.Finally, attention weights are computed, and the representations and Ω are multiplied by their respective weight matrices (as depicted in Eqs.",
    "16 and 17).",
    "( 10)Attention(Z t, Z t, Z p) = Softmax � Z t Z T t √ D h � ⋅ Z p (11) = 1 - (12) U = + U Θ ∈ Z (13) D sd = U.Softmax � Ψ √ D h � ⋅ Z p. (14) = � Softmax( Φ √ D h) ⋅ Z t � (15) Ω = Ω � U.Softmax( Ψ √ D h) ⋅ Z p. � (16) w = (. 1) T i.In Eq."
  ],
  "correct_citations": []
}