{
  "style": "",
  "texts": [
    "The mathematical formulations for the self-attention mechanism applied to Z p and Z t can be found in Eqs.",
    "9 and 10.The matrix product Z p Z T p is replaced with the covariance matrix, represented by.",
    "To normalize the dot product ( 6)Z p = [X class;x �� 1 n E;x �� 2 n E....x �� N n E] + E pos (7) F n = MaxPool(ReLU(Conv2d(X �� n))) (8) Z t = [x class; F �� 1 n E; F �� 2 n E....F �� N n E] + E pos (9) Attention(Z p, Z p, Z t) = Softmax � Z p Z T p √ D h � ⋅ Z tattention scores in self-attention, √ D h is used as a scaling factor.",
    "This helps to sustain gradient stability during training.The matrix product Z t Z T t is replaced with the covariance matrix, represented by Ψ.Stochastic depth is another idea derived from CCT.",
    "Stochastic depth refers to an approach for randomly skipping or dropping network layers during training for the CCT."
  ],
  "correct_citations": []
}