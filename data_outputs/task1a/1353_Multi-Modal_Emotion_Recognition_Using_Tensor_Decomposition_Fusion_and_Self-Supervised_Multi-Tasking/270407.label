{
  "style": "IEEE",
  "texts": [
    "It extracts key facial expression features by analyzing movements of facial muscles.",
    "Finally, to capture the continuity of facial expressions across frames, we employed the SLSTM model.",
    "SLSTM, by temporally modeling facial expressions in consecutive frames, effectively recognizes and understands the dynamic changes in facial expressions.",
    "Tensor fusionIn multimodal tasks, the Tensor Fusion Network [18] excels at modeling the interactions between three distinct modalities, accounting for intermodal dynamics.",
    "To facilitate this, a third-order Cartesian product is employed, formally defined as:z m = f ′ a ⊗ f ′ t ⊗ f ′ v (1)where f ′ a, f ′ t, and f ′ v are the augmented dimensionality representations of the extracted features f a, f t, and f v, respectively, and ⊗ denotes the outer product operation."
  ],
  "correct_citations": [
    "Tensor fusionIn multimodal tasks, the Tensor Fusion Network [18] excels at modeling the interactions between three distinct modalities, accounting for intermodal dynamics."
  ]
}