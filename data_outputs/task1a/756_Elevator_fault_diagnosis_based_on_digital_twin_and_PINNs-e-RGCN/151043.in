{
  "texts": [
    "In the kth iteration, find the step size α k that meets the following conditions function value reduction condition:",
    "Strong curvature condition:",
    "where p k is the search direction, c1 and c1(0 < c1 < c2 < 1) are constants for the strong Wolfe condition.",
    "If no α k is found to satisfy the equation start of the kth, the optimization is stopped.",
    "In order to restart the optimization process, the β constant is introduced so that the amplification factor is slightly greater than zero, given by the following Eq."
  ]
}