{
  "style": "IEEE",
  "texts": [
    "We restrict SA to these foreground positions and cluster its rows into N components (e.g., K-means on SA features, optionally concatenated with normalized coordinates (x, y) ∈ [-1, 1] 2 for spatial coherence).",
    "With one-hot assignments K ∈ {0, 1} F ×N (no gradient through K), the resulting instance masks areM = SA t [I, I] stopgrad(K) ∈ [0, 1] F ×N.",
    "(5)This yields instance masks that highlight pixels attending strongly within the same cluster and weakly outside, leading to sharper instance boundaries.",
    "Further details of the instance clustering procedure are provided in Appendix A.Repel guidance between instance masks From Eq.",
    "( 5) we obtain instance masks M [1],..., M [N] over foreground pixels."
  ],
  "correct_citations": [
    "With one-hot assignments K ∈ {0, 1} F ×N (no gradient through K), the resulting instance masks areM = SA t [I, I] stopgrad(K) ∈ [0, 1] F ×N.",
    "( 5) we obtain instance masks M [1],..., M [N] over foreground pixels."
  ]
}