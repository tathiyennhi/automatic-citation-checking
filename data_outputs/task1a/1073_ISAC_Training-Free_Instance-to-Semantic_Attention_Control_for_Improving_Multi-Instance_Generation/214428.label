{
  "style": "IEEE",
  "texts": [
    "In contrast, excessively large values of η cause latent collapse and noticeably degrade image quality.",
    "This trade-off is consistent with other latent-optimization methods [10], and ISAC is subject to the same limitation.",
    "For all cases, we provide \"A photo of two cats and a dog\" as the input prompt and use SD1.5 [1] as the baseline diffusion model.Empirically, we find that a step size of η = 10 -2 achieves stable improvement without visible artifacts across backbones and datasets (Fig. 8).",
    "The gradient-descent update Xt ← X t -η∇ Xt L t (X t) in Algorithm 1 is applied once per diffusion timestep, and η is the The image is generated with the prompt, \"a photo of two donuts\" on SD3.5-M [5].",
    "We averaged all accumulated self/cross-attention maps over timesteps into a single (SA, CA) pair."
  ],
  "correct_citations": [
    "This trade-off is consistent with other latent-optimization methods [10], and ISAC is subject to the same limitation.",
    "For all cases, we provide \"A photo of two cats and a dog\" as the input prompt and use SD1.5 [1] as the baseline diffusion model.Empirically, we find that a step size of η = 10 -2 achieves stable improvement without visible artifacts across backbones and datasets (Fig. 8).",
    "The gradient-descent update Xt ← X t -η∇ Xt L t (X t) in Algorithm 1 is applied once per diffusion timestep, and η is the The image is generated with the prompt, \"a photo of two donuts\" on SD3.5-M [5]."
  ]
}