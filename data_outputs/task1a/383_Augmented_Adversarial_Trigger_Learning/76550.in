{
  "texts": [
    "Let q be a distribution over ξ = (x, y, g) that is a uniform mixture of individual group distributions P g:",
    "We verify that assumptions in the theorem hold under the original assumptions in the lemma.",
    "We firstly show that the expected value of F g (θ) over distribution q is f g (θ), and then compute an unbiased stochastic subgradient ∇F g ′ (θ; (x, y, g))",
    "Recall that in the original assumptions, we have (1) ℓ(θ; (x, y)) ≤ B ℓ for all (x, y, θ) (2) ∥∇ℓ(θ; (x, y))∥ 2 ≤ B ∇ for all θ, x, y, and finally ∥θ∥ 2 ≤ B Θ for all θ ∈ Θ, we obtain:",
    "Plugging in these constants into the regret bound, we prove"
  ]
}