{
  "texts": [
    "During inference, the model performs sampling based generation.",
    "The attack will fail if the second largest generation, which is evasive, is sampled during inference.",
    "Both the affirmative response and the evasive response are highlighted in red.",
    "The weights w are calculated as:",
    "The above objective function is associated with two hyperparameters β and α. β controls the trade-off between the elicitation function and the suppression function, and α determines the contrast between the format-related tokens and question-related tokens."
  ]
}