{
  "texts": [
    "Formally, it can be described as follows:",
    "where F denotes the prediction function, x the input vector of the example under consideration, x 0 some baseline input vector, and the subscript i denotes dimension i of the respective vector.",
    "The integral can be approximated numerically.",
    "The baseline vector is a vector consisting only of padding tokens.",
    "We use the library transformers-interpret from Pierse (2023) which provides a convenient interface to Captum, requiring only minimal coding (a few lines)."
  ]
}