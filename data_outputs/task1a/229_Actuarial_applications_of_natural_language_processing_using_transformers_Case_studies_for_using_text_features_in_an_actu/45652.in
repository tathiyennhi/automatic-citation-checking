{
  "texts": [
    "d. Finally, the outputs Y h are concatenated and linearly transformed into",
    "another weight matrix learned during the training process. 2.",
    "To speed up convergence of the training process, apply residual connection (also known as skip connection) (He et al., 2016) and layer normalisation (Ba et al., 2016):",
    "The fully connected feed-forward neural network uses two linear transformations and a ReLU activation in between, to produce Y 3 2 R T × E.",
    "The exact same feed-forward network is independently applied to each position of the sequence. 4."
  ]
}