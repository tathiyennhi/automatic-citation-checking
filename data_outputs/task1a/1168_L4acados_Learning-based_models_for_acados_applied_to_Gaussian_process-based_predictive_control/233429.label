{
  "style": "",
  "texts": [
    "= sin(Φ( θk))δx k (θ k) -cos(Φ( θk))δy k (θ k) e l (x k, y k, θ k).",
    "= -cos(Φ( θk))δx k (θ k) -sin(Φ( θk))δy k (θ k),as well as the control inputs Ṫk, δk, while rewarding progress along the track in terms of the negative penalty term on θk.",
    "For computational efficiency, the position x ref ( θk), y ref ( θk) and the angle Φ( θk) of the centerline in the global coordinate system are evaluated at the progress variable θk of the previous MPC solution; accordingly, the component-wise centerline deviations employ a linearization-based approximationδx k (θ k).",
    "= x k -x ref ( θk) -cos(Φ( θk)) θ k -θk, δy k (θ k).",
    "= y k -y ref ( θk) -sin(Φ( θk)) θ k -θk.Using the additional constant term in the regressor, the MPCC cost can be efficiently expressed as a nonlinear-least-squares cost with a positive definite penalty matrixQ y."
  ],
  "correct_citations": []
}