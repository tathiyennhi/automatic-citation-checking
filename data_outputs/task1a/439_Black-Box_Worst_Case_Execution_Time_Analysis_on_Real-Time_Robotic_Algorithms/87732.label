{
  "style": "",
  "texts": [
    "By subtracting the cumulative overhead of executing the timestamp function and the initial loop overhead from the total loop overhead, we derive the overhead corresponding to LOOP COUNT times the timestamp overhead.",
    "In addition, another thread is run to make the experiment more realistic.",
    "The experiment is run with the other thread having a priority less, equal, and more than the main thread.",
    "In addition, both threads are run cyclically every milliseconds.The code shown in listing 5.1 is used to measure the timestamp overhead.",
    "The code goes through a loop with LOOP COUNT iterations."
  ],
  "correct_citations": []
}