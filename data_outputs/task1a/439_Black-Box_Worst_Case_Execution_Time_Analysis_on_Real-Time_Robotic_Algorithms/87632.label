{
  "style": "IEEE",
  "texts": [
    "The learning process leverages the Bellman equation and Temporal Difference (TD) learning to update the action-value function.During Q-learning updates, the selection of future actions a t+1 is based on the behavior policy, which employs an ϵ-greedy method for exploration.",
    "This method balances exploration and exploitation by choosing the action a ′ that yields the highest Q-value with probability 1ϵ, while with probability ϵ, a random action is selected to ensure sufficient exploration of the state-action space.",
    "Policy-Based MethodsIn contrast with value-based approaches, policy-based methodologies refine the policy directly, which is parameterized by ¹.",
    "The primary goal in RL is to maximize the expected return across various states.",
    "Consequently, the objective function tailored for the policy is defined asJ(¹) = E π θ [G t],where J(¹) represents the expected return under policy Ã θ [30].Williams et al. [31] introduced the REINFORCE algorithm, which improves the policy network by following the gradient ascent of ∇ θ J(¹)."
  ],
  "correct_citations": [
    "Consequently, the objective function tailored for the policy is defined asJ(¹) = E π θ [G t],where J(¹) represents the expected return under policy Ã θ [30].Williams et al. [31] introduced the REINFORCE algorithm, which improves the policy network by following the gradient ascent of ∇ θ J(¹)."
  ]
}