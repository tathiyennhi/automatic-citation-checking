{
  "texts": [
    "Its code is shown in Listing 5.1.A total of LOOP COUNT samples are gathered.",
    "LOOP COUNT is an arbitrary value.",
    "It is set to 10,000 for this experiment.",
    "Each sample represents the average overhead of INNER LOOP COUNT timestamp calls.",
    "IN-NER LOOP COUNT is also an arbitrary value, which is set to 100 for this experiment.To isolate the overhead introduced by the loop itself, we measure the overhead incurred by a loop, where no actual operations are executed during each iteration."
  ]
}