{
  "texts": [
    "The dataset used is PG-19.",
    "Figure 5:5Figure 5: Histograms of the mean intermediate layer latent state norm from each vocab token for both GPT-2 and LLaMa.",
    "Each vocab token was input individually into each model as the initial token.",
    "Figure 6:6Figure 6: Latent state norms across each layer of both GPT-2 and LLaMa for initial token inputs.",
    "For each model, all of its vocab tokens were individually input into the model as the initial token, and the mean norm for each layer was taken over all the vocab tokens."
  ]
}