{
  "texts": [
    "The GCN layer is defined by",
    "where A is the adjacent matrix adding a self loop, D is the diagonal degree matrix with di,i = j ai,j.",
    "xi denotes the node feature vector, W is the weight matrix, and Ni is the neighbor node set of node i.",
    "For the GAT layer, the attention coefficient of node i and j is defined as ei,j = a(W xi, W xj) according to 43, and is only computed when node j is in the neighborhood of node i.",
    "Then the GAT layer can be given by"
  ]
}