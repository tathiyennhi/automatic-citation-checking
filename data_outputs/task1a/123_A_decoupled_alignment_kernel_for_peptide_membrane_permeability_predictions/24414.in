{
  "texts": [
    "Here T is a natural number user-chosen bandwidth (maximum positional lag) controlling how far from the diagonal matches are allowed.",
    "Using such ω within GA yields a p.d.",
    "kernel whose computation drops to O(T min{n, m}).",
    "What we use in this work.We employ the standard GA recursion in defined in Equation ( 1) with a chemically meaningful local kernel at the monomer level (Section 2.2), and use cosine (unitdiagonal) normalization of the resulting Gram matrix for GP inference.K(A, B) = K GA (A, B) K GA (A, A) K GA (B, B).",
    "Monomer Decoupled Global Alignment KernelRepresentation.Each cyclic peptide M is represented as an ordered sequence of monomer SMILES (s 1,..., s n) extracted from CycPeptMPDB [1]."
  ]
}