{
  "texts": [
    "Once the monitoring Lambda detects that all workers are finished,(11) it runs the reducer which produces a search result that is updated in the DynamoDB workflow table, notifying the user again through AppSync that the search is complete Fig. 1414 Fig.",
    "14 Data model.",
    "The DataConfig describes the metadata accessible through the API.",
    "Image lookup queries return an ImageLookup object containing neuron images.",
    "A NeuronImage has a GUID called id which can be queried for Matches Fig. 15 Fig."
  ]
}