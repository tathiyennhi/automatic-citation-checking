{
  "texts": [
    "Again, defining ∆(r) = m(r)-f * (r) and ∆(Q,δ) = {∆(r): -δ ≤ ∆(r) ≤ δ for r with q D r > 0}, we have that sup",
    "Consider the following choice of δ(r).",
    "Define ∆(r) = arg max -δ≤ δ≤δ g(f * (r) + δ, W r; θ)g(f * (r),W r;θ), and let δ(r) = ∆(r)1{g(f * (r)+ ∆(r)},W r;θ)-g(f * (r),W r;θ) ≥ 0}.",
    "This choice is feasible, and so it follows that sup",
    "where we further used that the triangle inequality holds with equality when all terms in a summation are non-negative."
  ]
}