{
  "texts": [
    ", e j = u i.",
    "• 3: For each state of e j, there are two alternative positions placed on the falling edge or rising of ϕ bis ′(t), if ϕ bis ′(u i+1) > ϕ bis ′(u i),ϕ bis ′(u i) can be viewed as a floor.",
    "Although, if ϕ bis ′(u i+1) < ϕ bis ′(u i), then ϕ bis ′(u i) can be found on its falling edge.",
    "• 4: Interpolatebetween the maxima to perform the adaptation of the local cut-off frequency.",
    "where (ϕ bis ′(t)) specifies the cutoff frequency; a more detailed explanation may be found in 65."
  ]
}