{
  "texts": [
    "where EMD k (■) indicates the k-th IMF mode.",
    "• Repeat Eqs.",
    "(6) and (7) until the remaining signal is unsuitable for being further decomposed through the EMD algorithm and then terminate the algorithm.",
    "Finally, the time series X(t) decomposition result may be written as follows:",
    "The VMD technique is one of the most recent signal processing methods.."
  ]
}