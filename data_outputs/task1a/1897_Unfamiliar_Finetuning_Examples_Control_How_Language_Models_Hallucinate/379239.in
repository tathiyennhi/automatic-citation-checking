{
  "texts": [
    "We use a Cosine Annealing scheduler with T max = 1e4 and ETA min = 1e-10.",
    "We use a batch size of 24.",
    "We use the AdamW optimizer with learning rate = 1e-5, betas = (0.9, 0.95), eps = 1.0e-8, and weight decay=1.0e-6.",
    "We use a Cosine Annealing scheduler with T max = 1e4 and ETA min = 1e-10.",
    "We initialize all RL finetuning with the SFT model, and use the reward predicted by the reward model described above as supervision."
  ]
}