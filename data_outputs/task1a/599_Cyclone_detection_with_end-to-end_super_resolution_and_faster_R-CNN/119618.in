{
  "texts": [
    "This allows us to calculate the Euclidean distance between the feature maps extracted from the high-resolution (HR) image (I HR) and the super-resolution (SR) image (G (I LR)) using the fine-tuned network.",
    "where W i,j andH i,j indicate the width and height of the corresponding feature map respectively.",
    "The discriminator network loss function can be formulated as in Eq.",
    "Finally, the EESN network loss function is formulated as defined in Eq.",
    "where we prioritized content accuracy (λ1 = 1), downplayed perceptual details (λ2 = 0.001), used moderate adversarial loss (λ3 = 0.01), and emphasized edge preservation (λ4 = 5)."
  ]
}