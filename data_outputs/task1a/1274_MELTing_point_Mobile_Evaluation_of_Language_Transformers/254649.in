{
  "texts": [
    "5.3.1, we trace the low-level operations during different stages of inference.",
    "Next, in Sec.",
    "5.3.2, we use profiling tools to inspect the compute and memory behavior during inference.",
    "Operations.We start by introspecting Llama-7B (3-bit) on Android.",
    "We compile a custom version of TVM and MLC-LLM where we enable the vm_profiler in the backend and report kernel runtimes per operator of interest."
  ]
}