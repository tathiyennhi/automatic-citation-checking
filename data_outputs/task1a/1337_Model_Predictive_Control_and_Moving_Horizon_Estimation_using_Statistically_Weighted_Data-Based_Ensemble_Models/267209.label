{
  "style": "IEEE",
  "texts": [
    "In detail, the cost function (6a) represents a generic objective such as economic management or setpoint tracking.",
    "Constraint (6e) imposes the initial state for each model in the ensemble (see Section V).",
    "Constraints (6f)-(6g) enforce the states and control variables to lie within the admissible sets X and U, respectively.",
    "Similarly, constraint (6h) enforces the output to lie within Y(h), where the time dependency accounts for time-varying limits, such as temperature bounds that differ between day and night in energy systems [27].",
    "The dynamical model of the system (5) is embedded in the MPC formulation through constraints (6b)-(6d).As previously discussed, existing MPC formulations embedding ensemble models leverage suboptimal combination rules for model outputs."
  ],
  "correct_citations": [
    "Similarly, constraint (6h) enforces the output to lie within Y(h), where the time dependency accounts for time-varying limits, such as temperature bounds that differ between day and night in energy systems [27]."
  ]
}