{
  "style": "",
  "texts": [
    "Before employing the Earth-Adapter, the optimization objective of fine-tuning VFMs is to identify a set of parameters that minimize the loss of the entire model on the downstream task:arg min θ (x,y)∈D L seg (f θ (f ϕ (x)), y), (1)where D is the dataset and L seg is the loss function, here we use the default CE loss.",
    "After incorporating our Earth-Adapter, the optimization objective becomes:arg min θ,ϵ,ξ (x,y)∈D L seg   fθ    R ξ • E ϵ Earth-Adapter (f * ϕ (x))   , y   .",
    "(2) During training, the original parameters of the backbone (VFM) are kept frozen (denoted as f * ϕ).",
    "In the following description, we denote the segmentation network combined with Earth-Adapter as G θ,ξ,ϵ.Mixture of Adapters.",
    "Let F i ∈ R (hw)×c denote the visual feature at the i-th layer of the backbone, where hw represents the token sequence length and c denotes the token dimension."
  ],
  "correct_citations": []
}