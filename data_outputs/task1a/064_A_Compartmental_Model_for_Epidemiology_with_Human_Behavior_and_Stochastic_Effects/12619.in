{
  "texts": [
    "In this case, x 3 is locally asymptotically stable if R 0 (s 3, s * 3) < 1, and unstable if R 0 (s 3, s * 3) > 1, where x 3 = (0, 0, s 3, 0, s * 3, 0).",
    "Our theorem follows from [53,(A1) F, V ± are component-wise nonnegative when their arguments are nonnegative.",
    "(A2) For each i = 1,..., 6, V - i = 0 when x i = 0.",
    "(Recall, that the variables are ordered It is easily seen that (A1)-(A4) hold for our system.",
    "The only assumption that could possibly fail is (A5)."
  ]
}