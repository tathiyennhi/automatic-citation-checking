{
  "texts": [
    "In particular, for an equilibrium point x 0 ∈ U DF, the classical notion of local asymptotic stability implies this notion disease free stability for {x 0 }.To apply the definition as in [35], we linearize (2.2) around a point x s,s * = (0, 0, s, 0, s *, 0) ∈ X 0 satisfying s + s * ≤ b/δ.",
    "We see(2.10) DF(x s,s *) =    β(1 -α) 2 s β(1 -α)s • • • β(1 -α)s * βs *......   ,where all other entries of are DF(x s,s *) are zero.",
    "Next, (2.11)DV + (x s,s *) =         0 ν 0 0 0 0 µs * 0 0 0 0 0 0 0 0 0 ν 0 γ 0 0 0 0 ν 0 µs µs * 0 µs µs 0 γ 0 µs * 0 0         and (2.12) DV -(x s,s *) =         γ + δ + µs * 0 0 0 0 0 0 γ + δ + ν 0 0 0 0 β(1 -α) 2 s (β(1 -α) + µ)s δ + µs * 0 µs µs 0 0 0 δ + µs * 0 0 β(1 -α)s * βs * 0 0 δ + ν 0 0 0 0 0 0 δ + ν        .",
    "We let F s,s * = β(1 -α) 2 s β(1 -α)s β(1 -α)s * βs *, V s,s * = γ + δ + µs * -ν -µs * γ + δ + ν.These are the 2 × 2 principal minors for DF(x s,s *) and DV(x s,s *) = DV -(x s,s *) -DV + (x s,s *), respectively.",
    "Then the linearization of the infectious compartments of (2.6) around x s,s * is (2.13)x 1 x 2 ′ = (F s,s * -V s,s *) x 1 x 2.We note that V s,s * is invertible: its determinant (γ + δ)(γ+ δ + ν + µs *) is positive."
  ]
}