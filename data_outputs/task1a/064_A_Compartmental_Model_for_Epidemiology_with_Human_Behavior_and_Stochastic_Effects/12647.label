{
  "style": "",
  "texts": [
    "We will again frequently use that S, I, R, S *, I *, R * remain nonnegative and that the total population is bounded by b δ.",
    "One difficulty here is that u and u * do not have a definite sign (and this transfers to n, n *) which makes it unclear how to bound certain terms.",
    "However, we do know that u + s = S ≥ 0 and u * + s * = S * ≥ 0, which also gives n + s ≥ 0 and n * + s * ≥ 0.Taking derivatives and recalling that s, s * are defined so as to satisfy (1-ξ)b-µss * +νs * -δs = 0 and ξb + µss * -(ν + δ)s * = 0, we find thatdu = (-β(1 -α)(u + s)((1 -α)v + v *) -µu(n * + s *) -µsn * + νu * -δu)dt + (-σ β (1 -α) 2 (u + s)v -σ µ (u + s)(u * + s *))dW dv = (β(1 -α)(u + s)((1 -α)v + v *) -γv -µv(n * + s *) + νv * -δv)dt + (σ β (1 -α) 2 (u + s)v -σ µ vv *)dW dw = (γv -µw(n * + s *) + νw * -δw)dt -σ µ ss * dW du * = (-β(u * + s *)((1 -α)v + v *) + µ(u + s)(n * + s *) -(ν + δ)(u * + s *))dt + (-σ β (u * + s *)v * + σ µ (u + s)(u * + s *))dW dv * = (β(u * + s *)((1 -α)v + v *) -γv * + µv(n * + s *) -(ν + δ)v * + (σ β (u * + s *)v * + σ µ vv *)dW dw * = (γv * + µw(n * + s *) -(ν + δ)w *)dt + σ µ ww * dW (4.30)We first define V 1 = (n + n *) 2, and we find (4.31)dV 1 = -2δ(n + n *) 2 dt = (-2δn 2 -2δ(n *) 2 -4δnn *)dt.Next, defineV 2 = S * + I * + R * -s * -s * log( S * +I * +R * s *) = n * -s * log( n * + s * s *).",
    "We note that V 2 ≥ 0 for n * ≥ -s * (indeed, if we treat V 2 as a function of n *, we find V ′′ 2 ≥ 0 and V ′ 2 = V 2 = 0 at n * = 0, implying that V 2 takes a minimum value of 0 at n * = 0).",
    "Also In what follows, we will collect all stochastic terms in this manner: the function f will change from line to line, but will be continuous (usually polynomial) and thus bounded on the solution values."
  ],
  "correct_citations": []
}