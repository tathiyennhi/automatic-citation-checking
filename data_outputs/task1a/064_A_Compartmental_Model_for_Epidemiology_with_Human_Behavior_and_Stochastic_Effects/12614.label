{
  "style": "",
  "texts": [
    "The second encapsulates the \"worst case scenario\" where the population is at its steady state and the entire population is noncompliant.",
    "In this case we lose the (1 -α) 2 reduction in infectivity, but the reproductive ratio is still slightly decreased due to the possibility of individuals becoming compliant.Given this definition of R 0 (s, s *), we can establish stability properties for disease free equilibria.Lemma 2.2.",
    "As defined in (2.15), the maximum value of R 0 (s, s *) for s, s * ≥ 0 and s + s * ≤ b δ occurs at (s, s *) = (0, b δ).",
    "Fixing s *, it is clear that R 0 (s, s *) is increasing in s, since it is linear with a positive slope.",
    "A quick calculation shows thatdR 0 ds * = β γ + δ γ + δ + (1 -α)ν + µs * γ + δ + ν + µs * + α(1 -α)µs(γ + δ + µ) + ανµs * (γ + δ + ν + µs *) 2 > 0which implies that R 0 (s, s *) is increasing in s * for fixed s. Thus the maximum must occur somewhere on the boundary line s where 2.+ s * = b δ."
  ],
  "correct_citations": []
}