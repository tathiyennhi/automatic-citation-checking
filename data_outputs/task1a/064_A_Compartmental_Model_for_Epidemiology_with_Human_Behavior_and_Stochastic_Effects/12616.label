{
  "style": "",
  "texts": [
    "InsertingI = I * = R = R * = 0 into (2.2), we see the disease free steady states (s, s *) satisfy(1 -ξ)b -µss * + νs * -δs = 0, ξb + µss * -νs * -δs * = 0.",
    "(2.18)Adding the equations in (2.18), we see that s + s * = b δ (which also follows from (2.3)).",
    "Inserting x * = b δ -s into the first equation and rearranging yields the following quadratic equation for the steady states of the compliant population:s 2 - b δ + ν + δ µ s + b δ ν + (1 -ξ)δ µ = 0.After some algebra, the roots of this equation can be written(2.19) s ± = 1 2   b δ + ν + δ µ ± b δ - ν + δ µ 2 + 4ξb µ  .The corresponding noncompliant populations at the DFEs are recovered from s * ± = b δ -s ±.",
    "We include some observations regarding these (omitting the proofs because they are exercises in simple if tedious algebra).Proposition 2.4.",
    "If ξ = 0, the disease free equilibria (0, 0, s, 0, s *, 0) of (2.2) are given by (2.20)x 1 = 0, 0, b δ, 0, 0, 0, x 2 = 0, 0, δ + ν µ, 0, b δ - δ + ν µ, 0.The second is only distinct and physically meaningful if b δ > δ+ν µ."
  ],
  "correct_citations": []
}