{
  "texts": [
    "Considering (s, s *) = ( b δ -θ, θ) for θ ∈ [0, b δ], we see d dθ R 0 ( b δ -θ, θ) = αβ γ + δ (1 -α) + (1 -A) (2.A = (1 -α)µθ(γ + δ + ν + µθ) + ν(γ + δ + ν) -b δ -θ (1 -α)µ(γ + δ + ν) (γ + δ + ν + µθ)By dropping the negative term in the numerator, and adding µθ into the parentheses in the second term, we see A < 1 and thus (2.17) shows that R 0 ( b δ -θ, θ) increases in θ so that the maximum occurs at (s, s *) = (0, b δ).",
    "□Given this, we can achieve DF-stability for our admissible set of disease free states defined in (2.9) with a very strong assumption on R 0 (s, s *).Theorem 2.3.",
    "If R 0 (0, b δ) < 1, then U 0 is DF-stable.Proof.",
    "This follows from lemma 2.2 and [35, Theorem 3.1] after noting that the family of linearizations (2.13) satisfy ρ(F s,s * V -1 s,s *) = R 0 (s, s *) ≤ R 0 (0, b δ) < 1, meaning that (2.13) exhibits exponential decay with decay rate which is uniform over x 0 ∈ U 0.",
    "□ Beyond this, we derive the exact values for the disease free steady states."
  ]
}