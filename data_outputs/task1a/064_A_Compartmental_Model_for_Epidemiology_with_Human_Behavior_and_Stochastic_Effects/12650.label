{
  "style": "IEEE",
  "texts": [
    "(4.41)The last thing to do is to deal with the v and v * equations.",
    "Using the linearization (2.13), we see that(4.42) d v v * = (F s,s * -V s,s *) v v * + β(1 -α)u((1 -α)v + v *) -µvn * βu * ((1 -α)v + v *) + µvn * dt + Σ v Σ v * dW,whereΣ v = σ β (1 -α) 2 (u + s)v -σ µ vv *, Σ v * = σ β (u * + s *)v * + σ µ vv *.In particular, we haveΣ 2 v, Σ 2 v * ≤ 2 b δ 4 (σ 2 β + σ 2 µ).",
    "Since we are assuming R 0 (s, s *) < 1, we know that all eigenvalues of M.. = F s,s * -V s,s * have negative real part [53].",
    "Borrowing from the theory of Sylvester equations [11,Ch. 5], [14,Ch.",
    "8], this is a necessary and sufficient condition for the existence of a symmetric positive definite matrix Q such that M t Q + QM = -I."
  ],
  "correct_citations": [
    "Since we are assuming R 0 (s, s *) < 1, we know that all eigenvalues of M.. = F s,s * -V s,s * have negative real part [53]."
  ]
}