{
  "style": "",
  "texts": [
    "To do so, starting withk 0 ∈ N such that max{S 0, I 0, R 0, S * 0, I * 0, R * 0 } ≤ k 0, min{S 0, I 0, R 0, S * 0, I * 0, R * 0 } ≥ 1 k 0,we defineτ k = inf t ∈ [0, τ E): max{S, I, R, S *, I *, R * } ≥ k or min{S, I, R, S *, I *, R * } ≤ 1 k for all k ≥ k 0.Then τ k is an increasing sequence.",
    "Further if τ k → ∞ almost surely as k → ∞, then the solution almost surely exists and remains nonnegative for all time.",
    "Suppose that it is not true that τ k → ∞ almost surely.",
    "Then(3.2) ∃T > 0, ε ∈ (0, 1), k * ≥ k 0 such that P {τ k ≤ T } ≥ ε for k ≥ k *.We let N be a bound on the total population and fix k ≥ k *.",
    "Since all populations are nonnegative up to time τ k, each of the subpopulations is also bounded by N. DefineV (t) = A∈{S,I,R,S *,I *,R * } (A(t) -1 -log A(t)).From the inequality a -1 ≥ log(a) for a > 0, we see that V is nonnegative."
  ],
  "correct_citations": []
}