{
  "texts": [
    "LLMs Knowledge distillation entails a process wherein a large teacher language model T transfers its knowledge to a small student language model S. In this framework, given an unlabeled dataset U, the teacher model generates a pseudo-answer and a pseudo-explanation for each question q in dataset U.",
    "These outputs are represented as the answer-explanation pair (completion) (â, r).",
    "This generation process is modeled as:",
    "The result is a collection of triplets {(q, â, r)} |U | as the training dataset D. Subsequently, the student model S is fine-tuned using D, employing the standard language modeling loss, formulated as:",
    "4 Problem Setup"
  ]
}