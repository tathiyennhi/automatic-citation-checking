{
  "style": "",
  "texts": [
    "By definition, H ∞ (X) = k and we want to prove a lower bound on H ∞ (X ′).",
    "Using that pX ′ (x ′) is a marginal distribution of p [X ′, X′] ([x ′, x′]), we have that H ∞ (X ′) = -log 2 (max x ′ p X ′ (x ′)) = -log 2   max x ′ x′ ∈{0,1} c p [X ′, X′] ([x ′, x′])  .",
    "(19)Continuing from Equation ( 19) by passing the maximum inside the sum and then maximise over both variables (instead of one), we get-log 2   max x ′ x′ ∈{0,1} c p [X ′, X′] ([x ′, x′])   ≥ -log 2   x′ ∈{0,1} c max x ′ p [X ′, X′] ([x ′, x′])   (20) ≥ -log 2   x′ ∈{0,1} c max [x ′,x ′] p [X ′, X′] ([x ′, x′])   = -log 2   x′ ∈{0,1} c max x p X (x)   (21) ≥ -log 2   x′ ∈{0,1} c 2 -k   = -log 2 (2 -k 2 c) = k -c (22)where we have used that maxx p X (x) ≤ 2 -k, since H ∞ (X) = k by definition.",
    "Composing extractorsExtractors can be composed together to form new extractors with improved parameters.",
    "One can append a strong seeded extractor to a strong two-source extractor, taking the two-source extractor output as a seed and the two-source weak seed as the extractor input."
  ],
  "correct_citations": []
}