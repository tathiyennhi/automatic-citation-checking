{
  "texts": [
    "that p X,Y (x, y) = p X (x)p Y (y).",
    "We call the probability p guess (X) = max x p X (x) the (maximum) guessing probability and the (maximum) conditional guessing probability p guess (X|E) = max x,e p X|E=e (x) (for more detailed discussions and explanations, see [37]).",
    "In this work, we focus on the case that the extractor inputs are bit strings, e.g.",
    "X ∈ {0, 1} n is a random variable of n bits, and X = x is its realisation.",
    "Since the realisations x are bit strings, we denote the individual bits with a subscript, e.g."
  ]
}