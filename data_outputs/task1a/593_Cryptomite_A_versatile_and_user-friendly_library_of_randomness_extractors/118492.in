{
  "texts": [
    "the first half) to fix each of the l coefficients.p α 1 (x) = s i=1 x i α s-i 1.",
    "(69)Next, we combine the Reed-Solomon step with a Hadamard Step:• Let α 2 ∈ GF(2 l) be the finite field element generated using the second l seed bits (i.e.",
    "the second half) to fix each of the l coefficients.• Output z = ⊕ l i=1 (α 2) i p α 1 (x) i, where ⊕ denotes addition modulo 2.Total Trevisan computation time: The combined computation time is O(m log(m)t log(t) log(log(t)) weak design + msl log(l) m 1-bit extractions) (70) and we use the following relationships to determine the overall computation time in terms of n and ϵ 1: n ≈ m s = n l t ≈ O log(n) + log 2 ϵ 1 = O log n ϵ 1 l = t 2 = O log n ϵ 1.",
    "(71) This allows us to re-write Equation (70) as O n log(n) log n ϵ 1 log log n ϵ 1 log log log n ϵ 1 + n 2 log log n ϵ 1 (72) ≈ O      n log(n) log n ϵ 1 weak design + n 2 log log n ϵ 1 m 1-bit extractions     The weak design could be pre-computed to reduce the overall computation time of the Trevisan extraction, but the dominant term in Equation ( 73) comes from the m 1-bit extractions.",
    "A.7 Von Neumann implementationThe Von Neumann extractor is implemented in the following pseudo-code.Algorithm 3 Implementation of the Von Neumann Extractor end for 8:error ▷ Error if all bits are the same."
  ]
}