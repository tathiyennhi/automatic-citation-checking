{
  "style": "",
  "texts": [
    "n_1: integer, the length of the raw key bit string.",
    "m: integer, the length of the output secret key bit string.",
    "Returns --------list of bits,the extracted output (i.e.",
    "the shared secret key). \"\"\"",
    "# Initialise the Toeplitz extractor with the appropriate parameters: toeplitz = cryptomite.Toeplitz(n_1, m) # Perform Toeplitz extraction and return the output: return toeplitz.extract(raw_key_bits, seed_bits) import cryptomite from math import floor, log2 def improved_privacy_amplification(raw_key_bits: list, seed_bits: list, n_1 = 1.738 * 10**9, epsilon = 10**(-10), m = 41378264, r = 0.99) \"\"\" Perform improved privacy amplification for the QKD protocol."
  ],
  "correct_citations": []
}