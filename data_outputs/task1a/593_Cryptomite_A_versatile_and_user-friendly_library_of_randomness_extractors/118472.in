{
  "texts": [
    "k_1: float, the total min-entropy of the raw randomness.",
    "r: float, the min-entropy rate of the extractor (now weak) seed.",
    "Returns --------list of bits,the extracted output (i.e.",
    "the near-perfect randomness). \"\"\"",
    "# To use Circulant, the seed length needs to be a prime # with primitive root 2, we find the one closest # to n_1 + 1 (see Def."
  ]
}