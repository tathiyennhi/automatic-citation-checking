{
  "texts": [
    "The initial RSH seed is of length t ≥ 2l, where l is l = ⌈log 2 (n) + 2 log 2 (2/ϵ 1)⌉, (68) from [21], 'Section III.",
    "Polynomial hashing'.",
    "The first step in the RSH extractor is the Reed-Solomon step.",
    "This is implemented as follows:• Split the extractor input x into s = ⌈n/l⌉ chunks x i. Pad the last chunk x s with 0s if need be to make all chunks of length l.• View each chunk x i as an individual element in the field of GF(2 l) i.e.",
    "the binary representation of it, for example, if l = 3 and x 0 = {0, 0, 1} then x 0 = 1 in the field GF(2 3 = 8).• Then, we evaluate the polynomial Equation (69) (calculated using finite field multiplication) for each of the s elements x i in GF(2 l) constructed form the extractor input, where α 1 ∈ GF(2 l) is the element generated by using the first l seed bits (i.e."
  ]
}