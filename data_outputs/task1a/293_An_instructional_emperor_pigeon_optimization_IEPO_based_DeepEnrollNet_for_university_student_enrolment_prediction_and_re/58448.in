{
  "texts": [
    "The input cube is convolved with numerous learnable filters at each convolutional layer, producing a variety of feature maps.",
    "For example, assume that X is the i/p and that its size is mX n Xd, where m, n denotes spatial size, d denotes its channel count, and denotes the i th feature map of M. The following can be used to represent the convolutional layer's j th output by using the formula shown by Eq.",
    "f (mi * W j + B j) (16) f (•) activation function that improves nonlinearity.",
    "ReLu is used and it is given as per Eq.",
    "The pooling layers are occasionally added after multiple convolutional layers in the CNNs because redundant information in images exists."
  ]
}