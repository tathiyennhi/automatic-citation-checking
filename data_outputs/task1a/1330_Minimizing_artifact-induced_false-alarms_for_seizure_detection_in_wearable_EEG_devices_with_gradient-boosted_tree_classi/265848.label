{
  "style": "APA",
  "texts": [
    "Therefore, in 42 we applied pruning on the DTs with a MCCP algorithm 61 in order to prune the DTs down such that the tree would fit on the L1/L2 memory of the targeted processor.",
    "Additionally, since Gradient Boosted models (and in particular, XGBoost) proved to perform better than other tree-based models for seizure classification, we also explore their applicability.",
    "To this end, we use the TPOT-optimized hyperparameters and prune the trees for fitting the model on the L1/L2 memory of GAP9.",
    "The Gradient Boosting Classifier is a machine-learning technique that utilizes an ensemble of regression trees.",
    "Each tree in the ensemble is constructed by repeatedly splitting internal nodes based on specific features and threshold values."
  ],
  "correct_citations": [
    "Therefore, in 42 we applied pruning on the DTs with a MCCP algorithm 61 in order to prune the DTs down such that the tree would fit on the L1/L2 memory of the targeted processor."
  ]
}