{
  "texts": [
    "The full performance of logistic regression models.",
    "The bootstrapped AUROC/ AUPRC is determined the 30% held-out evaluation set (n = 300 bootstrapped iterations of 1000 patients).",
    "The box shows quartiles (25%, 50%, 75%ile), and whiskers extend to 1.5*interquartile range, with remaining points as outliers. b.",
    "Top detailed OMOP feature logistic regression coefficients are listed by importance for all model formulations.",
    "Top row shows coefficients from the model trained on all patients, while the bottom row shows coefficients from the model trained on matched cohorts."
  ]
}