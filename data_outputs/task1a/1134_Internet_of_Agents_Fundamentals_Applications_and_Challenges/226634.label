{
  "style": "IEEE",
  "texts": [
    "• IoT uses a decentralized end-edge-cloud architecture, often with brokered or P2P messaging (e.g., MQTT, CoAP) [48], [49], allowing devices to offload processing to edge or cloud nodes.",
    "• IoA adopts a hybrid decentralized architecture [8], combining P2P overlays and proxy gateway agents, enabling agents to join, leave, and collaborate across domains on demand.",
    "Agents can dynamically join or leave the network.",
    "Communication is driven by semantic interaction protocols and task-oriented networking, enabling agents to announce capabilities and initiate collaborations.3) Addressing & discovery mechanisms among the Internet, IoT, and IoA:• Traditional Internet relies on static IP addresses and DNS resolution, binding identifiers to physical locations or service entry points (e.g., 192.168.1.1 or www.example.com).Resource discovery is typically via manual uniform resource locator (URL) lookup or search engines.",
    "• IoT extends IP addressing with static device identifier and supports automated discovery using protocols such as multicast DNS (mDNS) and CoAP resource directories, and lightweight service discovery [48], [49]."
  ],
  "correct_citations": [
    "• IoT uses a decentralized end-edge-cloud architecture, often with brokered or P2P messaging (e.g., MQTT, CoAP) [48], [49], allowing devices to offload processing to edge or cloud nodes.",
    "• IoA adopts a hybrid decentralized architecture [8], combining P2P overlays and proxy gateway agents, enabling agents to join, leave, and collaborate across domains on demand.",
    "• IoT extends IP addressing with static device identifier and supports automated discovery using protocols such as multicast DNS (mDNS) and CoAP resource directories, and lightweight service discovery [48], [49]."
  ]
}