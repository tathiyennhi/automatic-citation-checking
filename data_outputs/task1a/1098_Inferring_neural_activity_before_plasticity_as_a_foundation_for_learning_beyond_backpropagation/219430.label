{
  "style": "APA",
  "texts": [
    "First, based on the example in Fig. 1, we expect prospective con figuration to require fewer episodes for learning than backpropagation.",
    "Before presenting the comparison, we describe how backpropagation is used to train ANNs.",
    "Typically, the weights are only modified after a batch of training examples based on the average of updates derived from individual examples (Fig. 4a).",
    "In fact, backpropagation relies heavily on averaging over multiple experiences to reach humanlevel performance 32, as it needs to stabilize training 33.",
    "By contrast, biological systems must update the weights after each experience, and we com pare learning performance in such a setting."
  ],
  "correct_citations": [
    "In fact, backpropagation relies heavily on averaging over multiple experiences to reach humanlevel performance 32, as it needs to stabilize training 33."
  ]
}