{
  "style": "APA",
  "texts": [
    "where g: R → [0, 1] is a monotone non-decreasing function (with g(z) = 1g(-z)) that converts reward differences into winning probabilities.",
    "When g is the sigmoid function σ(x) = 1 1+e -x, we get the Bradley-Terry (BT) preference model (Bradley and Terry, 1952).",
    "Given dataset D, containing feedback (x, y w, y l), the goal is to learn a language model policy π(y | x) to align the human preference.",
    "Given the reward function r(x, y), denoting the human preferences, RLHF fine-tunes policy π θ by optimizing the following objective:",
    "where β > 0 is an appropriate KL penalty coefficient."
  ],
  "correct_citations": [
    "When g is the sigmoid function σ(x) = 1 1+e -x, we get the Bradley-Terry (BT) preference model (Bradley and Terry, 1952)."
  ]
}