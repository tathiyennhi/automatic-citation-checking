{
  "style": "APA",
  "texts": [
    "where f * (x, y) is the density ratio estimated by Equation (19) on the preference dataset.",
    "Since the optimal density ratio is now represented in terms of the optimal policy, as opposed to the discriminator model, we can explicitly derive the following maximum likelihood objective for a parameterized policy over the preference dataset (Rafailov et al., 2024).",
    "Analogous to the approach used for density ratio estimation and using a change of variables, we can formalize the reverse KL objective as follows: Anthropic-HH (Bai et al., 2022): The Anthropic Helpful and Harmless dataset foot_1 contains 170,000 dialogues between humans and large language model assistants, used for single-turn dialogue evaluation tasks.",
    "Each dialogue includes a human prompt along with two model-generated responses, rated based on helpfulness and harmlessness.",
    "Consistent with DPO (Rafailov et al., 2024), we utilized the chosen responses during the SFT stage."
  ],
  "correct_citations": [
    "Since the optimal density ratio is now represented in terms of the optimal policy, as opposed to the discriminator model, we can explicitly derive the following maximum likelihood objective for a parameterized policy over the preference dataset (Rafailov et al., 2024).",
    "Analogous to the approach used for density ratio estimation and using a change of variables, we can formalize the reverse KL objective as follows: Anthropic-HH (Bai et al., 2022): The Anthropic Helpful and Harmless dataset foot_1 contains 170,000 dialogues between humans and large language model assistants, used for single-turn dialogue evaluation tasks.",
    "Consistent with DPO (Rafailov et al., 2024), we utilized the chosen responses during the SFT stage."
  ]
}