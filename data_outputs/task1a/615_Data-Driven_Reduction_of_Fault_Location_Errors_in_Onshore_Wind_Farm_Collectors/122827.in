{
  "texts": [
    "Within each group, the feature with thehighest MI was retained (Table 6).",
    "Features with MI scores below 0.1 werediscarded, resulting in a final feature set that is highly informative, non-redundant, and suitable for training the GRN-MM model.",
    "Table 6:6Selected features from highly correlated groups.FeaturesChosenFeaturesChosenin GroupFeaturein GroupFeaturecos(I F), cos(I loop) cos(I loop) cos(I pre loop), cos(V pre loop) cos(I pre loop)sin(I F), sin(I loop) sin(I loop) sin(I pre loop), sin(V pre loop) sin(V pre loop) Table 7:7Results of the hyperparameters tuning via Optuna.Parameter Best Parameter Parameter Importancehidden_dim19212%num_blocks237%dropout0.12987%lr0.0082844%lationship between hyperparameters and the objective function (based onMAE) over 2,000 trials, illustrates this sensitivity.",
    "The best results wereconsistently obtained with learning rates within a narrow range (approxi-mately 0.005 to 0.01).",
    "Similarly, models with 2 to 6 residual blocks generallyoutperformed deeper architectures, suggesting that excessive model depthmay lead to overfitting."
  ]
}