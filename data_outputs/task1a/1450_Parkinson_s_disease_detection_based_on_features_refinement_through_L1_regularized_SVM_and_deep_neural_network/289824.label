{
  "style": "APA",
  "texts": [
    "More likely, irrelevant features present in the feature space are also modeled accordingly.",
    "Noise is the result of Modeling irrelevant features 32.",
    "Thus, learning the noise from these irrelevant features negatively affects the acquired knowledge of data about the overall distribution of the data 32.",
    "If feature space contains irrelevant features, overfitting the network to the training data is another problem 32, 41.",
    "That is when the network learns irrelevant details from the training data."
  ],
  "correct_citations": [
    "Noise is the result of Modeling irrelevant features 32.",
    "Thus, learning the noise from these irrelevant features negatively affects the acquired knowledge of data about the overall distribution of the data 32.",
    "If feature space contains irrelevant features, overfitting the network to the training data is another problem 32, 41."
  ]
}