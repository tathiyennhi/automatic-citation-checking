{
  "texts": [
    "In 1995, Cortes and Vapnik proposed a modified version of SVM called Soft Margin SVM, which allows for mislabeled instances 38, and it has the following form:",
    "where the regularizer or penalty function is L 2 -norm, C > 0 is the error penalty parameter and ξ is slack variable used for misclassification measurement.",
    "In 1998, Bradley and Mangasarian proposed to use L 1 -norm as the regularizer 39, and the feature selection can be made using L 1 -norm SVM due to its sparse solutions.",
    "It is formulated as: where the regularizer or penalty function is L 1 -norm, C > 0 is the error penalty parameter and ξ is slack variable used for misclassification measurement.",
    "As discussed above, in (3), w is the weight vector."
  ]
}