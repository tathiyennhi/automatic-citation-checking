{
  "style": "APA",
  "texts": [
    "Loss Function of HINet: -",
    "where, i = The number of subnetworks, X i = Input of the subnetwork i, R i = output of the subnetwork i, Y = Ground Truth.",
    "The SRPFMNet model initially upsamples input images using bicubic interpolation and then processes the upsampled images through a novel Function Mixture Block (FMB) module (Zhang et al. 2020).",
    "The FMB module is designed to adaptively determine the optimal receptive field size and mapping function for each pixel, utilizing a separation aggregation strategy.",
    "At the outset, a basic convolution layer followed by a ReLU activation function extracts primary features."
  ],
  "correct_citations": [
    "The SRPFMNet model initially upsamples input images using bicubic interpolation and then processes the upsampled images through a novel Function Mixture Block (FMB) module (Zhang et al. 2020)."
  ]
}