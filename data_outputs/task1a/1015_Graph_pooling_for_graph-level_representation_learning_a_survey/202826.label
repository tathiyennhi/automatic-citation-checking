{
  "style": "APA",
  "texts": [
    "DiffPool (Zhang et al. 2020) learns the soft assignment matrix S l by the graph neural network, which can be denoted as:",
    "where sof tmax is a row-by-row computed softmax function that can output the clusters corresponding to the node.",
    "DiffPool is a soft assignment matrix computed using a one-layer graph neural network.",
    "This soft assignment matrix is a dense matrix, which can lead to inefficient computation for large-scale graphs.",
    "In addition, DiffPool is sensitive to the maximum number of clusters, and its cluster assignment varies depending on the depth of the network and the maximum number of clusters."
  ],
  "correct_citations": [
    "DiffPool (Zhang et al. 2020) learns the soft assignment matrix S l by the graph neural network, which can be denoted as:"
  ]
}