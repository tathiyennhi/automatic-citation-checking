{
  "texts": [
    "This representation is then fed into a Multilayer Perceptron (MLP) for downstream tasks.",
    "denotes the query vector",
    "~ /py-team/pytorch_geometric/blob/master/ torch_geometric/nn/aggr/ set2set.py SortPool (Zhang et al. 2018a) SortPool = k × h 1 c t k denotes a user-defined integer ~ /muhanzhang/DGCNN SOPool (Wang and Ji 2020)",
    "layer normalization, feedforward layer, and parameterized seed matrix, respectively ~ /py-team/ pytorch_geometric/blob/master/ torch_geometric/nn/aggr/ gmt.py 1 3",
    "Summation, averaging, and maximization, as early global pooling methods, dominate in computational efficiency due to their property of directly aggregating all node representations without the need for complex eigendecomposition, which led to the widespread adoption of these methods in the early days (Henaff et al. 1506; Yang et al. 2301)."
  ]
}