{
  "texts": [
    "where GCN is Graph Convolutional Neural Network which mainly encodes the input graph.",
    "GCN can be any message-passing-based graph neural network, which can be denoted as:",
    "where h l v is the representation of node v and ℵ (v) denotes the neighbors of node v.",
    "Graph pooling is a method used in graph neural networks to reduce the complexity of graph data, which is similar to the spatial pooling operation for image data in convolutional neural networks (Grattarola et al. 2022).",
    "In a convolutional neural network, the pooling layer reduces the spatial dimensionality by sampling the image region (e.g., maximum or mean sampling) while keeping key features constant or reducing the risk of overfitting."
  ]
}