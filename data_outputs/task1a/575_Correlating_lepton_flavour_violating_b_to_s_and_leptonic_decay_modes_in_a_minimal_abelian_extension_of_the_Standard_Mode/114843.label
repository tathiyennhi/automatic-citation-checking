{
  "style": "IEEE",
  "texts": [
    "( 28) one finds (C N P 10) τ τ ≃ (C N P 10) µµ + f (g Z, ϵ 1, ϵ 2),(77)where f depends on the scanned parameters.",
    "Hence, one writesB(B s → τ + τ -) ≃ λ 1/2 (m 2 Bs, m 2 τ, m 2 τ) λ 1/2 (m 2 Bs, m 2 µ, m 2 µ) B(B s → µ + µ -) + f (g Z, ϵ 1, ϵ 2),(78)where f (g Z, ϵ 1, ϵ 2) denotes the terms taking into account the function f in Eq.",
    "This relation shows that the experimental bound on B(B s → µ + µ -) also limits B(B s → τ + τ -).For the lepton flavour conserving modes B → K * ℓ + ℓ -, with ℓ = µ, τ, we use the local form factors determined in [76] and described in appendix A.",
    "Figure 8 shows the correlation between the branching fractions B( B0 → K * 0 µ + µ -) and B( B0 → K * 0 τ + τ -).",
    "The vertical band corresponds to the measurement B( B0 → K * 0 µ + µ -) exp = (9.4 ± 0.5) × 10 -7 [30]."
  ],
  "correct_citations": [
    "This relation shows that the experimental bound on B(B s → µ + µ -) also limits B(B s → τ + τ -).For the lepton flavour conserving modes B → K * ℓ + ℓ -, with ℓ = µ, τ, we use the local form factors determined in [76] and described in appendix A.",
    "The vertical band corresponds to the measurement B( B0 → K * 0 µ + µ -) exp = (9.4 ± 0.5) × 10 -7 [30]."
  ]
}