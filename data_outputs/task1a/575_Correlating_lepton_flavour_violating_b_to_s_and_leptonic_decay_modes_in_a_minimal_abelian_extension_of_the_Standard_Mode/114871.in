{
  "texts": [
    "The vertical line corresponds to the experimental upper bound for B(µ -→ e -γ) [94].",
    "The light yellow points are selected requiring that the LFC B( B0 → K * 0 µ + µ -) agrees within 1σ with experiment.",
    "Figure 19:19Figure 19: Correlation between B(B s → τ + µ -) and B(µ -→ e -e + e -) (top panels) and between B( B0 → K * 0 τ + µ -) and B(µ -→ e -e + e -) (bottom panels), for M Z ′ = 1 TeV (left panel) and M Z ′ = 3 TeV (right panel).",
    "The gray band corresponds to the upper bound B(µ -→ e -e + e -) < 1 × 10 -12 (at 90% CL) [30, 98].",
    "Figure 20:20Figure 20: Correlation between B(B s → τ + µ -) and B(µ -→ e -) due to conversion in nuclei (top panels) and between B( B0 → K * 0 τ + µ -) and B(µ -→ e -) due to conversion in nuclei (bottom panels), for M Z ′ = 1 TeV (left panel) and M Z ′ = 3 TeV (right panel)."
  ]
}