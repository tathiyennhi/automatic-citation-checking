{
  "style": "IEEE",
  "texts": [
    "The bound reduces the room for B(B s → τ + µ -).",
    "This provides us with a clear example of the most peculiar feature of the ABCD model, the interplay between quark and lepton observables.",
    "Within the model, quite large values of B(µ -→ e -γ) could be obtained, however, the requirement that the rate of B s → µ + µ -agrees with experiment suppresses B(µ -→ e -γ).",
    "The correlation between B( B0 → K * 0 τ + µ -) and B(µ -→ e -γ) is displayed in Fig. 18, and similar remarks hold: the bound on B(µ -→ e -γ) [94] reduces the allowed range for B(B → K * τ + µ -), see Table 1.",
    "The same occurs for B s → ϕτ + µ -."
  ],
  "correct_citations": [
    "The correlation between B( B0 → K * 0 τ + µ -) and B(µ -→ e -γ) is displayed in Fig. 18, and similar remarks hold: the bound on B(µ -→ e -γ) [94] reduces the allowed range for B(B → K * τ + µ -), see Table 1."
  ]
}