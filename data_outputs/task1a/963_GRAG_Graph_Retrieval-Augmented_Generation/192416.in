{
  "texts": [
    "The subset S N (G) ⊆ S(G) contains the N subgraphs with the highest semantic relevance to the query.",
    "Textual Subgraph Soft Pruning.",
    "Although we retrieve relevant subgraphs, some irrelevant nodes and edges may still be present, which can negatively impact the final generation.",
    "Therefore, we leverage a soft pruning approach to minimize the influence of these irrelevant entities.",
    "Specifically, we use two Multilayer Perceptrons (MLPs) to learn a scaling factor based on the distance between nodes & edges and the query as follows:"
  ]
}