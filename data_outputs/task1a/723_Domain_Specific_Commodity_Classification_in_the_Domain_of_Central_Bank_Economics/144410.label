{
  "style": "IEEE",
  "texts": [
    "This allows for parallization of the d v -dimentsional output values.",
    "The heads are concatenated as in the formula below, where h is the number of heads.",
    "Where the porjections are paramter matrices W and each matrices is represented byW Q i ∈ R d model ×d k the weight matrix of Queries, W K i ∈ R d model ×d k the weight matrix of keys, W V i ∈ R d model×dv the weight matrix of values and W O ∈ R hdvXd model is the parameter matrix of weights of the model [67].M utliHead(Q, K, V) = Concat(head 1,..., head h)W O where head i = Attention(QW Q i, KW K i, V W V i)Multi-head gives the advantage of jointly attending different representation subspaces at different positions within the sentence [67].Encoder: The encoder is made up of a stack of N = 6 layers.",
    "Each layer has two sublayers.",
    "The first is a multi-head self-attention mechanism, and the second is a simple, position-wise, fully connected feed-forward network."
  ],
  "correct_citations": [
    "Where the porjections are paramter matrices W and each matrices is represented byW Q i ∈ R d model ×d k the weight matrix of Queries, W K i ∈ R d model ×d k the weight matrix of keys, W V i ∈ R d model×dv the weight matrix of values and W O ∈ R hdvXd model is the parameter matrix of weights of the model [67].M utliHead(Q, K, V) = Concat(head 1,..., head h)W O where head i = Attention(QW Q i, KW K i, V W V i)Multi-head gives the advantage of jointly attending different representation subspaces at different positions within the sentence [67].Encoder: The encoder is made up of a stack of N = 6 layers."
  ]
}