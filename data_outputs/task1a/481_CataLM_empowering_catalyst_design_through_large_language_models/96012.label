{
  "style": "IEEE",
  "texts": [
    "For the establishment of vector databases, Sci-BERT [36] has been utilized as an embedding model.The study involved retrieving titles and abstracts from a dataset containing 12,643 documents, manually annotating catalytic reaction processes by domain experts, and then merging and converting these textual elements into vector representations using Sci-BERT as an embedding framework.",
    "In the context of a catalytic domain-specific task such as Name Entity Recognition (NER), the embedding model operates by converting the user query into vector form.",
    "Relevant articles are then identified by vector distance calculations to facilitate the retrieval of accurate and relevant information.",
    "Instruction tuningIn order to align pre-trained models with domain user intent, we need to construct instruction tuning datasets.",
    "Currently available generic instruction tuning datasets such as Alpaca-GPT4 [37] and ToolBench [38] can only teach models to follow human instructions."
  ],
  "correct_citations": [
    "For the establishment of vector databases, Sci-BERT [36] has been utilized as an embedding model.The study involved retrieving titles and abstracts from a dataset containing 12,643 documents, manually annotating catalytic reaction processes by domain experts, and then merging and converting these textual elements into vector representations using Sci-BERT as an embedding framework.",
    "Currently available generic instruction tuning datasets such as Alpaca-GPT4 [37] and ToolBench [38] can only teach models to follow human instructions."
  ]
}