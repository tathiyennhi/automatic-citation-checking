{
  "texts": [
    "( 4).Dm = σ (T k. [Um-1, Lm] + J k)(4)Finally, the outcome gates are utilized to generate the data gathered in the cell state, which is offered in Eq.",
    "(5).Sm = σ (T k. [Um-1, Lm] + Je) (5)Here, the weights are represented as T, bias is denoted as J in the training model, hidden states are specified as U, and the cell state is presented as L to the corresponding gates.",
    "Hence, the LSTM model has higher efficiency in resolving the gradient vanishing issues.",
    "The general architecture of the LSTM is signified in Fig. 4.",
    "Res-LSTMRes-LSTM 42 is the extended version of the classical LSTM structure, which introduces a residual connection that permits collecting the long-term dependencies and complicated temporal patterns fused with the sequential information."
  ]
}