{
  "style": "APA",
  "texts": [
    "The MN judge predicts the most likely score from a set S. The probability of score s ∈ S is",
    "where ϕ(e) ∈ R d is the embedding of base judge's rationale, p s is the probability that the base judge predicts score s, p = (p s) s∈S ∈ ∆ S is a probability vector, θ s ∈ R d+1 is a learned model parameter for score s, and c s is a population bias towards s. We denote all learned parameters by Θ = (θ s) s∈S and estimate p s using the next token probability in e (Gu et al., 2025, Section 2.3.3).",
    "Since (2) is equivalent to the probability of outcome s in multinomial logistic regression (Murphy, 2012, Chapter 8), the population bias c s plays the same role.",
    "We introduce p so that we can always learn a judge that performs at least as well as the base judge.",
    "Specifically, when θ s = 0 d ⊕ 1 and c s = 0 for all s ∈ S, the predicted probability becomes the base judge's probability because"
  ],
  "correct_citations": [
    "where ϕ(e) ∈ R d is the embedding of base judge's rationale, p s is the probability that the base judge predicts score s, p = (p s) s∈S ∈ ∆ S is a probability vector, θ s ∈ R d+1 is a learned model parameter for score s, and c s is a population bias towards s. We denote all learned parameters by Θ = (θ s) s∈S and estimate p s using the next token probability in e (Gu et al., 2025, Section 2.3.3).",
    "Since (2) is equivalent to the probability of outcome s in multinomial logistic regression (Murphy, 2012, Chapter 8), the population bias c s plays the same role."
  ]
}