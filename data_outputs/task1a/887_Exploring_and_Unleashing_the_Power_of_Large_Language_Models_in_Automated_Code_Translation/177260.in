{
  "texts": [
    "67 Source Program Translated Program Ground Truth Program return * max_element(arr, arr+n); //source C++ code //gets the max value of the top-n items.",
    "return Arrays.stream(arr).max().getAsInt(); //incorrect Java code //gets the max value of the whole array.",
    "Arrays.sort(arr, 0, n); return arr[n-1]; //ground truth Java code Syntax Incorrectly introduces the syntax of the source PL to the target PL or makes the syntactic errors in the target PL. return (!",
    "x: z; //source C++ code return (!",
    "x: z; //incorrect Java code //\"!\""
  ]
}